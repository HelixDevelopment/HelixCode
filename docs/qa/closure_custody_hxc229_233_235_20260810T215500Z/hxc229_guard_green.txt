PASS-with-caveat (HXC-229): pid=107268 carries GIN_MODE=release, read from the live process's own /proc/107268/environ — the primary invariant is ENFORCED and holds. CAVEAT: the corroborating route-dump cross-check could NOT run — the journal window since '2026-08-08 22:08:59 +05' holds 437 line(s) but no startup marker (rotated or vacuumed; expected on a long-lived process). A debug-mode gateway dumps its routes only at startup, so a clean scan of this window proves nothing on its own and is NOT claimed as evidence here. What is claimed: the running process's environment, plus 0 GIN-debug line(s) in the window (a hit would have FAILed above regardless of provability). To restore the full cross-check, restart the unit and re-run.
EXIT=0
