# tools/shell: the drain grace is a no-progress window, not a total output budget — QA evidence (§11.4.83)

**Run ID:** `racefix_shell_grace_fa1b52b3_20260728T232856Z`
**Source commit:** `fa1b52b32992d9ae0dac86643f9e0e28a55f252a`
**Captured (UTC):** 2026-07-28T23:29:50Z
**Verdict:** PASS
**Counts:** PASS=28 FAIL=0 SKIP=0

## Commit under test
```
fa1b52b32992d9ae0dac86643f9e0e28a55f252a
Test User
2026-07-29T03:19:18+05:00
fix(tools/shell): make the drain grace a no-progress window, not a total budget
```

## How this evidence was produced

Generated by `scripts/qa/capture_racefix_wave.sh` — a real HTTP capture harness.
Every row below was evaluated against bytes observed on the wire during this run;
the full bidirectional trace for each case (request lines sent, response headers and
body received) is in `transcripts/<case-id>.http`. The harness FAILs any assertion
whose transcript is missing or contains no response — it cannot emit a PASS without
captured evidence (§11.4 / §11.4.1 / §11.4.5).

Exit codes: `0`=all PASS, `1`=a security assertion FAILED, `2`=INCOMPLETE (a case was
SKIPped with reason, §11.4.3 — never counted as a pass).

## Per-assertion verdicts

| Result | Case | Assertion | Expected | Observed |
|--------|------|-----------|----------|----------|
| PASS | `host_context` | host context captured as a recorded fact (not a pass/fail criterion) | `0` | `0` |
| PASS | `tree_clean` | no uncommitted local edit to the files under test (§11.4.108: tested source == shipped source) | `0` | `0` |
| PASS | `sig_progress` | runtime signature present in the shipped tracked source: OutputStreamer exposes a monotonic Progress() counter — the signal the drain loop rearms on | `0` | `0` |
| PASS | `sig_progress` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `sig_rearm` | runtime signature present in the shipped tracked source: the drain deadline is REARMED while lines are still moving (the window is no longer a total budget) | `0` | `0` |
| PASS | `sig_rearm` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `sig_flagderived` | runtime signature present in the shipped tracked source: OutputIncomplete is DERIVED from what the scanners observed, not from which select branch won (M1/M3) | `0` | `0` |
| PASS | `sig_flagderived` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `sig_errpriority` | runtime signature present in the shipped tracked source: Err() prefers a genuine failure over the ErrStreamStopped teardown sentinel (M2) | `0` | `0` |
| PASS | `sig_errpriority` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `guard_notruncate` | guard tests exit 0 under -race -count=2 | `0` | `0` |
| PASS | `guard_notruncate` | guard TestExecuteStreamDoesNotTruncateASlowButDrainingConsumer genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestExecuteStreamDoesNotTruncateASlowButDrainingConsumer` | `present` |
| PASS | `guard_notruncate` | race detector reported no data race | `absent` | `absent` |
| PASS | `guard_notruncate` | the -run filter matched at least one test | `absent` | `absent` |
| PASS | `guard_hxc184` | guard tests exit 0 under -race -count=2 | `0` | `0` |
| PASS | `guard_hxc184` | guard TestExecuteStreamReturnsWhenGrandchildHoldsPipes genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestExecuteStreamReturnsWhenGrandchildHoldsPipes` | `present` |
| PASS | `guard_hxc184` | guard TestExecuteStreamHangDoesNotWedgeTheExecutor genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestExecuteStreamHangDoesNotWedgeTheExecutor` | `present` |
| PASS | `guard_hxc184` | guard TestExecuteStreamDoneBeforeDrainIsBoundedNotDeadlocked genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestExecuteStreamDoneBeforeDrainIsBoundedNotDeadlocked` | `present` |
| PASS | `guard_hxc184` | race detector reported no data race | `absent` | `absent` |
| PASS | `guard_hxc184` | the -run filter matched at least one test | `absent` | `absent` |
| PASS | `red_hxc184` | RED_MODE=1 FAILS on the fixed artifact — the guard is falsifiable, not blind (§11.4.115) | `not 0` | `1` |
| PASS | `red_hxc184` | guard TestExecuteStreamReturnsWhenGrandchildHoldsPipes genuinely RAN and reported FAIL under RED_MODE=1 (not a compile error masquerading as a reproduction) | `--- FAIL: TestExecuteStreamReturnsWhenGrandchildHoldsPipes` | `present` |
| PASS | `red_hxc184` | guard TestExecuteStreamHangDoesNotWedgeTheExecutor genuinely RAN and reported FAIL under RED_MODE=1 (not a compile error masquerading as a reproduction) | `--- FAIL: TestExecuteStreamHangDoesNotWedgeTheExecutor` | `present` |
| PASS | `red_hxc184` | the -run filter matched at least one test | `absent` | `absent` |
| PASS | `red_hxc184` | the non-zero exit came from the guard, not from a build failure | `absent` | `absent` |
| PASS | `suite_shell` | full package suite exits 0 under -race | `0` | `0` |
| PASS | `suite_shell` | race detector reported no data race across the package | `absent` | `absent` |
| PASS | `suite_shell` | no test in the package failed | `absent` | `absent` |

## Grounding (§11.4.108)

```
# Grounding facts captured 2026-07-28T23:28:56Z
# §11.4.108: the running artifact must be the one built from the fixed source.

## repo HEAD
faadf59e docs(tracker): re-sync derived docs after HXC-201 was filed

## fix commit under test
fa1b52b3 fix(tools/shell): make the drain grace a no-progress window, not a total budget

## fix commit is an ancestor of HEAD?
YES — fa1b52b32992d9ae0dac86643f9e0e28a55f252a <= HEAD

## live server under test
base url: <unset>
(no listening helixcode process found via ss; grounding limited)
```

## Files

- `verdicts.tsv` — machine-readable per-assertion results
- `grounding.txt` — artifact/runtime identity facts
- `transcripts/*.http` — full bidirectional wire traces

## Correction to the preamble above (§11.4.6)

The "How this evidence was produced" section above is boilerplate emitted by the
shared harness `scripts/qa/lib/sec_capture_lib.sh`, which was written for
HTTP-shaped captures. It is inaccurate for THIS run in two specifics, corrected
here rather than left to mislead:

* This is **not** an HTTP capture. No HTTP request is made and nothing is
  observed "on the wire". Every case is a real COMMAND execution
  (`scripts/qa/lib/cmd_capture_lib.sh`), captured with its full combined
  stdout+stderr and its real exit code.
* Transcripts are `transcripts/<case-id>.**txt**`, not `.http`. Each one records
  the command sent (`### command:`), the working directory, and everything the
  command returned — the bidirectional record §11.4.83 asks for, in
  command/response rather than request/response form.

What is accurate as written: no assertion can emit a PASS without a captured,
non-empty transcript carrying an exit-code trailer, and a SKIP is never counted
as a pass. See `NOTES.md` in this directory for what this specific run does and
does not certify.
