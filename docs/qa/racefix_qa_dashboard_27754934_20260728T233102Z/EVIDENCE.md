# terminal_ui: the QA dashboard renders from snapshots, not live session pointers (HXC-174) — QA evidence (§11.4.83)

**Run ID:** `racefix_qa_dashboard_27754934_20260728T233102Z`
**Source commit:** `27754934832412879c5ef6d1551c70217c9bed6a`
**Captured (UTC):** 2026-07-28T23:31:16Z
**Verdict:** PASS
**Counts:** PASS=23 FAIL=0 SKIP=0

## Commit under test
```
27754934832412879c5ef6d1551c70217c9bed6a
Test User
2026-07-29T03:12:01+05:00
fix(terminal_ui,helixqa): snapshot QA sessions before rendering (HXC-174)
```

## How this evidence was produced

Generated by `scripts/qa/capture_racefix_wave.sh` — a real HTTP capture harness.
Every row below was evaluated against bytes observed on the wire during this run;
the full bidirectional trace for each case (request lines sent, response headers and
body received) is in `transcripts/<case-id>.http`. The harness FAILs any assertion
whose transcript is missing or contains no response — it cannot emit a PASS without
captured evidence (§11.4 / §11.4.1 / §11.4.5).

Exit codes: `0`=all PASS, `1`=a security assertion FAILED, `2`=INCOMPLETE (a case was
SKIPped with reason, §11.4.3 — never counted as a pass).

## Per-assertion verdicts

| Result | Case | Assertion | Expected | Observed |
|--------|------|-----------|----------|----------|
| PASS | `host_context` | host context captured as a recorded fact (not a pass/fail criterion) | `0` | `0` |
| PASS | `tree_clean` | no uncommitted local edit to the files under test (§11.4.108: tested source == shipped source) | `0` | `0` |
| PASS | `sig_snapshots` | runtime signature present in the shipped tracked source: the detached point-in-time accessor exists on the engine | `0` | `0` |
| PASS | `sig_snapshots` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `sig_render` | runtime signature present in the shipped tracked source: the dashboard renders from snapshots | `0` | `0` |
| PASS | `sig_render` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `absent_livepointers` | removed pattern is genuinely absent (git grep finds nothing): no dashboard render path still reads off the LIVE session pointers | `not 0` | `1` |
| PASS | `absent_livepointers` | no file:line hit line was printed for the removed pattern | `absent` | `absent` |
| PASS | `guard_qarace` | guard tests exit 0 under -race | `0` | `0` |
| PASS | `guard_qarace` | guard TestShowQA_SessionReadsAreRaceFree genuinely RAN and passed (not an empty -run filter exiting 0, and not a SKIP) | `--- PASS: TestShowQA_SessionReadsAreRaceFree` | `present` |
| PASS | `guard_qarace` | race detector reported no data race | `absent` | `absent` |
| PASS | `guard_qarace` | the -run filter matched at least one test | `absent` | `absent` |
| PASS | `red_qarace` | RED_MODE=1 FAILS on the fixed artifact — the guard is falsifiable, not blind (§11.4.115) | `not 0` | `1` |
| PASS | `red_qarace` | guard TestShowQA_SessionReadsAreRaceFree genuinely RAN and reported FAIL under RED_MODE=1 (not a compile error masquerading as a reproduction) | `--- FAIL: TestShowQA_SessionReadsAreRaceFree` | `present` |
| PASS | `red_qarace` | the -run filter matched at least one test | `absent` | `absent` |
| PASS | `red_qarace` | the non-zero exit came from the guard, not from a build failure | `absent` | `absent` |
| PASS | `red_qarace` | the RED_MODE=1 failure is a genuine detector report, not an assertion the test merely wrote itself (§11.4.107(10)) | `DATA RACE` | `present` |
| PASS | `suite_terminalui` | full package suite exits 0 under -race | `0` | `0` |
| PASS | `suite_terminalui` | race detector reported no data race across the package | `absent` | `absent` |
| PASS | `suite_terminalui` | no test in the package failed | `absent` | `absent` |
| PASS | `suite_helixqa` | full package suite exits 0 under -race | `0` | `0` |
| PASS | `suite_helixqa` | race detector reported no data race across the package | `absent` | `absent` |
| PASS | `suite_helixqa` | no test in the package failed | `absent` | `absent` |

## Grounding (§11.4.108)

```
# Grounding facts captured 2026-07-28T23:31:02Z
# §11.4.108: the running artifact must be the one built from the fixed source.

## repo HEAD
faadf59e docs(tracker): re-sync derived docs after HXC-201 was filed

## fix commit under test
27754934 fix(terminal_ui,helixqa): snapshot QA sessions before rendering (HXC-174)

## fix commit is an ancestor of HEAD?
YES — 27754934832412879c5ef6d1551c70217c9bed6a <= HEAD

## live server under test
base url: <unset>
(no listening helixcode process found via ss; grounding limited)
```

## Files

- `verdicts.tsv` — machine-readable per-assertion results
- `grounding.txt` — artifact/runtime identity facts
- `transcripts/*.http` — full bidirectional wire traces

## Correction to the preamble above (§11.4.6)

The "How this evidence was produced" section above is boilerplate emitted by the
shared harness `scripts/qa/lib/sec_capture_lib.sh`, which was written for
HTTP-shaped captures. It is inaccurate for THIS run in two specifics, corrected
here rather than left to mislead:

* This is **not** an HTTP capture. No HTTP request is made and nothing is
  observed "on the wire". Every case is a real COMMAND execution
  (`scripts/qa/lib/cmd_capture_lib.sh`), captured with its full combined
  stdout+stderr and its real exit code.
* Transcripts are `transcripts/<case-id>.**txt**`, not `.http`. Each one records
  the command sent (`### command:`), the working directory, and everything the
  command returned — the bidirectional record §11.4.83 asks for, in
  command/response rather than request/response form.

What is accurate as written: no assertion can emit a PASS without a captured,
non-empty transcript carrying an exit-code trailer, and a SKIP is never counted
as a pass. See `NOTES.md` in this directory for what this specific run does and
does not certify.
