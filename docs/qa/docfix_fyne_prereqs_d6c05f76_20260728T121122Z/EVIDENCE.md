# Desktop/GUI (Fyne) build-host prerequisites documentation accuracy — §11.4.77 — QA evidence (§11.4.83)

**Run ID:** `docfix_fyne_prereqs_d6c05f76_20260728T121122Z`
**Source commit:** `d6c05f76fcc0000dfa366cf566420d244c60a23f`
**Captured (UTC):** 2026-07-28T12:11:29Z
**Verdict:** PASS
**Counts:** PASS=16 FAIL=0 SKIP=0

## Commit under test
```
d6c05f76fcc0000dfa366cf566420d244c60a23f
Milos Vasic
2026-07-08T15:53:40+05:00
docs(build): desktop/GUI (Fyne) build-host prerequisites — X11+OpenGL dev headers (§11.4.77)
```

## How this evidence was produced

Generated by `scripts/qa/capture_docfix_fyne_prereqs_d6c05f76.sh` — a real HTTP capture harness.
Every row below was evaluated against bytes observed on the wire during this run;
the full bidirectional trace for each case (request lines sent, response headers and
body received) is in `transcripts/<case-id>.http`. The harness FAILs any assertion
whose transcript is missing or contains no response — it cannot emit a PASS without
captured evidence (§11.4 / §11.4.1 / §11.4.5).

Exit codes: `0`=all PASS, `1`=a security assertion FAILED, `2`=INCOMPLETE (a case was
SKIPped with reason, §11.4.3 — never counted as a pass).

## Per-assertion verdicts

| Result | Case | Assertion | Expected | Observed |
|--------|------|-----------|----------|----------|
| PASS | `host_missing_gl_pc` | gl.pc is genuinely absent from this host's pkg-config search path | `not 0` | `1` |
| PASS | `host_missing_xlib_header` | X11/Xlib.h is genuinely absent from this host | `not 0` | `1` |
| PASS | `gui_build_fails` | the default GUI build genuinely fails on this host — not a hypothetical | `not 0` | `1` |
| PASS | `gui_build_fails` | failure is in the exact go-gl/gl package the README names | `github.com/go-gl/gl/v2.1/gl` | `present` |
| PASS | `gui_build_fails` | pkg-config error text is byte-identical to what the README quotes | `Package gl was not found in the pkg-config search path` | `present` |
| PASS | `gui_build_fails` | failure also reaches the exact go-gl/glfw package the README names | `github.com/go-gl/glfw/v3.3/glfw` | `present` |
| PASS | `gui_build_fails` | CGO compile error text is byte-identical to what the README quotes | `fatal error: X11/Xlib.h: No such file or directory` | `present` |
| PASS | `nogui_build_succeeds` | the documented -tags nogui workaround genuinely compiles on this same host | `0` | `0` |
| PASS | `nogui_build_succeeds_artifact` | the -tags nogui build produced a real, non-empty binary — not just an rc==0 with no output | `non-empty file` | `113182904 bytes` |
| PASS | `host_os_release` | this host's OS release matches the commit's cited host exactly | `PRETTY_NAME="ALT Workstation 11.1 (Prometheus)"` | `present` |
| PASS | `host_kernel` | this host's kernel matches the commit's cited kernel exactly | `6.12.41-6.12-alt1` | `present` |
| PASS | `host_gcc_version` | this host's gcc matches the commit's cited compiler exactly | `x86_64-alt-linux-gcc (GCC) 15.2.1` | `present` |
| PASS | `host_pkgconfig_version` | this host's pkg-config matches the commit's cited version exactly | `0.29.2` | `present` |
| PASS | `committed_readme_unconfirmed_flag` | the README is readable at this commit | `0` | `0` |
| PASS | `committed_readme_unconfirmed_flag` | the commit's own honest-boundary flag is present in what was committed | `**Honest boundary (UNCONFIRMED, §11.4.6):**` | `present` |
| PASS | `committed_readme_unconfirmed_flag` | the commit is explicit that .pc-filename confirmation was NOT performed — and this QA run does not perform it either (no package installation was run in this capture; see EVIDENCE.md scope note) | `not** independently confirmed by actually installing the packages` | `present` |

## Grounding (§11.4.108)

```
# Grounding facts captured 2026-07-28T12:11:22Z
# §11.4.108: the running artifact must be the one built from the fixed source.

## repo HEAD
d8c44618 docs(qa): §11.4.83 evidence for e330149f model-identity fix

## fix commit under test
d6c05f76 docs(build): desktop/GUI (Fyne) build-host prerequisites — X11+OpenGL dev headers (§11.4.77)

## fix commit is an ancestor of HEAD?
YES — d6c05f76fcc0000dfa366cf566420d244c60a23f <= HEAD

## live server under test
base url: <unset>
(no listening helixcode process found via ss; grounding limited)
```

## Files

- `verdicts.tsv` — machine-readable per-assertion results
- `grounding.txt` — artifact/runtime identity facts
- `transcripts/*.http` — full bidirectional wire traces

## Honest scope statement (§11.4.6) — read this before the table above

**What this commit's user-visible surface actually is: none.** `d6c05f76`
touches exactly one file, `helix_code/applications/desktop/README.md` (74
lines added, 0 removed). No executable or library source changed. There is
no request/response behaviour, no CLI output, and no end-user journey for
this run to exercise, and none is claimed above.

**What this commit's actual deliverable is, and what this run proves**: the
README documents a build-host environment prerequisite (X11/OpenGL dev
headers) as a **fact about the host**, not a code defect — and this run
verifies that fact is genuinely true, live, on a real build host, with real
command execution captured in `transcripts/*.txt` (not `*.http` — there is
no wire trace for a documentation-only change):

1. This host genuinely lacks `gl.pc` (`pkg-config --exists gl` fails) and
   `X11/Xlib.h`, exactly as the README's prerequisite list implies.
2. The default (GUI) build, `go build -o bin/helix-desktop
   ./applications/desktop`, genuinely fails on this host, and the captured
   failure text is **byte-identical** to the error block the commit quotes
   verbatim in the README (same `github.com/go-gl/gl` / `go-gl/glfw` package
   paths, same `pkg-config`/CGO error lines).
3. The documented workaround, `go build -tags nogui -o bin/helix-desktop-cli
   ./applications/desktop`, genuinely succeeds on this same host and
   produces a real, non-empty, executable binary — not just an exit code.
4. The specific host facts the commit cites (`ALT Workstation 11.1
   "Prometheus"`, kernel `6.12.41-6.12-alt1`, `gcc 15.2.1`, `pkg-config
   0.29.2`) are checked live against this host's actual `/etc/os-release`,
   `uname -r`, `gcc --version`, and `pkg-config --version` output — reported
   honestly whichever way they came out (they matched exactly in this run;
   see the per-assertion table above for the captured values, not an
   assumption that they would match).

**What this run does NOT prove — the commit's own flagged gap, left
flagged, not silently resolved:** the commit itself states, as an
"UNCONFIRMED" boundary, that the exact `.pc` filename each ALT Linux
`-devel` package installs (e.g. whether `libGL-devel` ships a file literally
named `gl.pc`) was **not** confirmed by actually installing the packages and
rebuilding. This run does not install any package and does not close that
gap either — it only re-confirms the gap is still honestly stated in the
committed text (PHASE 5 above). Anyone closing that gap should install the
listed packages and re-run `go build -o bin/helix-desktop
./applications/desktop`, then confirm `pkg-config --exists gl` succeeds.
