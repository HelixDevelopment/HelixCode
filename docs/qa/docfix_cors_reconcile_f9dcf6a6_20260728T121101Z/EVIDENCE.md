# CORSMiddleware call-site reconciliation (doc.go example + test call sites) — §11.4.120 — QA evidence (§11.4.83)

**Run ID:** `docfix_cors_reconcile_f9dcf6a6_20260728T121101Z`
**Source commit:** `f9dcf6a6a688f158edfa1881641f649e84ac5671`
**Captured (UTC):** 2026-07-28T12:11:09Z
**Verdict:** PASS
**Counts:** PASS=15 FAIL=0 SKIP=0

## Commit under test
```
f9dcf6a6a688f158edfa1881641f649e84ac5671
Milos Vasic
2026-07-08T18:38:16+05:00
fix(server): update CORSMiddleware call sites for allowlist signature (§11.4.120 reconcile)
```

## How this evidence was produced

Generated by `scripts/qa/capture_docfix_cors_reconcile_f9dcf6a6.sh` — a real HTTP capture harness.
Every row below was evaluated against bytes observed on the wire during this run;
the full bidirectional trace for each case (request lines sent, response headers and
body received) is in `transcripts/<case-id>.http`. The harness FAILs any assertion
whose transcript is missing or contains no response — it cannot emit a PASS without
captured evidence (§11.4 / §11.4.1 / §11.4.5).

Exit codes: `0`=all PASS, `1`=a security assertion FAILED, `2`=INCOMPLETE (a case was
SKIPped with reason, §11.4.3 — never counted as a pass).

## Per-assertion verdicts

| Result | Case | Assertion | Expected | Observed |
|--------|------|-----------|----------|----------|
| PASS | `parent_vet_server` | go vet ./internal/server/... FAILS at the parent commit (arity mismatch, not yet reconciled) | `not 0` | `1` |
| PASS | `parent_vet_server` | vet error names the exact arity mismatch this commit fixes | `not enough arguments in call to CORSMiddleware` | `present` |
| PASS | `parent_vet_regression` | go vet ./tests/regression/... FAILS at the parent commit | `not 0` | `1` |
| PASS | `parent_vet_regression` | vet error names the exact call site this commit fixes | `not enough arguments in call to server.CORSMiddleware` | `present` |
| PASS | `parent_test_server` | go test FAILS to even build at the parent commit — the package is genuinely broken, not cosmetically different | `not 0` | `1` |
| PASS | `parent_test_server` | go test reports a build failure, confirming the test file itself does not compile pre-fix | `build failed` | `present` |
| PASS | `fixed_vet_all` | go vet is clean at this commit — the reconciliation compiles | `0` | `0` |
| PASS | `fixed_test_server` | go test PASSES at this commit — the reconciled call sites exercise the REAL CORSMiddleware(allowedOrigins) implementation | `0` | `0` |
| PASS | `fixed_test_server` | the reconciled test genuinely asserts allowlisted-origin echo + Vary:Origin behaviour, not a trivial no-op | `PASS: TestCORSMiddleware_AllowedOrigin_EchoedWithVary` | `present` |
| PASS | `fixed_test_server` | no test in the TestCORSMiddleware family regressed | `absent` | `absent` |
| PASS | `fixed_godoc_signature` | go doc resolves CORSMiddleware cleanly at this commit | `0` | `0` |
| PASS | `fixed_godoc_signature` | the REAL exported signature matches what doc.go's example claims it is | `func CORSMiddleware(allowedOrigins []string) gin.HandlerFunc` | `present` |
| PASS | `committed_docgo_example` | doc.go is readable at this commit | `0` | `0` |
| PASS | `committed_docgo_example` | doc.go's committed example calls CORSMiddleware with the allowedOrigins parameter — matching the real signature captured above, not the stale zero-arg form | `server.CORSMiddleware(allowedOrigins)` | `present` |
| PASS | `committed_docgo_example` | doc.go no longer documents the removed zero-argument call shape | `absent` | `absent` |

## Grounding (§11.4.108)

```
# Grounding facts captured 2026-07-28T12:11:01Z
# §11.4.108: the running artifact must be the one built from the fixed source.

## repo HEAD
d8c44618 docs(qa): §11.4.83 evidence for e330149f model-identity fix

## fix commit under test
f9dcf6a6 fix(server): update CORSMiddleware call sites for allowlist signature (§11.4.120 reconcile)

## fix commit is an ancestor of HEAD?
YES — f9dcf6a6a688f158edfa1881641f649e84ac5671 <= HEAD

## live server under test
base url: <unset>
(no listening helixcode process found via ss; grounding limited)
```

## Files

- `verdicts.tsv` — machine-readable per-assertion results
- `grounding.txt` — artifact/runtime identity facts
- `transcripts/*.http` — full bidirectional wire traces

## Honest scope statement (§11.4.6) — read this before the table above

**What this commit's user-visible surface actually is: none.** `f9dcf6a6`
touches `helix_code/internal/server/doc.go` (a package-comment example — not
compiled, not executable), plus `helix_code/internal/server/server_test.go`
and `helix_code/tests/regression/critical_paths_test.go` (Go test SOURCE,
compiled and run by `go vet`/`go test`, but never shipped in a production
binary). `helix_code/internal/server/server.go` — the actual production
`CORSMiddleware` implementation an end user's HTTP requests pass through — is
**not touched** by this commit. There is therefore no request/response
behaviour, no CLI output, and no user journey for this run to exercise, and
none is claimed above.

**What this run DOES prove**, with real, repeated command execution captured
in `transcripts/*.txt` (not `*.http` — there is no wire trace for a
doc/test-only change; every transcript here is a captured combined
stdout+stderr + real exit code from an actual `go vet` / `go test` / `go doc`
/ `git show` invocation):

1. The commit's own parent revision (`f9dcf6a6^`) genuinely fails `go
   vet`/`go test` — three call sites still invoked the OLD zero-argument
   `CORSMiddleware()` against the signature `4727a9d0` had already changed to
   `CORSMiddleware(allowedOrigins []string)`. This is captured live from an
   isolated, ephemeral `git worktree` checked out at that exact historical
   commit — not asserted from the diff alone.
2. This commit's own revision genuinely compiles clean and its reconciled
   `TestCORSMiddleware` family genuinely passes against the REAL
   `CORSMiddleware` implementation (also captured from an isolated worktree
   at this exact commit).
3. `doc.go`'s documented example — `server.CORSMiddleware(allowedOrigins)` —
   is **accurate**: `go doc` against the real, live exported signature at
   this commit is captured verbatim and cross-checked against the exact text
   `doc.go` committed. Accurate documentation is this commit's actual,
   narrow deliverable, and that is what is certified here — nothing more.

**What this run does NOT prove:** that any end user, request, or workflow is
affected — because none is. Characterising this as "zero executable change"
(as an earlier summary of this commit put it) is only partly right: the
`doc.go` half is non-executable, but the two test-file changes are real,
necessary Go test-source fixes (proven by PHASE 1 above: the package did not
compile without them) — a legitimate §11.4.120 gate-reconciliation, not a
no-op.
