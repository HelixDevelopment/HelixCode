# harmony_os: system monitor stops on a channel, not an unsynchronised bool — QA evidence (§11.4.83)

**Run ID:** `racefix_harmony_monitor_ccce0b77_20260728T210559Z`
**Source commit:** `ccce0b77a25e5cd3d84e8bc5b40b7c467d54320b`
**Captured (UTC):** 2026-07-28T21:06:14Z
**Verdict:** INCOMPLETE (PASS with SKIPs — certifies nothing about the skipped cases)
**Counts:** PASS=15 FAIL=0 SKIP=1

## Commit under test
```
ccce0b77a25e5cd3d84e8bc5b40b7c467d54320b
Test User
2026-07-29T00:25:25+05:00
fix(harmony_os): stop the system monitor on a channel, not an unsynchronised bool
```

## How this evidence was produced

Generated by `scripts/qa/capture_racefix_wave.sh` — a real HTTP capture harness.
Every row below was evaluated against bytes observed on the wire during this run;
the full bidirectional trace for each case (request lines sent, response headers and
body received) is in `transcripts/<case-id>.http`. The harness FAILs any assertion
whose transcript is missing or contains no response — it cannot emit a PASS without
captured evidence (§11.4 / §11.4.1 / §11.4.5).

Exit codes: `0`=all PASS, `1`=a security assertion FAILED, `2`=INCOMPLETE (a case was
SKIPped with reason, §11.4.3 — never counted as a pass).

## Per-assertion verdicts

| Result | Case | Assertion | Expected | Observed |
|--------|------|-----------|----------|----------|
| PASS | `host_context` | host context captured as a recorded fact (not a pass/fail criterion) | `0` | `0` |
| PASS | `tree_clean` | no uncommitted local edit to the files under test (§11.4.108: tested source == shipped source) | `0` | `0` |
| PASS | `sig_accessor` | runtime signature present in the shipped tracked source: the monitoring flag is reached only through mutex-guarded accessors | `0` | `0` |
| PASS | `sig_accessor` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `sig_monitordone` | runtime signature present in the shipped tracked source: the monitor publishes a done channel the teardown joins | `0` | `0` |
| PASS | `sig_monitordone` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `sig_tickorstop` | runtime signature present in the shipped tracked source: the loop stops on the app's stop channel via the shared primitive | `0` | `0` |
| PASS | `sig_tickorstop` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `guard_cleanup` | guard tests exit 0 under -race -count=2 | `0` | `0` |
| PASS | `guard_cleanup` | guard TestCleanup genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestCleanup` | `present` |
| PASS | `guard_cleanup` | race detector reported no data race | `absent` | `absent` |
| PASS | `guard_cleanup` | the -run filter matched at least one test | `absent` | `absent` |
| PASS | `suite_harmony` | full package suite exits 0 under -race | `0` | `0` |
| PASS | `suite_harmony` | race detector reported no data race across the package | `absent` | `absent` |
| PASS | `suite_harmony` | no test in the package failed | `absent` | `absent` |
| SKIP | `metric_race_runtime` | runtime exercise of the SECOND half of this commit — the five system-metric fields (cpu/mem/gpu/temperature/power) now guarded by a mutex between updateSystemMetrics and createHarmonySystemTab | `n/a` | `hardware_not_present-class gap in the test tree, not in the fix: NO test in applications/harmony_os constructs the system tab, so the reader side never runs and the race detector never observes that pair. The commit says so itself. Certified here by inspection + the accessor signatures only; a runtime proof needs a test that builds the tab, which does not exist yet.` |

## SKIPped cases (§11.4.3) — NOT certified by this run

- `metric_race_runtime` — runtime exercise of the SECOND half of this commit — the five system-metric fields (cpu/mem/gpu/temperature/power) now guarded by a mutex between updateSystemMetrics and createHarmonySystemTab — **reason:** hardware_not_present-class gap in the test tree, not in the fix: NO test in applications/harmony_os constructs the system tab, so the reader side never runs and the race detector never observes that pair. The commit says so itself. Certified here by inspection + the accessor signatures only; a runtime proof needs a test that builds the tab, which does not exist yet.

## Grounding (§11.4.108)

```
# Grounding facts captured 2026-07-28T21:05:59Z
# §11.4.108: the running artifact must be the one built from the fixed source.

## repo HEAD
03434f39 feat(gates): add CM-SUPERSEDED-GENERATOR-NAMING regression guard (HXC-160)

## fix commit under test
ccce0b77 fix(harmony_os): stop the system monitor on a channel, not an unsynchronised bool

## fix commit is an ancestor of HEAD?
YES — ccce0b77a25e5cd3d84e8bc5b40b7c467d54320b <= HEAD

## live server under test
base url: <unset>
(no listening helixcode process found via ss; grounding limited)
```

## Files

- `verdicts.tsv` — machine-readable per-assertion results
- `grounding.txt` — artifact/runtime identity facts
- `transcripts/*.http` — full bidirectional wire traces

## Correction to the preamble above (§11.4.6)

The "How this evidence was produced" section above is boilerplate emitted by the
shared harness `scripts/qa/lib/sec_capture_lib.sh`, which was written for
HTTP-shaped captures. It is inaccurate for THIS run in two specifics, corrected
here rather than left to mislead:

* This is **not** an HTTP capture. No HTTP request is made and nothing is
  observed "on the wire". Every case is a real COMMAND execution
  (`scripts/qa/lib/cmd_capture_lib.sh`), captured with its full combined
  stdout+stderr and its real exit code.
* Transcripts are `transcripts/<case-id>.**txt**`, not `.http`. Each one records
  the command sent (`### command:`), the working directory, and everything the
  command returned — the bidirectional record §11.4.83 asks for, in
  command/response rather than request/response form.

What is accurate as written: no assertion can emit a PASS without a captured,
non-empty transcript carrying an exit-code trailer, and a SKIP is never counted
as a pass. See `NOTES.md` in this directory for what this specific run does and
does not certify.
