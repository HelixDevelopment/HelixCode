# 3 defects from the independent Fable review: autosave residual window, served-model reporting — QA evidence (§11.4.83)

**Run ID:** `racefix_fable3_ed0cdcb8_20260728T210426Z`
**Source commit:** `ed0cdcb861631d84b22b258b6c873507f729b933`
**Captured (UTC):** 2026-07-28T21:04:38Z
**Verdict:** PASS
**Counts:** PASS=18 FAIL=0 SKIP=0

## Commit under test
```
ed0cdcb861631d84b22b258b6c873507f729b933
Test User
2026-07-28T21:03:35+05:00
fix(server,llm,persistence): 3 real defects from independent Fable review
```

## How this evidence was produced

Generated by `scripts/qa/capture_racefix_wave.sh` — a real HTTP capture harness.
Every row below was evaluated against bytes observed on the wire during this run;
the full bidirectional trace for each case (request lines sent, response headers and
body received) is in `transcripts/<case-id>.http`. The harness FAILs any assertion
whose transcript is missing or contains no response — it cannot emit a PASS without
captured evidence (§11.4 / §11.4.1 / §11.4.5).

Exit codes: `0`=all PASS, `1`=a security assertion FAILED, `2`=INCOMPLETE (a case was
SKIPped with reason, §11.4.3 — never counted as a pass).

## Per-assertion verdicts

| Result | Case | Assertion | Expected | Observed |
|--------|------|-----------|----------|----------|
| PASS | `host_context` | host context captured as a recorded fact (not a pass/fail criterion) | `0` | `0` |
| PASS | `tree_clean` | no uncommitted local edit to the files under test (§11.4.108: tested source == shipped source) | `0` | `0` |
| PASS | `sig_racehook` | runtime signature present in the shipped tracked source: the residual-window race hook seam exists in the shipped store | `0` | `0` |
| PASS | `sig_racehook` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `sig_servedmodel` | runtime signature present in the shipped tracked source: the server reports the model that ACTUALLY served the request | `0` | `0` |
| PASS | `sig_servedmodel` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `guard_residual` | guard tests exit 0 under -race -count=2 | `0` | `0` |
| PASS | `guard_residual` | guard TestAutoSaveTick_ResidualWindowStopRaceDuringBlockingSelect genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestAutoSaveTick_ResidualWindowStopRaceDuringBlockingSelect` | `present` |
| PASS | `guard_residual` | race detector reported no data race | `absent` | `absent` |
| PASS | `guard_residual` | the -run filter matched at least one test | `absent` | `absent` |
| PASS | `guard_servedmodel` | guard tests exit 0 under -race -count=2 | `0` | `0` |
| PASS | `guard_servedmodel` | guard TestGenerateLLM_ResponseModel_ServedNotRequested genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestGenerateLLM_ResponseModel_ServedNotRequested` | `present` |
| PASS | `guard_servedmodel` | guard TestGenerateLLM_ResponseModel_FallsBackWhenProviderOmitsIt genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestGenerateLLM_ResponseModel_FallsBackWhenProviderOmitsIt` | `present` |
| PASS | `guard_servedmodel` | race detector reported no data race | `absent` | `absent` |
| PASS | `guard_servedmodel` | the -run filter matched at least one test | `absent` | `absent` |
| PASS | `suite_persistence` | full package suite exits 0 under -race | `0` | `0` |
| PASS | `suite_persistence` | race detector reported no data race across the package | `absent` | `absent` |
| PASS | `suite_persistence` | no test in the package failed | `absent` | `absent` |

## Grounding (§11.4.108)

```
# Grounding facts captured 2026-07-28T21:04:26Z
# §11.4.108: the running artifact must be the one built from the fixed source.

## repo HEAD
53ecefec docs(governance): name the DB exporter, not the superseded shell generators (HXC-160)

## fix commit under test
ed0cdcb8 fix(server,llm,persistence): 3 real defects from independent Fable review

## fix commit is an ancestor of HEAD?
YES — ed0cdcb861631d84b22b258b6c873507f729b933 <= HEAD

## live server under test
base url: <unset>
(no listening helixcode process found via ss; grounding limited)
```

## Files

- `verdicts.tsv` — machine-readable per-assertion results
- `grounding.txt` — artifact/runtime identity facts
- `transcripts/*.http` — full bidirectional wire traces

## Correction to the preamble above (§11.4.6)

The "How this evidence was produced" section above is boilerplate emitted by the
shared harness `scripts/qa/lib/sec_capture_lib.sh`, which was written for
HTTP-shaped captures. It is inaccurate for THIS run in two specifics, corrected
here rather than left to mislead:

* This is **not** an HTTP capture. No HTTP request is made and nothing is
  observed "on the wire". Every case is a real COMMAND execution
  (`scripts/qa/lib/cmd_capture_lib.sh`), captured with its full combined
  stdout+stderr and its real exit code.
* Transcripts are `transcripts/<case-id>.**txt**`, not `.http`. Each one records
  the command sent (`### command:`), the working directory, and everything the
  command returned — the bidirectional record §11.4.83 asks for, in
  command/response rather than request/response form.

What is accurate as written: no assertion can emit a PASS without a captured,
non-empty transcript carrying an exit-code trailer, and a SKIP is never counted
as a pass. See `NOTES.md` in this directory for what this specific run does and
does not certify.
