# harmony_os/aurora_os: background widget updates routed through internal/fyneui — QA evidence (§11.4.83)

**Run ID:** `racefix_fyneui_route_0a4df699_20260728T210614Z`
**Source commit:** `0a4df699b32df9e0eb74c54aee08c52dd90c5765`
**Captured (UTC):** 2026-07-28T21:07:14Z
**Verdict:** PASS
**Counts:** PASS=21 FAIL=0 SKIP=0

## Commit under test
```
0a4df699b32df9e0eb74c54aee08c52dd90c5765
Test User
2026-07-29T00:26:06+05:00
fix(harmony_os,aurora_os): route background widget updates through internal/fyneui
```

## How this evidence was produced

Generated by `scripts/qa/capture_racefix_wave.sh` — a real HTTP capture harness.
Every row below was evaluated against bytes observed on the wire during this run;
the full bidirectional trace for each case (request lines sent, response headers and
body received) is in `transcripts/<case-id>.http`. The harness FAILs any assertion
whose transcript is missing or contains no response — it cannot emit a PASS without
captured evidence (§11.4 / §11.4.1 / §11.4.5).

Exit codes: `0`=all PASS, `1`=a security assertion FAILED, `2`=INCOMPLETE (a case was
SKIPped with reason, §11.4.3 — never counted as a pass).

## Per-assertion verdicts

| Result | Case | Assertion | Expected | Observed |
|--------|------|-----------|----------|----------|
| PASS | `host_context` | host context captured as a recorded fact (not a pass/fail criterion) | `0` | `0` |
| PASS | `tree_clean` | no uncommitted local edit to the files under test (§11.4.108: tested source == shipped source) | `0` | `0` |
| PASS | `sig_aurora_dispatch` | runtime signature present in the shipped tracked source: aurora dispatches label updates onto the UI thread | `0` | `0` |
| PASS | `sig_aurora_dispatch` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `sig_aurora_tickorstop` | runtime signature present in the shipped tracked source: aurora's ticker loops honour the app's stop channel | `0` | `0` |
| PASS | `sig_aurora_tickorstop` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `sig_harmony_import` | runtime signature present in the shipped tracked source: harmony imports the shared UI-thread package | `0` | `0` |
| PASS | `sig_harmony_import` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `suite_harmony` | full package suite exits 0 under -race | `0` | `0` |
| PASS | `suite_harmony` | race detector reported no data race across the package | `absent` | `absent` |
| PASS | `suite_harmony` | no test in the package failed | `absent` | `absent` |
| PASS | `suite_aurora` | full package suite exits 0 under -race | `0` | `0` |
| PASS | `suite_aurora` | race detector reported no data race across the package | `absent` | `absent` |
| PASS | `suite_aurora` | no test in the package failed | `absent` | `absent` |
| PASS | `dispatch_runtime_aurora` | guard tests exit 0 under -race | `0` | `0` |
| PASS | `dispatch_runtime_aurora` | guard TestAuroraLLMTab_BackgroundWidgetMutationsAreUIThreadSafe genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestAuroraLLMTab_BackgroundWidgetMutationsAreUIThreadSafe` | `present` |
| PASS | `dispatch_runtime_aurora` | guard TestAuroraDashboardTab_TickerWidgetMutationsAreUIThreadSafe genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestAuroraDashboardTab_TickerWidgetMutationsAreUIThreadSafe` | `present` |
| PASS | `dispatch_runtime_aurora` | race detector reported no data race | `absent` | `absent` |
| PASS | `dispatch_runtime_harmony` | guard tests exit 0 under -race | `0` | `0` |
| PASS | `dispatch_runtime_harmony` | guard TestHarmonyLLMTab_BackgroundWidgetMutationsAreUIThreadSafe genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestHarmonyLLMTab_BackgroundWidgetMutationsAreUIThreadSafe` | `present` |
| PASS | `dispatch_runtime_harmony` | race detector reported no data race | `absent` | `absent` |

## Grounding (§11.4.108)

```
# Grounding facts captured 2026-07-28T21:06:14Z
# §11.4.108: the running artifact must be the one built from the fixed source.

## repo HEAD
983f8417 fix(speckit): replace stock constitution placeholder with a CONST-059 pointer

## fix commit under test
0a4df699 fix(harmony_os,aurora_os): route background widget updates through internal/fyneui

## fix commit is an ancestor of HEAD?
YES — 0a4df699b32df9e0eb74c54aee08c52dd90c5765 <= HEAD

## live server under test
base url: <unset>
(no listening helixcode process found via ss; grounding limited)
```

## Files

- `verdicts.tsv` — machine-readable per-assertion results
- `grounding.txt` — artifact/runtime identity facts
- `transcripts/*.http` — full bidirectional wire traces

## Correction to the preamble above (§11.4.6)

The "How this evidence was produced" section above is boilerplate emitted by the
shared harness `scripts/qa/lib/sec_capture_lib.sh`, which was written for
HTTP-shaped captures. It is inaccurate for THIS run in two specifics, corrected
here rather than left to mislead:

* This is **not** an HTTP capture. No HTTP request is made and nothing is
  observed "on the wire". Every case is a real COMMAND execution
  (`scripts/qa/lib/cmd_capture_lib.sh`), captured with its full combined
  stdout+stderr and its real exit code.
* Transcripts are `transcripts/<case-id>.**txt**`, not `.http`. Each one records
  the command sent (`### command:`), the working directory, and everything the
  command returned — the bidirectional record §11.4.83 asks for, in
  command/response rather than request/response form.

What is accurate as written: no assertion can emit a PASS without a captured,
non-empty transcript carrying an exit-code trailer, and a SKIP is never counted
as a pass. See `NOTES.md` in this directory for what this specific run does and
does not certify.
