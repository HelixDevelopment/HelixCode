# Azure provider: a whitespace-only endpoint is refused cleanly at construction (no garbage endpoint, no crash) — QA evidence (§11.4.83)

**Run ID:** `feat_azureguard_eb233785_20260728T103204Z`
**Source commit:** `eb23378513d94409447e324b1ad989ece20a1f08`
**Captured (UTC):** 2026-07-28T10:32:06Z
**Verdict:** PASS
**Counts:** PASS=6 FAIL=0 SKIP=0

## Commit under test
```
eb23378513d94409447e324b1ad989ece20a1f08
Test User
2026-07-12T12:58:56+05:00
fix(infra-defects): Azure nil-ptr panic + stale integration test + e2e -all flag
```

## How this evidence was produced

Generated by `scripts/qa/capture_feat_azureguard_eb233785.sh` — a real HTTP capture harness.
Every row below was evaluated against bytes observed on the wire during this run;
the full bidirectional trace for each case (request lines sent, response headers and
body received) is in `transcripts/<case-id>.http`. The harness FAILs any assertion
whose transcript is missing or contains no response — it cannot emit a PASS without
captured evidence (§11.4 / §11.4.1 / §11.4.5).

Exit codes: `0`=all PASS, `1`=a security assertion FAILED, `2`=INCOMPLETE (a case was
SKIPped with reason, §11.4.3 — never counted as a pass).

## Per-assertion verdicts

| Result | Case | Assertion | Expected | Observed |
|--------|------|-----------|----------|----------|
| PASS | `eph_azure_whitespace_endpoint` | status != 401 | `not 401` | `503` |
| PASS | `eph_azure_whitespace_endpoint` | status == 503 | `503` | `503` |
| PASS | `eph_azure_whitespace_endpoint` | body contains 'azure endpoint is required' | `azure endpoint is required` | `present` |
| PASS | `eph_azure_whitespace_endpoint` | body excludes '"choices"' | `absent` | `absent` |
| PASS | `eph_azure_whitespace_endpoint` | body excludes '"completion_tokens"' | `absent` | `absent` |
| PASS | `eph_alive_after_bad_endpoint` | status == 200 | `200` | `200` |

## Grounding (§11.4.108)

```
# Grounding facts captured 2026-07-28T10:32:04Z
# §11.4.108: the running artifact must be the one built from the fixed source.

## repo HEAD
0a4eb8d0 docs(resume): correct live-state anchors after the end-of-session push

## fix commit under test
eb233785 fix(infra-defects): Azure nil-ptr panic + stale integration test + e2e -all flag

## fix commit is an ancestor of HEAD?
YES — eb23378513d94409447e324b1ad989ece20a1f08 <= HEAD

## live server under test
base url: <unset>
(no listening helixcode process found via ss; grounding limited)
```

## Files

- `verdicts.tsv` — machine-readable per-assertion results
- `grounding.txt` — artifact/runtime identity facts
- `transcripts/*.http` — full bidirectional wire traces
- `ephemeral-server.log` — boot log of the purpose-configured instance
- `ephemeral-config.yaml` — generated config for that instance
