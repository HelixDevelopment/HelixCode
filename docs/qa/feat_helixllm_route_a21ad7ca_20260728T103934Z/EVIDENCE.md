# HelixCode LLM routes to the local HelixLLM coder (:18434): real completion, coder-advertised model, no 'unknown provider' — QA evidence (§11.4.83)

**Run ID:** `feat_helixllm_route_a21ad7ca_20260728T103934Z`
**Source commit:** `a21ad7ca4bdb8c17dd68d6065bc4ff8174a0b15d`
**Captured (UTC):** 2026-07-28T10:39:37Z
**Verdict:** FAIL
**Counts:** PASS=7 FAIL=1 SKIP=0

## Commit under test
```
a21ad7ca4bdb8c17dd68d6065bc4ff8174a0b15d
Milos Vasic
2026-07-08T12:44:46+05:00
feat(server): route HelixCode LLM to local HelixLLM coder (:18434) — §11.4.115
```

## How this evidence was produced

Generated by `scripts/qa/capture_feat_helixllm_a21ad7ca.sh` — a real HTTP capture harness.
Every row below was evaluated against bytes observed on the wire during this run;
the full bidirectional trace for each case (request lines sent, response headers and
body received) is in `transcripts/<case-id>.http`. The harness FAILs any assertion
whose transcript is missing or contains no response — it cannot emit a PASS without
captured evidence (§11.4 / §11.4.1 / §11.4.5).

Exit codes: `0`=all PASS, `1`=a security assertion FAILED, `2`=INCOMPLETE (a case was
SKIPped with reason, §11.4.3 — never counted as a pass).

## Per-assertion verdicts

| Result | Case | Assertion | Expected | Observed |
|--------|------|-----------|----------|----------|
| PASS | `coder_models` | status == 200 | `200` | `200` |
| PASS | `coder_models` | body contains '"object":"list"' | `"object":"list"` | `present` |
| PASS | `eph_helixllm_generate` | status == 200 | `200` | `200` |
| PASS | `eph_helixllm_generate` | body excludes 'unknown provider' | `absent` | `absent` |
| PASS | `eph_helixllm_generate` | body contains 'qanonce59796599ab6f' | `qanonce59796599ab6f` | `present` |
| FAIL | `eph_helixllm_generate` | body contains '/models/Qwen3-Coder-30B-A3B-Instruct-Q4_K_M.gguf' | `/models/Qwen3-Coder-30B-A3B-Instruct-Q4_K_M.gguf` | `<absent>` |
| PASS | `eph_helixllm_generate` | body contains '"completion_tokens"' | `"completion_tokens"` | `present` |
| PASS | `eph_helixllm_generate` | body contains '"finish_reason"' | `"finish_reason"` | `present` |

## Grounding (§11.4.108)

```
# Grounding facts captured 2026-07-28T10:39:34Z
# §11.4.108: the running artifact must be the one built from the fixed source.

## repo HEAD
0a4eb8d0 docs(resume): correct live-state anchors after the end-of-session push

## fix commit under test
a21ad7ca feat(server): route HelixCode LLM to local HelixLLM coder (:18434) — §11.4.115

## fix commit is an ancestor of HEAD?
YES — a21ad7ca4bdb8c17dd68d6065bc4ff8174a0b15d <= HEAD

## live server under test
base url: <unset>
(no listening helixcode process found via ss; grounding limited)
```

## Files

- `verdicts.tsv` — machine-readable per-assertion results
- `grounding.txt` — artifact/runtime identity facts
- `transcripts/*.http` — full bidirectional wire traces
- `ephemeral-server.log` — boot log of the purpose-configured instance
- `ephemeral-config.yaml` — generated config for that instance
