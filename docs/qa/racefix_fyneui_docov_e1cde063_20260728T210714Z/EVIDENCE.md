# fyneui: false test comment corrected, Do lock path covered, stopOnce bypass closed — QA evidence (§11.4.83)

**Run ID:** `racefix_fyneui_docov_e1cde063_20260728T210714Z`
**Source commit:** `e1cde063586961880c899bea49faaee5bfaa0dfd`
**Captured (UTC):** 2026-07-28T21:08:18Z
**Verdict:** PASS
**Counts:** PASS=21 FAIL=0 SKIP=0

## Commit under test
```
e1cde063586961880c899bea49faaee5bfaa0dfd
Test User
2026-07-29T00:32:28+05:00
fix(fyneui): correct a false test comment, add Do coverage, close a stopOnce bypass
```

## How this evidence was produced

Generated by `scripts/qa/capture_racefix_wave.sh` — a real HTTP capture harness.
Every row below was evaluated against bytes observed on the wire during this run;
the full bidirectional trace for each case (request lines sent, response headers and
body received) is in `transcripts/<case-id>.http`. The harness FAILs any assertion
whose transcript is missing or contains no response — it cannot emit a PASS without
captured evidence (§11.4 / §11.4.1 / §11.4.5).

Exit codes: `0`=all PASS, `1`=a security assertion FAILED, `2`=INCOMPLETE (a case was
SKIPped with reason, §11.4.3 — never counted as a pass).

## Per-assertion verdicts

| Result | Case | Assertion | Expected | Observed |
|--------|------|-----------|----------|----------|
| PASS | `host_context` | host context captured as a recorded fact (not a pass/fail criterion) | `0` | `0` |
| PASS | `tree_clean` | no uncommitted local edit to the files under test (§11.4.108: tested source == shipped source) | `0` | `0` |
| PASS | `sig_limitation` | runtime signature present in the shipped tracked source: the package doc records the DoAndWait/shutdown limitation instead of implying none | `0` | `0` |
| PASS | `sig_limitation` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `sig_docoverage` | runtime signature present in the shipped tracked source: the Do lock path now has its own direct guard | `0` | `0` |
| PASS | `sig_docoverage` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `sig_stoponce` | runtime signature present in the shipped tracked source: the desktop test teardown now routes through the same sync.Once Close() uses | `0` | `0` |
| PASS | `sig_stoponce` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `absent_bypass` | removed pattern is genuinely absent (git grep finds nothing): no test closes stopUpdate directly, bypassing the sync.Once in Close() | `not 0` | `1` |
| PASS | `absent_bypass` | no file:line hit line was printed for the removed pattern | `absent` | `absent` |
| PASS | `guard_uithread` | guard tests exit 0 under -race -count=2 | `0` | `0` |
| PASS | `guard_uithread` | guard TestDoSerializesAgainstSync genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestDoSerializesAgainstSync` | `present` |
| PASS | `guard_uithread` | guard TestSyncSerializesAgainstDo genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestSyncSerializesAgainstDo` | `present` |
| PASS | `guard_uithread` | race detector reported no data race | `absent` | `absent` |
| PASS | `guard_uithread` | the -run filter matched at least one test | `absent` | `absent` |
| PASS | `suite_fyneui` | full package suite exits 0 under -race | `0` | `0` |
| PASS | `suite_fyneui` | race detector reported no data race across the package | `absent` | `absent` |
| PASS | `suite_fyneui` | no test in the package failed | `absent` | `absent` |
| PASS | `suite_desktop` | full package suite exits 0 under -race | `0` | `0` |
| PASS | `suite_desktop` | race detector reported no data race across the package | `absent` | `absent` |
| PASS | `suite_desktop` | no test in the package failed | `absent` | `absent` |

## Grounding (§11.4.108)

```
# Grounding facts captured 2026-07-28T21:07:14Z
# §11.4.108: the running artifact must be the one built from the fixed source.

## repo HEAD
983f8417 fix(speckit): replace stock constitution placeholder with a CONST-059 pointer

## fix commit under test
e1cde063 fix(fyneui): correct a false test comment, add Do coverage, close a stopOnce bypass

## fix commit is an ancestor of HEAD?
YES — e1cde063586961880c899bea49faaee5bfaa0dfd <= HEAD

## live server under test
base url: <unset>
(no listening helixcode process found via ss; grounding limited)
```

## Files

- `verdicts.tsv` — machine-readable per-assertion results
- `grounding.txt` — artifact/runtime identity facts
- `transcripts/*.http` — full bidirectional wire traces

## Correction to the preamble above (§11.4.6)

The "How this evidence was produced" section above is boilerplate emitted by the
shared harness `scripts/qa/lib/sec_capture_lib.sh`, which was written for
HTTP-shaped captures. It is inaccurate for THIS run in two specifics, corrected
here rather than left to mislead:

* This is **not** an HTTP capture. No HTTP request is made and nothing is
  observed "on the wire". Every case is a real COMMAND execution
  (`scripts/qa/lib/cmd_capture_lib.sh`), captured with its full combined
  stdout+stderr and its real exit code.
* Transcripts are `transcripts/<case-id>.**txt**`, not `.http`. Each one records
  the command sent (`### command:`), the working directory, and everything the
  command returned — the bidirectional record §11.4.83 asks for, in
  command/response rather than request/response form.

What is accurate as written: no assertion can emit a PASS without a captured,
non-empty transcript carrying an exit-code trailer, and a SKIP is never counted
as a pass. See `NOTES.md` in this directory for what this specific run does and
does not certify.
