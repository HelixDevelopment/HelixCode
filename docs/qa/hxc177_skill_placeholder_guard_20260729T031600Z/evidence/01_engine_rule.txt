### engine resolution rule (verbatim source)
115:var substRegex = regexp.MustCompile(`\{\{([A-Z_][A-Z0-9_]*(?:\.[A-Za-z0-9_]+)?(?::[^}]+)?)\}\}`)

### the ONLY named-variable resolution case
146:		case strings.HasPrefix(token, "ARG."):
147-			varName := strings.TrimPrefix(token, "ARG.")
148-			return c.variables[varName]
149-
