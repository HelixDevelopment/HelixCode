# tools/shell: the abandoned-consumer bound corrected, and M2/M3 pinned by standing guards — QA evidence (§11.4.83)

**Run ID:** `racefix_shell_bounds_0c2f4940_20260728T232950Z`
**Source commit:** `0c2f4940e4e40234ab4e0c1cd3331cf363480bb2`
**Captured (UTC):** 2026-07-28T23:30:36Z
**Verdict:** PASS
**Counts:** PASS=25 FAIL=0 SKIP=0

## Commit under test
```
0c2f4940e4e40234ab4e0c1cd3331cf363480bb2
Test User
2026-07-29T03:52:58+05:00
docs+test(tools/shell): correct the abandoned-consumer bound, guard M2/M3
```

## How this evidence was produced

Generated by `scripts/qa/capture_racefix_wave.sh` — a real HTTP capture harness.
Every row below was evaluated against bytes observed on the wire during this run;
the full bidirectional trace for each case (request lines sent, response headers and
body received) is in `transcripts/<case-id>.http`. The harness FAILs any assertion
whose transcript is missing or contains no response — it cannot emit a PASS without
captured evidence (§11.4 / §11.4.1 / §11.4.5).

Exit codes: `0`=all PASS, `1`=a security assertion FAILED, `2`=INCOMPLETE (a case was
SKIPped with reason, §11.4.3 — never counted as a pass).

## Per-assertion verdicts

| Result | Case | Assertion | Expected | Observed |
|--------|------|-----------|----------|----------|
| PASS | `host_context` | host context captured as a recorded fact (not a pass/fail criterion) | `0` | `0` |
| PASS | `tree_clean` | no uncommitted local edit to the files under test (§11.4.108: tested source == shipped source) | `0` | `0` |
| PASS | `sig_bound` | runtime signature present in the shipped tracked source: the drain loop states the REAL abandoned-consumer bound (~(headroom+1) x grace), not the two-window claim it replaced | `0` | `0` |
| PASS | `sig_bound` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `sig_contract` | runtime signature present in the shipped tracked source: StreamingExecution names abandoning-without-Cancel as the pattern to avoid | `0` | `0` |
| PASS | `sig_contract` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `sig_progressdoc` | runtime signature present in the shipped tracked source: Progress() no longer claims to prove a consumer exists | `0` | `0` |
| PASS | `sig_progressdoc` | a real file:line hit line was printed — the match is in a tracked file, not just in the command echo | `.go:` | `present` |
| PASS | `guard_bounded` | guard tests exit 0 under -race -count=2 | `0` | `0` |
| PASS | `guard_bounded` | guard TestExecuteStreamAbandonedConsumerStaysBoundedWhileProducerTrickles genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestExecuteStreamAbandonedConsumerStaysBoundedWhileProducerTrickles` | `present` |
| PASS | `guard_bounded` | race detector reported no data race | `absent` | `absent` |
| PASS | `guard_bounded` | the -run filter matched at least one test | `absent` | `absent` |
| PASS | `guard_m2` | guard tests exit 0 under -race -count=2 | `0` | `0` |
| PASS | `guard_m2` | guard TestOutputStreamerErrPrefersGenuineFailureOverTeardown genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestOutputStreamerErrPrefersGenuineFailureOverTeardown` | `present` |
| PASS | `guard_m2` | guard TestExecuteStreamSurfacesStderrFailureDespiteStdoutTeardown genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestExecuteStreamSurfacesStderrFailureDespiteStdoutTeardown` | `present` |
| PASS | `guard_m2` | race detector reported no data race | `absent` | `absent` |
| PASS | `guard_m2` | the -run filter matched at least one test | `absent` | `absent` |
| PASS | `guard_m3` | guard tests exit 0 under -race -count=2 | `0` | `0` |
| PASS | `guard_m3` | guard TestExecuteStreamCancelMidStreamMarksOutputIncomplete genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestExecuteStreamCancelMidStreamMarksOutputIncomplete` | `present` |
| PASS | `guard_m3` | guard TestExecuteStreamCancelAfterCleanEOFKeepsOutputComplete genuinely RAN and passed (not an empty -run filter exiting 0) | `--- PASS: TestExecuteStreamCancelAfterCleanEOFKeepsOutputComplete` | `present` |
| PASS | `guard_m3` | race detector reported no data race | `absent` | `absent` |
| PASS | `guard_m3` | the -run filter matched at least one test | `absent` | `absent` |
| PASS | `suite_shell` | full package suite exits 0 under -race | `0` | `0` |
| PASS | `suite_shell` | race detector reported no data race across the package | `absent` | `absent` |
| PASS | `suite_shell` | no test in the package failed | `absent` | `absent` |

## Grounding (§11.4.108)

```
# Grounding facts captured 2026-07-28T23:29:50Z
# §11.4.108: the running artifact must be the one built from the fixed source.

## repo HEAD
faadf59e docs(tracker): re-sync derived docs after HXC-201 was filed

## fix commit under test
0c2f4940 docs+test(tools/shell): correct the abandoned-consumer bound, guard M2/M3

## fix commit is an ancestor of HEAD?
YES — 0c2f4940e4e40234ab4e0c1cd3331cf363480bb2 <= HEAD

## live server under test
base url: <unset>
(no listening helixcode process found via ss; grounding limited)
```

## Files

- `verdicts.tsv` — machine-readable per-assertion results
- `grounding.txt` — artifact/runtime identity facts
- `transcripts/*.http` — full bidirectional wire traces

## Correction to the preamble above (§11.4.6)

The "How this evidence was produced" section above is boilerplate emitted by the
shared harness `scripts/qa/lib/sec_capture_lib.sh`, which was written for
HTTP-shaped captures. It is inaccurate for THIS run in two specifics, corrected
here rather than left to mislead:

* This is **not** an HTTP capture. No HTTP request is made and nothing is
  observed "on the wire". Every case is a real COMMAND execution
  (`scripts/qa/lib/cmd_capture_lib.sh`), captured with its full combined
  stdout+stderr and its real exit code.
* Transcripts are `transcripts/<case-id>.**txt**`, not `.http`. Each one records
  the command sent (`### command:`), the working directory, and everything the
  command returned — the bidirectional record §11.4.83 asks for, in
  command/response rather than request/response form.

What is accurate as written: no assertion can emit a PASS without a captured,
non-empty transcript carrying an exit-code trailer, and a SKIP is never counted
as a pass. See `NOTES.md` in this directory for what this specific run does and
does not certify.
