# HXC-292 — landed-fix closure evidence

- **Date:** 2026-08-13 · **Triaged by:** AI-agent (T1/main - claude1) · **Verdict:** CLOSE (Bug → fixed)

## Fix commit
- Repo `submodules/llms_verifier`, commit `9b9c4da9`, branch tip `9bf5457d`, **reachable**.

## Defect gone (read at the committed tip, NOT the working tree)
Verified against a clean `git archive 9bf5457d` extract (sibling's untracked `clientip/` absent — instrument valid).
Both halves the item names are addressed:

- **Claimed-header trust:** `llm-verifier/api/middleware.go:437` — `if isTrustedProxyPeer(r.RemoteAddr) {`.
  The forwarded header is no longer read unconditionally; the allowlist is `LLM_VERIFIER_TRUSTED_PROXIES` and is
  **default-deny** (empty ⇒ no peer trusted).
- **Identity split:** `middleware.go:463` returns `normalizeRemoteAddr(r.RemoteAddr)`, which uses
  `net.SplitHostPort` (`security/client_ip_trust.go:235`) instead of a last-colon split — so identity is
  path-independent for bracketed IPv6.
- Chain walk is right-to-left skipping trusted hops (`resolveForwardedFor:105`), closing the leftmost-selection bypass.

## Covering guard (falsifiable, `RED_MODE`)
- `llm-verifier/api/hxc292_client_ip_trust_red_test.go` — `TestHXC292ClientIPTrustRED`, `…Census`, `…Concurrent`
- `go test -race -count=1 -run HXC292 ./api/` → **exit 0**
- `RED_MODE=1 …` → **exit 1**, all three sub-defects reported as correctly non-reproducing (bracket fork,
  header forgery, appending-proxy leftmost bypass).

## Notes
The sibling's untracked `clientip/` is a dedup **refactor on top of** this landed fix (filed separately), not
evidence the fix is unlanded — the fix is fully present at the tip.
