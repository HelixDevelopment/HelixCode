# HXC-299 — landed-fix closure evidence

- **Date:** 2026-08-13 · **Triaged by:** AI-agent (T1/main - claude1) · **Verdict:** CLOSE (Bug → fixed)

## Fix commit
- Repo `submodules/llms_verifier`, commit `9bf5457d` (is the tip), **reachable**.

## Defect gone (read at the committed tip, NOT the working tree)
Verified against a clean `git archive 9bf5457d` extract (sibling's untracked `clientip/` absent — instrument valid).

- `llm-verifier/security/security.go:540` — `extractIPAddress` now simply `return resolveClientIP(r)`.
- `llm-verifier/security/client_ip_trust.go:235` —
  `if host, _, err := net.SplitHostPort(remoteAddr); err == nil { … return host }`.
  The first-colon truncation is gone: `[2001:db8::1]:11111` → `2001:db8::1`, no longer the `[2001` fragment that
  collapsed unrelated callers onto one identity.
- Header trust gated on `isTrustedProxyPeer` (`client_ip_trust.go:61`), default-deny.

## Covering guard (falsifiable, `RED_MODE`)
- `llm-verifier/security/hxc299_extract_ip_trust_red_test.go` — `TestHXC299ExtractIPAddressTrustRED`, `…Census`, `…Concurrent`
- `go test -race -count=1 -run HXC299 ./security/` → **exit 0**
- `RED_MODE=1 …` → **exit 1**; the two distinct callers `[2001:db8::1]:11111` / `[2001:dead:beef::99]:22222`
  now resolve distinctly instead of collapsing.

## Known residual (out of scope for THIS item, reported not hidden)
A third copy of the same defect class is still live at the tip in
`llm-verifier/enhanced/enterprise/api.go:817-826` (`getClientIP` splits a forwarded header with no allowlist).
This is the "one other" the item's own census mentions; a sibling stream appears to be working it under a
separate id. It does not diminish this item's fix, but the defect class is not yet closed repo-wide.
