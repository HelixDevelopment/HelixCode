# HXC-268 — landed-fix closure evidence

- **Date:** 2026-08-13 · **Triaged by:** AI-agent (T1/main - claude1) · **Verdict:** CLOSE (Bug → fixed)

## Fix commit
- Repo `submodules/llms_verifier`, commit `3951010c`, branch tip `9bf5457d`, **reachable** (`merge-base --is-ancestor` → true).

## Defect gone (read at the committed tip, NOT the working tree)
The repo's working tree is dirty with a sibling stream's uncommitted work, so all reads/tests were pinned to
a clean `git archive 9bf5457d` extract. Instrument validity confirmed: the extract does **not** contain the
sibling's untracked `llm-verifier/clientip/` package.

- `llm-verifier/pkg/helixendpoint/helixendpoint.go:723`
  `return "http://" + net.JoinHostPort(normalized, strconv.Itoa(port))` — bracket-aware composition replaces
  `fmt.Sprintf("http://%s:%d", ...)`. `normalizeHost` (:589) unbrackets first, so an already-bracketed host is
  not double-bracketed.
- Remaining unbracketed sites in the capability-config generator: **0**
  (`grep -cE '%s:%d' capabilities/config_generator.go` → 0). The item's acceptance criterion
  ("count of remaining unbracketed sites is zero") is met. 15 sites changed, matching the item's corrected count.

## Covering guard (falsifiable, §11.4.115 polarity switch = env `RED_MODE`)
- `capabilities/hxc268_ipv6_authority_red_test.go::TestHXC268IPv6AuthorityRED` (+ sibling `hxc268_dial_address_red_test.go`)
- `go test -count=1 -run HXC268 ./capabilities/ ./pkg/helixendpoint/ ./auth/ ./events/ ./pkg/cliagents/ ./scoring/` → **exit 0**
- `RED_MODE=1 <same>` → **exit 1** (defect correctly does NOT reproduce on the fixed artifact) — proves the guard
  can fail and is not a test that merely agrees with the fix.

## Notes
Commit fixed 24 sites total (15 in-scope + 9 the item deliberately excluded). 3 residual `%s:%d` grep hits are
`//` doc-comment citations of the removed pattern, not live code.
