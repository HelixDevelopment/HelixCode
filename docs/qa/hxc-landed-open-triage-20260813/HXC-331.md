# HXC-331 — landed-fix closure evidence

- **Date:** 2026-08-13 · **Triaged by:** AI-agent (T1/main - claude1) · **Verdict:** CLOSE (Bug → fixed)

## Fix commit
- Repo `meta`, commit `d634cc86`, branch tip `bd623933`, **reachable**. Nothing between the two commits touches
  this item's files (`git diff --name-only d634cc86 bd623933` → go.mod, go.sum, two submodule pointers only).

## Both halves the item asks for are delivered (read at the committed tip; `scripts/secret_scan.sh` is sibling-owned and dirty)
- **(a) The seven findings were triaged.** Disposition recorded in the tracker row: **zero real credentials**,
  no rotation required, so no CONST-042 event. Kinds only (no values reproduced anywhere): documentation-context
  matches inside a prior triage table and a config snippet whose body is an ellipsis, plus synthetic test fixtures
  including a meta-test plant whose "key body" is an English sentence.
  Remediation is two mechanisms: four explicit `.html` allowlist entries in `.scan-secrets-allow` (root cause: the
  allowlist held zero `.html` globs while the doc-export wave added hundreds of rendered siblings), and an
  in-content marker at `scripts/test-verify-all-constitution-rules.sh:142` (chosen because the G5 gate keys off the
  `.pem` filename and never reads the body, so an allowlist entry would have permanently blinded a live script).
- **(b) The audit row was corrected.** `git show bd623933:docs/audit/bypass_events.md`
  `:4` `### CORRECTIONS 2026-08-13 (HXC-331)`; `:13` row 2's "git tree verified secret-clean" explicitly
  **RETRACTED**. Append-only: original rows left verbatim and corrected by appended retraction. `.html` twin
  carries `RETRACTED`; `.pdf` regenerated in the same commit.

## Covering guard — EXERCISED via §1.1 paired mutation, in an isolated sandbox
The in-repo G5 meta-test writes to the repo (`git add -f` a fixture) and `secret_scan.sh` is sibling-owned and
dirty, so neither could be run against the live tree. Instead the LANDED state was materialised into a non-git
scratch sandbox (`REPO_ROOT` falls back to `pwd`), containing only the tip's `scripts/secret_scan.sh`, the tip's
`.scan-secrets-allow`, and the four allowlisted `.html` files at their tip paths:

- **Baseline** `bash scripts/secret_scan.sh <4 files>` → **exit 0**
  (`OK: no unallowlisted key-shaped secret pattern found (mode=files)`)
- **Mutation** — drop each allowlist entry in turn, one at a time:
  - drop `docs/improvements/05_phase_0_evidence.html` → **exit 1**
  - drop `docs/troubleshooting/guide.html` → **exit 1**
  - drop `docs/superpowers/plans/2026-05-04-phase-0-foundation-cleanup.html` → **exit 1**
  - drop `docs/superpowers/plans/2026-05-06-p2-f22-aider-git-auto-commit.html` → **exit 1**
  - restore all → **exit 0**

Every entry is load-bearing, so the fix genuinely binds and the scanner still detects these strings when the
entry is absent — i.e. the remediation suppresses by explicit allowlist, not by weakening detection.
No matched value was printed at any point; nothing in the repo was read-modified, staged, or committed.

## Residual (recorded, not blocking)
- The commit message's sub-classification ("two documentation examples and five synthetic fixtures") is wrong —
  measured contexts read 3 documentation + 4 fixtures. Total of 7 unaffected; the correction currently lives only
  in the uncommitted worktree DB.
- No `docs/qa/*hxc331*` artifact at tip: the full-tree rc=1→rc=0 runs are narrated in the commit body, not captured.
- Scope boundary stated honestly in the record: "zero real credentials" covers **these seven findings**, and is
  not a clean-tree proof.
