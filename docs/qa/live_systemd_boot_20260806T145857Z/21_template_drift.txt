=== drift: tracked template (expanded) vs installed copy — helixllm-gateway ===
24c24
< # submodules/helix_llm/internal/shared/config/config.go. A previous revision set
---
> # internal/shared/config/config.go. A previous revision set
28c28
< # listens there, and the gateway logged on every start:
---
> # listens there, and the gateway logged
31,32c31,32
< # Verified 2026-07-28 by diffing every Environment= name in this unit against
< # every env tag in config.go: these two were the only mismatches.
---
> # on every start. Verified 2026-07-28 by diffing the unit's Environment= names
> # against every env tag in config.go: these two were the only mismatches.
38c38
< # natively as a user service that produced
---
> # natively as a user service, that produced
40c40
< # for every auto-download. %h/models is the host's existing model store.
---
> # for every auto-download. %h/models is the host's existing 28G model store.
diff_exit=1

=== same check for the other five units ===
IN-SYNC   helixcode-infra
IN-SYNC   llmsverifier
IN-SYNC   helixllm-coder
IN-SYNC   helixagent
IN-SYNC   helixcode-server
