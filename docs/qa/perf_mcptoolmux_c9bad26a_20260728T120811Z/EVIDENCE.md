# MCP tools/list stays correct and fast under concurrent client load (toolMux lock-hold fix) — QA evidence (§11.4.83)

**Run ID:** `perf_mcptoolmux_c9bad26a_20260728T120811Z`
**Source commit:** `c9bad26aa7821832cc6297d7d2193731ba8ac733`
**Captured (UTC):** 2026-07-28T12:08:13Z
**Verdict:** PASS
**Counts:** PASS=9 FAIL=0 SKIP=0

## Commit under test
```
c9bad26aa7821832cc6297d7d2193731ba8ac733
Test User
2026-06-24T13:33:54+03:00
fix(mcp): move blocking I/O out of toolMux critical section (lock-hold O(N^2) → snapshot-then-act)
```

## How this evidence was produced

Generated by `scripts/qa/capture_perf_mcptoolmux_c9bad26a.sh` — a real HTTP capture harness.
Every row below was evaluated against bytes observed on the wire during this run;
the full bidirectional trace for each case (request lines sent, response headers and
body received) is in `transcripts/<case-id>.http`. The harness FAILs any assertion
whose transcript is missing or contains no response — it cannot emit a PASS without
captured evidence (§11.4 / §11.4.1 / §11.4.5).

Exit codes: `0`=all PASS, `1`=a security assertion FAILED, `2`=INCOMPLETE (a case was
SKIPped with reason, §11.4.3 — never counted as a pass).

## Per-assertion verdicts

| Result | Case | Assertion | Expected | Observed |
|--------|------|-----------|----------|----------|
| PASS | `live_ws_route_gated` | status == 401 | `401` | `401` |
| PASS | `live_ws_route_gated` | response header 'Sec-WebSocket-Accept' absent | `absent` | `absent` |
| PASS | `live_ws_route_gated` | body excludes 'page not found' | `absent` | `absent` |
| PASS | `live_ws_unregistered_control` | status == 404 | `404` | `404` |
| PASS | `live_ws_unregistered_control` | body contains 'page not found' | `page not found` | `present` |
| PASS | `eph_tools_list_functional` | single tools/list call returns a well-formed, correlated response | `ok=true` | `ok=true` |
| PASS | `eph_concurrent_all_ok` | every concurrent call returns a well-formed, correlated tools/list response | `0 errors` | `0 errors / 32 calls` |
| PASS | `eph_concurrent_faster_than_sequential` | concurrent_wall_clock_ms < sequential_wall_clock_ms | `concurrent < 34.088383999915095ms` | `23.63165800124989ms` |
| PASS | `eph_concurrent_latency_bound` | concurrent p95 latency <= 3x sequential max (floor 50ms) | `<= 150.000ms` | `0.7601690012961626ms` |

## Grounding (§11.4.108)

```
# Grounding facts captured 2026-07-28T12:08:11Z
# §11.4.108: the running artifact must be the one built from the fixed source.

## repo HEAD
d8c44618 docs(qa): §11.4.83 evidence for e330149f model-identity fix

## fix commit under test
c9bad26a fix(mcp): move blocking I/O out of toolMux critical section (lock-hold O(N^2) → snapshot-then-act)

## fix commit is an ancestor of HEAD?
YES — c9bad26aa7821832cc6297d7d2193731ba8ac733 <= HEAD

## live server under test
base url: http://localhost:8081
(no listening helixcode process found via ss; grounding limited)
```

## Files

- `verdicts.tsv` — machine-readable per-assertion results
- `grounding.txt` — artifact/runtime identity facts
- `transcripts/*.http` — full bidirectional wire traces
- `ephemeral-server.log` — boot log of the purpose-configured instance
- `ephemeral-config.yaml` — generated config for that instance
