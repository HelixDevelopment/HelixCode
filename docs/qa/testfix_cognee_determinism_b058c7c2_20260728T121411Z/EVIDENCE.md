# internal/cognee package determinism under repeated/parallel load — §11.4.50/§11.4.6 — QA evidence (§11.4.83)

**Run ID:** `testfix_cognee_determinism_b058c7c2_20260728T121411Z`
**Source commit:** `b058c7c28c48dd09aa30cfaa5b5bf60c8565c35e`
**Captured (UTC):** 2026-07-28T12:17:11Z
**Verdict:** PASS
**Counts:** PASS=17 FAIL=0 SKIP=0

## Commit under test
```
b058c7c28c48dd09aa30cfaa5b5bf60c8565c35e
Test User
2026-06-24T12:43:14+03:00
fix(tests/cognee): make the package deterministic under load (§11.4.50/§11.4.6) — no product change
```

## How this evidence was produced

Generated by `scripts/qa/capture_testfix_cognee_determinism_b058c7c2.sh` — a real HTTP capture harness.
Every row below was evaluated against bytes observed on the wire during this run;
the full bidirectional trace for each case (request lines sent, response headers and
body received) is in `transcripts/<case-id>.http`. The harness FAILs any assertion
whose transcript is missing or contains no response — it cannot emit a PASS without
captured evidence (§11.4 / §11.4.1 / §11.4.5).

Exit codes: `0`=all PASS, `1`=a security assertion FAILED, `2`=INCOMPLETE (a case was
SKIPped with reason, §11.4.3 — never counted as a pass).

## Per-assertion verdicts

| Result | Case | Assertion | Expected | Observed |
|--------|------|-----------|----------|----------|
| PASS | `prod_var_never_reassigned` | zero matches: no production (non-test) code path anywhere in internal/cognee ever reassigns these vars — grep found nothing to reassign, so it correctly exits non-zero | `not 0` | `1` |
| PASS | `prod_var_never_reassigned` | the one production file that changed does not itself contain a reassignment — only the var declarations (checked next), which is not a reassignment | `absent` | `absent` |
| PASS | `prod_defaults_unchanged` | the three var declarations are present and readable | `0` | `0` |
| PASS | `prod_defaults_unchanged` | nvidia default is unchanged at 2s (matches the const value before this commit) | `var nvidiaSmiQueryTimeout = 2 * time.Second` | `present` |
| PASS | `prod_defaults_unchanged` | apple ioreg default is unchanged at 2s | `var appleIoregQueryTimeout = 2 * time.Second` | `present` |
| PASS | `prod_defaults_unchanged` | intel default is unchanged at 3s | `var intelGPUTopQueryTimeout = 3 * time.Second` | `present` |
| PASS | `full_package_count5` | the full package passes cleanly across 5 repeated runs in a single invocation | `0` | `0` |
| PASS | `full_package_count5` | no run in the 5x repetition reports a failure | `absent` | `absent` |
| PASS | `full_package_independent_run_1` | independent process invocation #1 passes | `0` | `0` |
| PASS | `full_package_independent_run_2` | independent process invocation #2 passes | `0` | `0` |
| PASS | `full_package_independent_run_3` | independent process invocation #3 passes | `0` | `0` |
| PASS | `stress_concurrent_alone_repeated` | TestClientHTTP_Stress_ConcurrentRequests, run alone and repeated 5x, passes every time — the commit's own '12/15 fail even alone' pre-fix failure mode does not reproduce | `0` | `0` |
| PASS | `stress_concurrent_alone_repeated` | zero failures across the repeated stress-test-alone runs | `absent` | `absent` |
| PASS | `gpu_probe_chain_repeated` | all TestGetGPUUsage_ProbeChain_* variants pass across 5 repeated runs | `0` | `0` |
| PASS | `gpu_probe_chain_repeated` | zero failures across the repeated GPU probe-chain runs | `absent` | `absent` |
| PASS | `full_package_race` | -race is clean across the full package | `0` | `0` |
| PASS | `full_package_race` | no data race detected | `absent` | `absent` |

## Grounding (§11.4.108)

```
# Grounding facts captured 2026-07-28T12:14:11Z
# §11.4.108: the running artifact must be the one built from the fixed source.

## repo HEAD
1c9fdcf2 docs(qa): §11.4.83 evidence for the Cerebras, Cohere and MCP-concurrency commits

## fix commit under test
b058c7c2 fix(tests/cognee): make the package deterministic under load (§11.4.50/§11.4.6) — no product change

## fix commit is an ancestor of HEAD?
YES — b058c7c28c48dd09aa30cfaa5b5bf60c8565c35e <= HEAD

## live server under test
base url: <unset>
(no listening helixcode process found via ss; grounding limited)
```

## Files

- `verdicts.tsv` — machine-readable per-assertion results
- `grounding.txt` — artifact/runtime identity facts
- `transcripts/*.http` — full bidirectional wire traces

## Honest scope statement (§11.4.6) — read this before the table above

**What this commit's user-visible surface actually is: effectively none.**
`b058c7c2` touches five files under `internal/cognee`: four are Go test
source, and one — `performance_optimizer.go` — is production source, but
the change there is a mutability change only (three GPU-probe shell-out
timeout constants converted from `const` to `var`, mirroring the
pre-existing `rocmSmiQueryTimeout` var), not a behaviour change: the
default values are unchanged and no non-test code path anywhere in the
package ever reassigns them (verified live in PHASE 1 below — this run does
not just repeat the commit message's "no product change" claim, it checks
it). There is no request/response behaviour, no CLI output, and no
end-user journey this run exercises, and none is claimed above.

**What this commit's actual deliverable is, and what this run proves**:
DETERMINISM — that the `internal/cognee` package, previously load-flaky
(a different test failing each run, all passing in isolation), now passes
consistently under repeated and parallel execution. This run captures real,
repeated `go test` invocations in `transcripts/*.txt` (not `*.http` —
there is no wire trace for a test-only change):

1. The full package passes cleanly across 5 repeated runs in a
   single `-count=5` invocation, AND across 3 fully independent
   process invocations run back-to-back (independent processes catch
   scheduling-level nondeterminism that in-process repetition can mask).
2. `TestClientHTTP_Stress_ConcurrentRequests` — the test the commit
   specifically names as having failed even in isolation pre-fix
   ("12/15 fail even alone") — passes every time across 5 repeated
   solo runs.
3. The `TestGetGPUUsage_ProbeChain_*` family — the tests the commit
   describes as flaking under parallel `go test ./...` load due to a fake
   subprocess being signal-killed before the (pre-fix, fixed-duration) probe
   timeout elapsed — pass every time across 5 repeated runs.
4. `go test -race` is clean across the full package: the keep-alive /
   client-timeout rework in `client_stresschaos_test.go` introduced no
   new data race.

**What this run does NOT prove:** that any end user or production request
path is affected, because none is — this is a test-infrastructure fix. It
also does not prove ABSOLUTE non-flakiness (no finite number of repeated
runs can); it proves the specific, previously-documented failure modes this
commit targeted do not reproduce across 5+ repeated and 3
independent runs in this session, which is the concrete, falsifiable claim
the commit message actually makes ("Verified: ... -count=3 PASS twice").
