=== proof G28 was free: git log -p on the base revision this HEAD started from ===
(grep for any G28 in verify-all-constitution-rules.sh at HEAD before this change)
10:# Gates included (each scoped to what's mechanically checkable):
128:# G1 — Governance cascade
142:# G2 — CONST-035 anti-bluff smoke (production code only)
162:# G3 — CONST-050(A) mock-from-production
189:# G4 — CONST-051(C) nested-own-org submodule chains
222:# G5 — CONST-053 .gitignore audit + sensitive-file presence
263:# G6 — CONST-052 case-conformance (soft, surfaces candidates only)
372:# G7 — §11.4.83 docs/qa end-user evidence (enforcing, baseline-scoped)
411:# G8 — §11.4.90 Obsolete-Details presence on Obsolete-status items
425:# G9 — §11.4.91 summary-doc clarity (no anti-pattern one-liners)
439:# G10 — §11.4.81 cross-platform parity (HXC-015)
453:# G11 — §11.4.93/95 workable-items md↔db in sync (HXC-013/HXC-026)
467:# G12 — §11.4.12/53 summary freshness (Issues_Summary/Fixed_Summary vs the
484:# G13 — §11.4.99 Sources-verified footer coverage (ENFORCING since HXC-030 closed)
501:# G14 — §11.4.106 docs_chain verify — governance docs tree in-sync
550:# G15 — §11.4.153/§11.4.86 feature-Status video-evidence durability
564:# G16 — §11.4.135 Android challenge-matrix per-OS-dispatch contract guard
578:# G17 — §11.4.108/§11.4.110 fyneui main-goroutine dispatch contract
604:# G18 — §11.4.12/§11.4.120 Fixed-summary item visibility
636:# G19 — §11.4.108 per-commit compile integrity (recent window)
706:# G20 — §11.4.106/§11.4.157 superseded-generator naming
738:# G21 — §11.4.10/CONST-042 QA-transcript redaction, fail-closed
783:# G22 — CONST-046 toolschema i18n default resolves real text (runtime probe)
834:# G23 — HXC-194 mock-service CORS origin allowlist (runtime probe)
877:# G24 — HXC-201 workable-items export writes to its DOCUMENTED destination
899:# Go toolchain / workable-items source / tracked DB present) and MUST NOT be
922:# G25 — HXC-193 + HXC-195 mcp-server port agreement (runtime bind probe)
963:# G26 — HXC-212 mcp-server SSE CORS allowlist (CM-MCP-SERVER-CORS-ALLOWLIST)
993:# G27 — HXC-186 G7 RULE 3 citation-vs-coincidence (CM-QA-EVIDENCE-DECLARED-CITATION)
995:# Guards the ENFORCING §11.4.83 gate (G7) against being satisfied by accident.
996:# G7's RULE 3 cleared a feature commit whenever any tracked docs/qa file

=== full gate-id enumeration on ORIGINAL file (pre-change) ===
# G1 —
# G10 —
# G11 —
# G12 —
# G13 —
# G14 —
# G15 —
# G16 —
# G17 —
# G18 —
# G19 —
# G2 —
# G20 —
# G21 —
# G22 —
# G23 —
# G24 —
# G25 —
# G26 —
# G27 —
# G3 —
# G4 —
# G5 —
# G6 —
# G7 —
# G8 —
# G9 —

=== full gate-id enumeration on CURRENT file (post-change) ===
# G1 —
# G10 —
# G11 —
# G12 —
# G13 —
# G14 —
# G15 —
# G16 —
# G17 —
# G18 —
# G19 —
# G2 —
# G20 —
# G21 —
# G22 —
# G23 —
# G24 —
# G25 —
# G26 —
# G27 —
# G28 —
# G3 —
# G4 —
# G5 —
# G6 —
# G7 —
# G8 —
# G9 —
