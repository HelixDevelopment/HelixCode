# Execution state for 002-adaptive-local-model-serving
# Updated by /speckit-superspec-execute as tasks complete.
feature: 002-adaptive-local-model-serving
branch: main
started: 2026-09-02T19:24+02:00
total_tasks: 97

current_phase: 2
phase_name: Foundational (Blocking Prerequisites)

in_flight_agents: 6   # at the §11.4.58 parallel cap
  # T006-T012 capability measurement chain
  # T014+T019 catalogue loader + integrity
  # T015 text.yaml · T016 vision_image.yaml · T017 video.yaml · T018 speech_audio.yaml

tasks:
  T001:
    status: complete
    summary: >-
      Seven package skeletons created under internal/ with doc.go stating each
      package's single responsibility: capability, catalogue, selection, runtime,
      lifecycle, telemetry, failover. gofmt clean; go build ./internal/... exit 0.
  T002:
    status: complete
    summary: >-
      Six fixture hosts + HostCapabilityProfile. DeviceIdentity is a distinct type
      so an enumeration index cannot be assigned (§11.4.111 enforced by the
      compiler). AcceleratorState is a separate field from the slice, so
      no-accelerator and unmeasurable both have len()==0 and cannot be confused.
      DualAcceleratorReversed() exists for the T008 RED. Two paired mutations both
      failed correctly, then restored.
  T003:
    status: complete
    summary: >-
      Entry type + five fixtures. StreamingEligible() reads the roster only;
      Architecture has no path into it (research D1 enforced structurally). The
      ineligible-but-MoE fixture shares architecture AND memory with the roster
      member, isolating roster-vs-architecture and memory-vs-storage. Paired
      mutation reintroducing the D1 defect failed correctly, then restored.
  T005:
    status: complete
    note: satisfied by T002 (profile.go)
  T013:
    status: complete
    note: satisfied by T003 (entry.go)
  T004:
    status: complete
    summary: >-
      BASELINE GREEN. 29 packages ok, 0 FAIL, 6 with no test files. Completion
      proven rather than assumed: the run reached internal/vrambroker, and no
      package sorts alphabetically after it, so the run was not truncated.

gates:
  baseline_build: PASS      # go build ./... exit 0, 2026-09-02
  baseline_unit_tests: PASS   # 29 ok / 0 FAIL, run reached the last package

notes:
  - Video generation added to scope by operator decision 2026-09-02; wired into
    spec, plan and tasks before execution began.
  - Phase checkpoints require explicit operator approval before the next phase.

# Defects found in EXISTING code/docs while executing Phase 2.
# Recorded rather than silently fixed — each needs its own investigation
# and its own commit (§11.4.124 investigate-before-change).
findings:
  - id: EX-1
    where: submodules/helix_llm/services/videogen/.gitignore-meta/wan_ltx_gguf.yaml
    what: >-
      The manifest declares expected_disk_usage "~18 GiB" for the default video
      path. Measured live from the upstream blob API on 2026-09-02: 31.85 GiB.
      Understated by ~14 GiB because the UMT5-XXL text encoder is downloaded in
      full and then CPU-offloaded. A host provisioned to the manifest figure
      would run out of disk mid-fetch.
    severity: HIGH
    status: recorded

  - id: EX-2
    where: submodules/helix_llm/services/videogen/videogen_server.py
    what: >-
      The `ltx` backend is configured against the Lightricks/LTX-Video repo id,
      which contains NO .gguf file at all. LTXPipeline.from_pretrained() on that
      id resolves the diffusers-format subtree (a smaller transformer), not the
      13B checkpoints the service documents. So the shipped ltx backend does not
      serve 13B without a VIDEOGEN_MODEL override.
    severity: HIGH
    status: recorded

  - id: EX-3
    where: submodules/helix_llm/internal/catalogue/entry.go
    what: >-
      Missing CapabilityFamily constants for video-generation and
      audio-classification; six real catalogue entries fail Validate().
      Found independently by two data agents.
    severity: CRITICAL
    status: routed to the agent that owns internal/catalogue/*.go

  - id: SELF-1
    where: this session's own baseline-completion waiter
    what: >-
      I armed `until ! pgrep -f "make test-unit"` to detect the baseline
      finishing. The waiter's OWN command line contains that literal string, so
      pgrep matched the waiter itself and it would have waited forever. The
      baseline had actually finished 17 minutes earlier. Detected by checking
      whether the log was GROWING rather than trusting process-alive as liveness.
      This is precisely the §11.4.201 false-positive class and the §11.4.174
      "is that process actually ours?" discipline — a filter that matches the
      checking command is a broken filter.
    severity: MEDIUM
    status: fixed (waiter killed; result verified directly from the log)

  - id: SELF-2
    where: this session's dispatch of the four catalogue-data agents
    what: >-
      I sent four agents to produce catalogue data and pointed each at
      data-model.md (PROSE) for its schema, rather than at the Go type that
      actually defines it. Four agents produced four different, individually
      reasonable YAML shapes; none loads. 294 divergences. The loader agent was
      correct to refuse to widen the loader to swallow all four — unknown-key
      tolerance is precisely how a misspelled storage_requried_bytes becomes a
      silent zero.
      LESSON: schema-sensitive work must not be parallelised until the schema is
      an artifact. It now is — a doc comment at the top of internal/catalogue/
      load.go — so reconciliation goes to ONE agent, not four.
    severity: HIGH
    status: schema now authoritative; reconciliation pending as a single task

  - id: EX-4
    where: submodules/helix_llm/internal/catalogue/entry.go (Validate)
    what: >-
      Validate required a complete integrity expectation, which made the
      catalogue unloadable in its ordinary initial state: a digest is captured AT
      first download, so requiring one to load meant nothing could load, nothing
      could be downloaded, and no digest could ever be captured. All four
      independently-researched data files hit this simultaneously — the strongest
      possible signal it was structural rather than a data defect.
      FIXED: the requirement moved to ValidateForAcquisition, alongside Source.
      An entry is now valid to OFFER while unfetched, and invalid to ACQUIRE.
      SC-011 governs loading a model FILE, not reading the catalogue, so the
      invariant is unchanged — only the gate it sits at.
      Two pre-existing subtests asserted the old behaviour; they were RECONCILED
      to the new gate (not deleted, not weakened) and a paired mutation confirms
      the reconciled gate still fails when the check is removed.
    severity: CRITICAL (was fleet-blocking)
    status: fixed

  - id: EX-5
    where: submodules/helix_llm/internal/gateway/openai.go:33
    what: >-
      `hardcodedModels` is a static three-entry model list returned by
      GET /v1/models and /v1/models/:id whenever no Brain is configured. It is
      the BLUFF-002 anti-pattern the project's own manual names explicitly
      (CONST-036/037: no hardcoded model lists; the verifier is the single
      source of truth).
      It is worse than a stale list: it advertises `gpt-4o` and
      `claude-sonnet-4-20250514` as OwnedBy "helix". A client listing models with
      no backend configured receives three models that do not exist and cannot
      serve — the exact false-availability defect FR-019 forbids ("a model not
      actually being served is never listed as available").
      Honest fix: return an empty list plus a stated reason (no backend
      configured), rather than fabricating three. Not deleted here — it is
      outside this feature's task list and deserves its own investigation and
      commit (§11.4.124).
    severity: HIGH
    status: recorded

  - id: EX-6
    where: submodules/helix_llm/internal/brain (routing)
    what: >-
      Found by the naming agent, and it nearly shipped as a silent defect:
      changing the published `id` WITHOUT a name-resolution path would have been
      actively dangerous. Routing matches on provider model names, so a client
      that listed a new identifier and then requested it would miss every exact
      match and fall through to "any available provider" — a silent MISROUTE,
      not an error. The agent built ResolveModelName and wired it into
      Complete/CompleteStream so both a new identifier and a raw name already in
      a user's config resolve to the same provider.
    severity: CRITICAL (prevented before landing)
    status: fixed as part of T043

  - id: EX-7
    where: submodules/helix_agent/internal/catalog/defaults.go:20
    what: >-
      `DefaultHelixLLMModels()` returns a hardcoded []string{"helixllm-default"},
      fed into router.go:858 as the catalogue's model source. This is the SAME
      hardcoded-model-list anti-pattern as EX-5 in the HelixLLM gateway, now
      found independently in a second repository — which suggests the pattern,
      not the instance, is what needs fixing.
      The live option source is now plumbed and tested (Options.HelixLLM) but
      deliberately NOT wired into router.go: the adapter is constructed nowhere
      in that repo, so wiring it is a new integration (per-request HTTP inside a
      JSON handler, timeouts, failure policy) rather than a substitution. Left
      as an explicit operator decision; today's /v1/catalog output is
      byte-identical to before.
    severity: HIGH
    status: recorded; live source plumbed but not wired

  - id: OPEN-1
    where: submodules/helix_llm — /v1/models listing semantics
    what: >-
      FR-019 requires a consuming tool to distinguish available from unavailable
      models, but /v1/models is an OpenAI-COMPATIBLE endpoint where clients
      assume everything listed is usable. Listing withheld entries there would
      break that assumption; omitting them loses FR-019 on the HTTP surface.
      Resolved for now by emitting affirmative `availability: "serving"` plus
      `host` for listed models (honest, compat-safe, and enough for HelixAgent's
      three-valued unreported/serving/withheld decode). The withheld half needs
      a richer endpoint or an explicit opt-in query parameter — a design choice,
      not something to settle by quietly stuffing withheld entries into an
      OpenAI-compatible listing.
    severity: MEDIUM
    status: open — needs a design decision

  - id: EX-8
    where: submodules/helix_llm/internal/catalogue/load.go (entryDoc.entry)
    what: >-
      `Source` was declared with a yaml tag and decoded, but entry() never
      assigned it into the returned Entry — so EVERY loaded entry had
      Source == "". The FR-012/SC-011 source allowlist was therefore silently
      DISARMED: the gate still ran on every acquisition, but always against an
      empty string.
      This was MY defect. I added Entry.Source and tested the struct field; I did
      not test the LOAD PATH. The struct test passed throughout. Found
      independently by two agents (boot-migration and reconciliation).
      FIXED, plus two permanent guards: a round-trip test asserting a source
      survives loading, and TestShippedCatalogueLoads — because nothing in the
      suite had ever loaded the real data/ directory, which is exactly how four
      incompatible file shapes landed there unnoticed. Shipped catalogue now
      proven: 30 entries, 6 families, 24 carrying a source.
    severity: HIGH (security-adjacent — a disarmed allowlist)
    status: fixed

  - id: SELF-3
    where: this session's paired mutation on the Source fix
    what: >-
      My first mutation attempt reported "ok" — tests passing under mutation,
      which reads as a toothless guard. It was not: gofmt had re-aligned the
      struct field, so my replacement text never matched and the mutation NEVER
      APPLIED. A mutation that fails to apply and a guard with no teeth produce
      byte-identical output.
      LESSON: every mutation must assert it actually changed the file (exact-count
      assert plus a diff check) before its result is believed. A "surviving"
      mutation is a claim requiring evidence, not a finding.
    severity: MEDIUM
    status: fixed; mutation re-run correctly and the guard FAILs as it should

  - id: EX-9
    where: submodules/helix_llm/internal/selection/family.go (familyOrder)
    what: >-
      familyOrder listed 8 families while the catalogue records 10. The two
      missing were exactly the two added later: video-generation (when the
      operator brought it into scope) and audio-classification (when research
      showed it must stay separate from audio-generation, one being
      processor-viable and the other not).
      The failure was SILENT: a request naming no families enumerated neither —
      no error, no empty family with a stated reason, just an absence. Three
      video entries load and would never have been offered. That is precisely
      the outcome the per-family non-empty guarantee exists to prevent, defeated
      one level above where the guarantee is enforced.
      Found by the documentation agent while reading code to write the guide —
      it flagged it honestly as "probable, could not run it to confirm". It was
      real. FIXED, with a test binding familyOrder to the recorded family set so
      the next family added cannot be forgotten the same way.
    severity: HIGH
    status: fixed

  - id: EX-10
    where: submodules/helix_llm/internal/catalogue/data/ (all four files)
    what: >-
      Measured, not estimated: all 30 shipped entries carry an integrity
      algorithm and size but NO digest, so Integrity.Complete() is false for
      every one and ValidateForAcquisition fails catalogue-wide. Nothing shipped
      is acquirable today. Source is present on 24 of 30 (absent on speech/audio
      and video).
      This is correct behaviour, not a defect — digests are captured at first
      download — but it means "visible but not acquirable" is currently the
      UNIVERSAL state of the catalogue rather than an edge case, and the docs
      now say so with the count rather than describing it hypothetically.
    severity: INFO
    status: recorded as a measured fact

  - id: EX-11
    where: submodules/helix_llm/services/imagegen/imagegen_server.py:86
    what: >-
      The imagegen BOOT BINARY was migrated onto measured selection, but the
      service kept `MODEL_ID = _env("IMAGEGEN_MODEL", "black-forest-labs/
      FLUX.1-schnell")` one layer down — so a boot path that correctly refused
      to choose, or an operator running the service directly, still received a
      model no host was measured against. Fixing the binary alone fixed the half
      that is easy to see. PRECISION had the same shape, and it determines the
      memory footprint admission was granted for.
      Found by the videogen agent while migrating the sibling lane.
      FIXED with the same `_decided()` no-default helper the videogen service
      uses, plus an AST-based guard scoped to module-level assignments.
    severity: HIGH
    status: fixed

  - id: EX-12
    where: submodules/helix_llm/internal/catalogue/data/video.yaml
    what: >-
      No video entry recorded `source:`, so the videogen lane could choose a
      model and then refuse to boot it (exit 24, "records no weight source").
      The migrating agent deliberately did NOT fall back to
      annotations.weights_repo — annotations are contractually "carried and
      shown, never read to make a decision", so using one would hand the runtime
      a location the allowlist gate never saw. That restraint kept the gate
      meaningful instead of quietly routing around it.
      FIXED for the two WAN entries against the shipped weight manifest.
      LTX DELIBERATELY LEFT WITHOUT A SOURCE: the obvious URL
      (huggingface.co/Lightricks/LTX-Video) contains no .gguf at all and would
      reproduce EX-2 — a source that passes the gate and then fails at load. The
      reason is recorded in the data file itself.
      VERIFIED END TO END on this host: videogen-boot now measures, withholds LTX
      on usage terms with a distinct remedy, and chooses wan2.2-ti2v-5b from
      measurement rather than configuration.
    severity: HIGH
    status: fixed (2 of 3 entries; the third is correctly unservable)

  - id: SELF-4
    where: this session's imagegen guard test
    what: >-
      My first version flagged every repository string in the file, including
      the module docstring explaining WHICH FLUX repositories are gated and why —
      knowledge someone established with a dated API check. That test goes green
      by deleting the explanation. It also flagged
      _default_nvfp4_transformer(model_id), which DERIVES the quantised
      transformer from the chosen model and cannot produce one when nothing was
      chosen.
      LESSON: the defect class was "a value standing ready when nothing chose
      one", not "a repository string appears somewhere". Scoped to module-level
      assignments, the guard catches the defect and costs nothing true.
    severity: LOW
    status: fixed before landing

  - id: EX-13
    where: submodules/helix_llm/internal/catalogue/data/speech_audio.yaml
    what: >-
      For a period the only LOADING text-to-speech entry was xtts-v2 under CPML —
      non-commercial, and unlicensable at all since Coqui shut down. A commercial
      caller asking for speech synthesis therefore received NOTHING: an empty
      family. Nothing failed and nothing warned; the catalogue looked healthy.
      The refusal was correct — those licences really do exclude commercial use —
      but an empty family is the state the per-family guarantee exists to
      surface, not to pass over quietly.
      FIXED by promoting kokoro-82m (Apache-2.0, processor-viable, no cloning)
      from the deferred block. It was deferred solely for want of an on-disk
      weight size, which the audio agent measured by HTTP HEAD.
      PROVENANCE RECORDED HONESTLY: that figure is SINGLE-SOURCED. My
      re-verification from the build host returned a 15-byte error page
      (restricted network), so it is one measurement, not two agreeing ones. The
      method is named in the entry so it can be re-run elsewhere.
      GUARDED by TestFamiliesWithACommerciallyUsableOption, which names the
      families where a commercially-usable option is known to exist and fails
      when one stops existing. Deliberately a list, not "every family" — some
      families genuinely have no commercially-safe option yet, and asserting
      otherwise would be asserting a wish.
    severity: HIGH
    status: fixed

  - id: EX-14
    where: submodules/helix_llm/container/Containerfile.whisper
    what: >-
      Carried `WHISPER_MODEL=base` — a configuration value naming the model to
      run, which is precisely the static selection FR-056 replaces. An unmeasured
      host silently started `base`.
      FIXED: removed; an unmeasured host now reports WHY it cannot choose and
      returns 503 rather than starting something nothing chose. Parakeet is
      refused BY NAME with the reason (CTranslate2 loads Whisper weights, not
      NeMo) instead of being silently mapped to a Whisper size.
      Two corrections to the research record also came out of this work: Piper is
      MIT, not GPL-3.0 (which undercuts the "avoid copyleft" framing the family
      statement used), and YAMNet emits 521 classes, not 527 — load-bearing,
      since the score guard checks vector length against the loaded class map.
    severity: HIGH
    status: fixed

  - id: EX-15
    where: submodules/helix_llm/cmd/visiongen-boot/vision_boot_test.sh:119
    what: >-
      A PASS-BLUFF MECHANISM in the very script whose purpose is proving the
      vision endpoint genuinely works.
      `base64 -w0` is a GNU coreutils extension; BSD/macOS base64 has no -w. The
      script runs under `set -u` but NOT `set -e` and NOT `pipefail`, so on a
      non-GNU host the encode fails silently, B64 is left empty, and the payload
      becomes the literal "data:image/png;base64," — an empty image.
      The downstream assertion only requires non-empty, non-bluff-keyword prose.
      A vision model can answer "Describe this image in one short sentence" from
      the TEXT prompt alone. Result: a PASS claiming the endpoint described an
      image it never received — a sink-side bluff at exactly the layer this
      script exists to close.
      FIXED: portable `base64 | tr -d '\n'` plus an explicit emptiness guard that
      converts a silent empty encode into a loud, named failure. Verified on
      Linux to produce byte-identical output to the old form on real input.
    severity: HIGH
    status: fixed

  - id: EX-16
    where: submodules/helix_llm/internal/capability/measure_darwin_parse.go
    what: >-
      The Darwin honesty boundary was stated in measure_darwin.go and in the
      parser's TEST file, but NOT in the parser itself — the one file that
      actually encodes the shape assumption (vm_stat header format, page-class
      names, sysctl leaf names). A reader arriving at the parser to change it had
      no way to learn its ground truth is documentation rather than observation.
      FIXED: the boundary is now stated where that reader will find it, including
      why it is repeated rather than only cross-referenced.
      Worth noting the shape of this one: not a wrong answer, and every failure
      path already returns an error rather than a figure, so a wrong assumption
      yields a refusal and never a fabricated measurement. The defect was that
      the warning lived everywhere except the place it was needed.
    severity: LOW
    status: fixed

  - id: EX-17
    where: submodules/helix_llm/ (repo root)
    what: >-
      A 10 MB unstripped ELF binary `videogen-boot` sat at the repository root,
      NOT matched by any .gitignore rule — one `git add -A` from being committed.
      Build artefacts are not versioned. Removed.
      This is why staging is done with explicit paths and a deletion check rather
      than `git add -A`; the guard that would have caught it at commit time is
      the same one that has caught contaminated staging twice in this session.
    severity: MEDIUM
    status: fixed

  - id: PATTERN-1
    where: three independent occurrences this session
    what: >-
      "NO OUTPUT" IS AMBIGUOUS BETWEEN "CLEAN" AND "BROKEN", and every instance
      of that ambiguity this session read as success.
        1. My baseline waiter: `until ! pgrep -f "make test-unit"` matched its OWN
           command line, so it waited forever for itself. The baseline had
           finished 17 minutes earlier. Caught only by checking whether the log
           was GROWING rather than trusting process-alive.
        2. My paired mutation on the Source fix: gofmt had re-aligned the struct
           field, so the replacement never matched and the mutation NEVER
           APPLIED. Tests reported "ok" — byte-identical to a guard with no
           teeth. Caught by asserting the file actually changed.
        3. The toolkit agent's secret-leak sweep: a crashed
           `grep -r ... 2>/dev/null` yields the same empty result as a clean
           sweep. Its own hygiene test flagged the assertion as vacuous. It also
           hit (1) independently — `pkill -f run-all.sh` killed its own shell.
      THE COMMON SHAPE: a check whose success signal is the ABSENCE of something
      cannot distinguish "the thing is absent" from "the check did not run".
      THE FIX, in all three: assert the check actually ran before believing what
      it did not find — a growing log, a changed file, a positive scanned-file
      count plus an exit status that rules out a crash.
    severity: METHOD
    status: recorded; all three instances fixed

  - id: EX-18
    where: claude_toolkit/scripts/claude-providers.sh (T044/T047/T048)
    what: >-
      Two design judgments worth preserving.
      SECRET CONTAINMENT: the discovery secret is piped into `curl --config -` on
      STDIN, never argv — so it never appears in /proc/<pid>/cmdline or `ps aux`,
      where any local user could read it. The test carries a CONTROL NEEDLE: the
      mock servers record the Authorization header they actually received, so a
      green result cannot come from a request that never carried one. Without
      that control, "no leak found" would also be the result of never
      authenticating at all.
      FR-018 RESTRAINT: the fan-out was deliberately NOT wired into the default
      `sync`. The session hook runs a sync per interactive shell, so wiring it
      would silently rewrite another tool's configuration on every new shell —
      exactly what FR-018 forbids. Export writes the catalogue and prints;
      `--apply` is a separate, explicit act. `sync` behaviour is byte-identical.
      Zero regressions: the failure set is IDENTICAL before and after (2
      pre-existing failures, 12 assertions, unchanged).
    severity: INFO
    status: landed

  - id: CRITICAL-1
    where: submodules/helix_llm/internal/testing/runner.go + challenges/banks/
    what: >-
      THE CHALLENGE HARNESS CANNOT FAIL. Verified directly, not inferred:
        make test-challenges  ->  "0 passed, 0 failed, 0 skipped"  exit 0
      Three independent mechanisms, each sufficient on its own:
        1. runner.go:188 dispatches on `step.Action`. Every bank writes
           `method:`/`path:`/`assertions:` and never `action:`, so Action is
           always empty, every step hits the `default` branch and is marked
           SKIPPED, and runChallenge then sets Status="passed" because nothing
           FAILED. The same "3 passed" prints against an unroutable base URL
           (192.0.2.1:9) — proven at runtime by the sweep agent.
        2. 19 of 28 bank files use a top-level `steps:` key the Bank struct has
           no field for. ~115 declared entries load as ZERO, silently, exit 0.
        3. An empty bank directory is also a green result.
      SWEEP TALLY across 14 mandated targets: 4 genuinely pass, 1 passes the
      WRONG package (test-e2e runs ./tests/integration/ — tests/e2e never runs in
      any target, nor do tests/benchmark or tests/security), 3 fail honestly, and
      6 REPORT SUCCESS WITHOUT EXERCISING ANYTHING. Latency tests pass on
      p50=0s p95=0s p99=0s (n=0).
      WHY THIS MATTERS MOST: this repository's constitution opens with the
      operator's verbatim mandate — "all tests do execute with success and all
      Challenges as well, but in reality the most of the features does not work".
      The harness written to prevent that outcome IS that outcome. Every green
      challenge result in this repo's history means less than it appears to.
      NOT FIXED HERE. This is a structural defect in the verification
      infrastructure, outside this feature's task list, and it changes what every
      past green result means — it needs an operator decision and its own commit,
      not a quiet patch inside a feature branch.
    severity: CRITICAL
    status: FIXED 2026-09-02 — see CRITICAL-1-RESOLVED

  - id: EX-19
    where: submodules/helix_llm — sweep methodology note
    what: >-
      The sweep agent disclosed two things that make its report trustworthy
      rather than merely favourable.
      TREE DRIFT: another writer changed 13 source files in this checkout while
      the sweep ran, so TestShippedCatalogueLoads read 31/6 at one point and 34/8
      later. It confirmed drift rather than flakiness (four runs, three filters,
      consistent), took a confirming re-run with identical tree fingerprints
      either side, and said plainly: do not quote this sweep as "the state of
      HEAD".
      DISCLOSED DEVIATION: six targets first failed with exit 127 (binary
      absent). It ran `make build` and re-ran them, because otherwise the only
      finding would have been "binary missing" — which conceals everything above.
      Both readings are reported separately rather than merged.
    severity: INFO
    status: recorded

  - id: EX-20
    where: submodules/helix_llm/services/{imagegen,videogen}/Containerfile
    what: >-
      MY EX-11 FIX WAS DEFEATED ONE LAYER OUT, and I recorded it as fixed.
      Removing the Python default made _decided() refuse when nothing chose. But
      the IMAGES set the variables anyway — 8 assignments across two
      Containerfiles — so _decided() always read a populated value and the
      refusal path was UNREACHABLE. Running either image directly served a model
      no host was ever measured against.
      Both files carried a comment two lines above claiming "no hardcoded
      model/precision literal baked in as behaviour". The comment asserted
      exactly what the next line contradicted, which is worse than the defect
      alone: a false assurance where a reader would look for the truth.
      FOUND by the T094 guard's DERIVED binding — the forbidden set is each
      service's own _decided("X") call sites, not a name heuristic. That is why
      it caught VIDEOGEN_BACKEND/_SIZE/_NUM_FRAMES/_FPS, which no
      "looks-like-a-model-name" rule would flag. My own grep missed all eight
      because they use backslash line-continuations.
      FIXED: all 8 decided values removed; the 5 genuine operator knobs
      (CPU_OFFLOAD, MAX_STEPS, QUANTIZE_T5) kept; the false comment replaced with
      why the values are absent and what baking them destroys.
    severity: HIGH
    status: fixed

  - id: EX-21
    where: submodules/helix_llm/internal/catalogue (embedding + vector families)
    what: >-
      The INVERSE of EX-9. FamilyEmbedding and FamilyVector were declared in FIVE
      places — the const block, Known(), familyOrder, the Python gate's
      KNOWN_FAMILIES, and asserted by a test — with ZERO catalogue entries. So
      selection took the no-candidates path and reported
      "no catalogued model serves this capability on any host" — on EVERY host,
      regardless of what it could actually run. Declared everywhere, offerable
      nowhere.
      FIXED: 3 entries added (2 embedding, 1 vector), catalogue 31 -> 34 entries
      across 8 families, every figure labelled RECORDED / MEASURED / DERIVED.
      A boundary the agent did NOT paper over: vtracer, the vector-graphics
      engine that actually ships, is not a model — no weights, no licence gating
      a purpose, no per-host memory figure. Cataloguing it would have meant
      inventing every field the catalogue exists to check. Its absence is stated
      in the data file so nobody reads it as a gap.
    severity: MEDIUM
    status: fixed

  - id: OPEN-2
    where: container/helix_model_gate.py vs internal/selection
    what: >-
      A REAL Go/Python divergence, reported rather than silently patched.
      gate.select() ranks admissible entries cheapest-first
      (helix_model_gate.py:401, sorted by memory then storage). Go's
      selection.Select returns offers in CATALOGUE order and leaves preference to
      the lane — videogen-boot's prefer() sorts MOST-CAPABLE-first. So the same
      host picks q4_k_m through the Python path and f16 through the Go path.
      The gate's own header says "Where Go and this module differ, Go is the
      source of truth and this module is the defect", so the direction of the fix
      is already settled; only the decision to make it is outstanding.
    severity: MEDIUM
    status: "closed - see OPEN-2-resolved (previously - open — needs a decision)"

  - id: EX-22
    where: helix_code/config/config.yaml (llm section) vs internal/config.LLMConfig
    what: >-
      T096 asked me to investigate the dead helix-llm/helix-debate provider block
      before proposing its removal (§11.4.124 investigate-before-remove). Doing
      that properly found a LARGER defect than the one I was sent to look at.
      FOUR keys under `llm:` are declared in config.yaml and read by NOTHING —
      computed, not eyeballed, by diffing the YAML keys against LLMConfig's
      mapstructure tags:
          max_retries   timeout   providers   selection
      LLMConfig accepts only default_provider, default_model, max_tokens,
      temperature. Everything else decodes into nothing, silently.
      `timeout` and `max_retries` are WORSE than the provider block. An operator
      who sets `llm.timeout: 30` has every reason to believe they configured a
      timeout. They did not, and nothing tells them. A dead provider block is
      inert; an ignored timeout is an operator holding a false belief about their
      system's behaviour under load.
      Also established: the top-level `ProvidersConfig` that DOES exist is about
      Mem0/Zep/Memonto/BaseAI — memory providers. The name collision with
      `llm.providers` is coincidental and misleading to anyone grepping.
      Also: `default_model` is a struct field the YAML never sets.
      NOT FIXED. This is now a bigger question than "delete the block": either
      those four keys should be honoured, or they should be removed, or the
      loader should REFUSE unknown keys the way the catalogue loader does — which
      is the choice that would have prevented all four. That is an operator
      decision, and the strict-loader option has consequences beyond this file.
    severity: HIGH
    status: investigated — operator decision required, now well-formed

  - id: SELF-5
    where: this session's dispatch of T031 and T072
    what: >-
      I dispatched two agents into the SAME Go package. T031 (vrambroker lease
      wiring) and T072 (streaming process lifecycle) both own internal/runtime/,
      and each independently declared `Lease`, `Admitter` and `brokerAdmitter`.
      The package stopped compiling:
        streaming_launch.go:47: Lease redeclared in this block
        lease.go:41: other declaration of Lease
      Caught not by me but by a THIRD agent (placement), which hit the broken
      build while verifying its own unrelated work and correctly reported it as
      "not mine and pre-existing" rather than trying to fix it.
      The agents then resolved it between themselves — streaming_launch.go kept
      the declarations, lease.go dropped them — and the full tree builds again.
      I verified across two consecutive attempts rather than trusting one read,
      since any single reading of a concurrently-edited tree is a moment in time.
      LESSON: my package-ownership rule ("you own internal/X, stay out of others")
      only works if no two agents are given the same X. I checked scope overlap
      for the DATA files after the schema divergence, but not for Go packages.
      One-package-one-agent needs to be checked at dispatch, not discovered at
      build time.
    severity: MEDIUM
    status: self-resolved by the agents; no code lost

  - id: EX-23
    where: submodules/helix_llm/cmd/visiongen-boot/compose.vision.yml
    what: >-
      THE SAME DEFECT, A THIRD LAYER OUT. The model default was baked into the
      COMPOSE ARGV in shell-expansion form:
        - /models/${VISIONGEN_MODEL_GGUF:-Qwen2.5-VL-3B-Instruct-Q4_K_M.gguf}
      visiongen-boot refuses correctly, so through the binary the default never
      fires. Running the compose directly starts Qwen2.5-VL-3B on an unmeasured
      host with no error — the refusal path is unreachable, exactly as it was at
      the two layers already fixed.
      The full progression of one defect: (1) the Python service default, (2) the
      Containerfile ENV, (3) the compose argv. Each fix was correct and each left
      the next layer intact.
      NOTHING GUARDED THIS ONE: the Containerfile guard scopes itself to
      Containerfiles by name, and no Go test read a compose file at all.
      FIXED, with a guard that is deliberately NAME-AGNOSTIC — it forbids any
      weight-file literal anywhere in the configuration, so moving the default to
      a differently-named variable does not escape it.
    severity: HIGH
    status: fixed

  - id: OPEN-3
    where: submodules/helix_llm/cmd/agentgen-boot/
    what: >-
      AN ENTIRELY UN-MIGRATED LANE. agentgen-boot has NO modelchoice.go at all —
      it never received the measured-selection migration the vision, image and
      video lanes did. main.go:118 seeds AGENTGEN_MODEL_GGUF, and
      compose.agent.yml:44 bakes a REAL model default:
        ${AGENTGEN_MODEL_GGUF:-Mistral-Nemo-Instruct-2407-Q4_K_M.gguf}
      Same severity as the visiongen defect just fixed: running that compose
      starts a specific model on an unmeasured host, silently.
      NOT FIXED — this is a whole lane migration, not a one-line change, and the
      spec's capability families do not include an "agent" family, so it is
      genuinely outside feature 002's scope rather than an oversight within it.
      Needs an operator decision: migrate the lane, or fix only the compose
      default (which stops the silent start without giving the lane measured
      selection).
    severity: HIGH
    status: "closed - see OPEN-3-resolved (previously - open — operator decision required)"

  - id: EX-24
    where: submodules/helix_llm/deploy/compose.yaml + compose.dev.yaml
    what: >-
      Both carry ${HELIX_LLM_LOCAL_MODEL:-model.gguf}. Recorded for completeness,
      but LESSER in kind than the others: `model.gguf` is a placeholder, not a
      real model, so the fallback fails loudly at load ("no such file") rather
      than silently serving something nothing chose. A loud failure is a
      different defect class from a silent substitution, and conflating them
      would overstate this one.
    severity: LOW
    status: recorded

  - id: OPEN-4
    where: submodules/helix_llm/internal/runtime/streaming_launch.go
    what: >-
      `brokerAdmitter.Acquire` returns the concrete *vrambroker.Lease without a
      typed-nil guard. On a hypothetical (nil, nil) from the broker it would
      yield a NON-NIL Lease interface holding nothing, and the `lease == nil`
      check in runtime.Acquire would not catch it — the classic Go typed-nil
      trap. LATENT, not live: the broker never returns that shape today.
      Reported by the lease agent about the streaming agent's file rather than
      edited across scope boundaries.
    severity: LOW
    status: open

  - id: OPEN-5
    where: submodules/helix_llm/cmd/{visiongen,agentgen,videogen,imagegen}-boot
    what: >-
      All four boot lanes duplicate `needBytesFor` + `admit` + `classifyAdmit`,
      and three of them duplicate `modelchoice.go` near-verbatim (~380 lines).
      Two agents independently flagged the duplication; the third wrote the third
      copy and said so rather than quietly making it worse.
      `runtime.Acquire` + `AdmissionRefusal.Reason` is now the seam they can
      collapse onto — the reason values map 1:1 onto the lanes' existing exit
      codes 10-14. A follow-up, not this feature's work, but the seam exists now
      where it did not before.
    severity: MEDIUM
    status: open — follow-up

  - id: CRITICAL-2
    where: internal/runtime/choose.go:265 vs internal/catalogue/data/text.yaml:369
    what: >-
      THE STREAMING PATH ADMITS HOSTS THAT CANNOT RUN THE MODEL. Verified
      arithmetic, not inferred:
        catalogue glm-5.2 : memory_required_bytes = 17179869184 (16 GiB),
                            annotated memory_sourced: '"16 GB min"'
        default applied   : defaultResidentMemoryFraction = 0.25
        result            : a 4 GiB host is admitted for a model whose runtime
                            states a 16 GB minimum.
      For a `runtime: streaming` row, memory_required_bytes IS the runtime's
      stated floor, not an in-memory footprint to discount. Applying a resident
      fraction to it is subtracting from a number that was already the minimum —
      precisely the admission defect T073 exists to prevent.
      DEEPER, and the reason this is not a one-line fix: Choose step (a) reads
      the SAME field as the IN-MEMORY requirement, so a 20 GiB-free host would be
      told in-memory can serve a 744B model with a 372 GiB footprint. The data
      already records the correct answer — text.yaml:304 documents "two distinct
      artifact shapes per model", i.e. the GGUF row and the Colibri row should be
      SEPARATE catalogue entries with different figures, not one row read two
      ways.
      NOT FIXED, deliberately. The mechanism is already correct and configurable
      (ResidentMemoryFraction: 1 yields the full figure); only the default is
      wrong for real streaming rows. Enforcing it breaks 12 tests including two
      landed ones whose fixtures assume a discount. The real resolution spans
      catalogue + selection + runtime and is a design decision, not a constant.
      Live scope is narrow: exactly ONE active entry is served by streaming today.
    severity: CRITICAL
    status: open — design decision required

  - id: SELF-6
    where: my framing of the T070 decision to the operator
    what: >-
      I offered "Adopt it — add the dependency" as an option, implying a go.mod
      change. plan.md:175 ALREADY recorded that this is impossible: the streaming
      runtime is a C runtime with no Go binding, and "incorporation means a
      launch/lifecycle integration, not a library import". I had read that plan;
      I wrote it. The operator chose on my inaccurate framing.
      The agent re-investigated on receiving the grant, found the plan's own text,
      and built the only adoption that exists here — a real OS process and a real
      HTTP health probe, with binary path, argv and health URL left as
      configuration because the research records the repo, licence, build and
      roster but documents NO serving invocation, port or health endpoint.
      Inventing those would have been guessing.
      go.mod is unchanged, and that is the correct outcome rather than a
      shortfall. Still un-evidenced: no run against a real build, which needs an
      external clone plus a C toolchain.
    severity: MEDIUM
    status: recorded; the operator should know the decision was made on a wrong premise

  - id: EX-25
    where: submodules/helix_llm — offer ordering (OPEN-2 resolved)
    what: >-
      Resolved per operator decision: Go now matches Python, cheapest-admissible
      first, and the rule moved INTO internal/selection so the three lanes stop
      sorting. The three lane copies of prefer() were byte-identical — proven by
      extracting and diffing them — which is exactly why the divergence was
      invisible: they agreed with each other and all three disagreed with Python.
      THE FINDING THAT MATTERS: nothing pinned the old behaviour. No Go test
      asserted most-capable-first anywhere. The three prefer() functions were
      entirely unguarded, which is how a system-wide ordering rule could differ
      between two implementations of the same product without a single test
      failing.
      The gate's header now carries a ONE RECORDED EXCEPTION block naming this as
      the divergence resolved by changing Go, so nobody later "fixes" Python back.
      The user guide, which said "most capable … largest first", was corrected.
    severity: INFO
    status: fixed

  - id: SECURITY-1
    where: helix_code/config/config.yaml:28 + internal/config/config.go:711
    what: >-
      A SERVER STARTED WITHOUT HELIX_AUTH_JWT_SECRET SIGNS TOKENS WITH A
      PUBLICLY-KNOWN LITERAL. Confirmed by tracing the startup path, not inferred:
        - config.yaml ships  jwt_secret: "${HELIX_AUTH_JWT_SECRET}"
        - expandShellDefaults expands Redis host and DB host/user/dbname/server
          address. It does NOT expand auth, so the placeholder stays literal.
        - validateConfig (line 711) rejects only "" and
          "default-secret-change-in-production". The 24-character literal
          "${HELIX_AUTH_JWT_SECRET}" is neither, so it PASSES.
        - the len(JWTSecret) >= 32 checks that WOULD catch it live in
          ConfigurationValidator.Validate / ValidateField — and Load() never
          calls them. NewConfigurationValidator appears only in doc.go comments
          and its own definition. NO PRODUCTION CALLER.
      Result: anyone who clones the repo and starts the server without setting
      that variable — the default state — signs JWTs with a 24-byte string that
      is committed in this repository. Tokens are forgeable by anyone who reads
      the config file.
      NOT FIXED. Two candidate fixes with different blast radii: expand auth in
      expandShellDefaults (then an unset var yields "" and the existing check
      fires), or reject any secret still containing an unexpanded ${...}. The
      second is narrower and catches the whole class. Needs an operator decision
      because it will refuse startup for anyone currently relying on the
      accidental behaviour.
      NOTE ON MY OWN VERIFICATION: my first probe reported the placeholder as
      REJECTED. That was a false pass — I passed an empty Config{}, so
      validateConfig failed on an unrelated missing field. The same vacuity trap
      recorded as PATTERN-1, hit by me while checking for it.
    severity: CRITICAL (security)
    status: "closed - see SECURITY-1-helix_code and SECURITY-1-helix_llm (previously - open — operator decision required)"

  - id: CRITICAL-3
    where: helix_code/internal/config/config.go (RedisConfig.Database)
    what: >-
      EVERY DEPLOYMENT HAS BEEN CONNECTING TO REDIS DATABASE 0 REGARDLESS OF
      CONFIGURATION. The struct field was tagged mapstructure:"database" while
      setDefaultsOn writes v.SetDefault("redis.db", 0) and all 9 shipped configs
      spell it `db`. Zero files anywhere use `redis.database`. So neither the
      default nor any file ever populated the field, and internal/redis/redis.go
      passes it straight to redis.Options.DB. `redis.db: 3` was silently
      discarded.
      A TEST CERTIFIED THE BUG: helix_config_test.go:95 asserted
      Redis.Database == 0 — which passed trivially because the field was always
      its zero value. The guard confirmed the defect it existed to prevent.
      FIXED: tag corrected to "db", with a regression guard that loads db: 7 and
      asserts it arrives.
    severity: CRITICAL
    status: fixed

  - id: EX-26
    where: helix_code/config/ (whole-tree census)
    what: >-
      The strict-loader census found seven orphan groups across 10 config files,
      not the four keys I sent the agent to look at. Beyond redis.db above:
      the ENTIRE `notifications` block (~40 keys) is read by nothing — Config has
      no Notifications field, and the channel code builds everything from
      os.Getenv, hardcoding the channel name and username the config file
      appears to set. Its `rules:` list, mapping events to channels with
      priorities, is read by nothing at all.
      Also: config/production-config.yaml IS NOT VALID YAML — from line ~569 it
      contains a pasted AI-assistant transcript ("Now let me create the Phase 3
      test implementation:<function_calls>..."). Pointing HELIX_CONFIG at it fails
      today. Repair means deciding what it was meant to contain — an operator
      call, not a safe edit.
      The loader is now two-tier rather than all-or-nothing: unknown keys are a
      hard startup error naming the key and section; known-orphan keys warn from
      a hand-maintained register with a per-key reason. A pure refuse-everything
      loader would have rejected the repo's own config — an outage, not a fix.
    severity: HIGH
    status: fixed (strictness); contents flagged for decisions

  - id: SELF-7
    where: my Kokoro provenance note (EX-13)
    what: >-
      I recorded Kokoro's storage as 353,746,785 B and wrote that
      re-verification was INCONCLUSIVE because an HTTP HEAD returned a 15-byte
      error page. I concluded the network was restricted. It is not — HEAD is
      blocked, the model API is not. The video agent hit the working path and
      corrected my brief, which is how I learned my own note was wrong.
      Re-measured first-hand: graph 325,532,232 + 55 voice files 28,725,248 =
      354,257,480. The recorded figure UNDERSTATED by 510,695 B — about one voice
      file, so the earlier count likely saw 54 of 55. Understating is the
      dangerous direction: the entry is offered, and the fetch then runs out of
      room.
      Corrected, with the exact re-run command in the entry.
      LESSON: one failed probe of one shape is not "the network is restricted".
      I generalised from a single HEAD to a whole capability and wrote that
      generalisation into a data file as provenance, where it would have been
      believed.
    severity: MEDIUM
    status: fixed

  - id: CRITICAL-4
    where: submodules/helix_llm/services/videogen/ + internal/catalogue/data/video.yaml
    what: >-
      A THIRD instance of the ltx defect, and the root cause of all three: THE
      VIDEO SERVICE HAS NO GGUF LOAD PATH AT ALL — no from_single_file, no
      GGUFQuantizationConfig anywhere in it. Yet the Go side declares gguf-q4
      servable, and wan2.2-t2v-a14b records `quantisation: gguf-q4` with a
      source that PASSES the allowlist gate and points at the UNQUANTISED repo.
      That path was live. Boot could select it, and from_pretrained would have
      served the full unquantised model — at a memory cost its 8 GiB figure was
      never measured for, after downloading 117.53 GiB against a manifest
      claiming 18.
      THE SILENT SUBSTITUTION PROVEN, not inferred: real 13B GGUF-Q4 builds
      exist, but none carries model_index.json, which from_pretrained requires.
      Lightricks/LTX-Video DOES carry one — so from_pretrained SUCCEEDS, on a
      28x32x64 (~2B, 7.17 GiB) transformer instead of the real 13B's 48x32x128
      (24.29 GiB). Confirmed from the configs themselves.
      CONTAINED: gguf-q4 refused at the precision layer (covering both gguf
      entries without disabling the working fp8 default), ltx marked UNSUPPORTED
      with two concrete routes to establish support. Runtime-proven: refusal
      precedes any from_pretrained, so no weight is fetched.
      RESIDUAL, recorded not decided: wan2.2-t2v-a14b's source still passes the
      gate. Fully resolving it means recording a source that genuinely serves a
      GGUF-Q4 build, or withdrawing the source so boot refuses it as ltx now is.
    severity: CRITICAL
    status: contained — data decision outstanding

  - id: EX-27
    where: helix_llm/internal/gateway + helix_agent/internal/router
    what: >-
      Both hardcoded model lists removed per operator decision.
      GATEWAY: /v1/models now returns an explicitly empty list plus a stated
      reason, with Data as []api.Model{} rather than nil so the wire carries
      "data":[] (a listing that says none) instead of "data":null (which reads as
      a malformed body). /v1/models/:id returns 404 for every id with the reason
      in the message — that is how a client tells "this backend lacks that model"
      from "this server has no backend", and only the second is actionable. Both
      messages became i18n keys, closing a CONST-046 slip on that path.
      ROUTER: wired to the live source with a 3 s timeout applied TWICE (closure
      context AND adapter config, because the source is built on
      context.Background() so the closure's own deadline is load-bearing), a 30 s
      cache TTL chosen to sit inside the CONST-038 60 s status-accuracy window
      rather than picked for convenience, and a failure policy that DISCARDS the
      cached entry — serving a remembered list after the serving layer stopped
      answering would assert those models are running on evidence that just
      failed to confirm it.
      The agent also DROPPED a test it had written after realising it asserted
      only that context.Background() has no deadline — a tautology with no power
      over the code under test.
    severity: INFO
    status: fixed

  - id: CRITICAL-1-RESOLVED
    where: submodules/helix_llm/internal/testing/ + cmd/helixllm/ + Makefile
    what: >-
      THE HARNESS CAN NOW FAIL. Measured, before and after:
        BEFORE:   0 passed,  0 failed,  0 skipped   exit 0
        AFTER:   20 passed, 83 failed, 28 skipped   exit 1   (against a live server)
      131 declared entries, fully reconciled: 20+83+28 = 131.
      A FOURTH MECHANISM was found that I had not listed, and it was the one
      producing the headline 0/0/0: LoadBanksDir did not recurse, and
      challenges/banks/ contains ONLY subdirectories (12 of them, zero YAML at
      root). So the Makefile's own target loaded zero banks, and the three
      mechanisms I did identify were masked behind it.
      THE AUTHORITATIVE-SIDE DECISION, and its reasoning: the 28 BANK FILES were
      made authoritative and the DISPATCHER was changed. Not for keystroke count
      — the runner's action:"GET /path" + expected:<substring> shape is strictly
      LESS EXPRESSIVE than what the banks declare. It cannot represent a POST
      body, a header, a JSON field path, a status code, a concurrency level or a
      repeat count. Rewriting 28 files into it would have DESTROYED declared test
      intent: 78 request bodies, 294 assertions across 22 types, concurrent:100,
      repeat:100, retry blocks. Zero bank files were edited.
      A SECOND BLUFF FOUND EN ROUTE: with a server running, every challenge
      failed on x509 (self-signed cert), which would have made the whole suite
      transport-red and equally uninformative. Fixed by PINNING certs/cert.pem
      via a new --ca-cert flag rather than disabling verification — a
      non-verifying client would make every transport-level pass meaningless.
      All 7 Makefile invocations now pass it.
      A PRE-EXISTING TEST ASSERTED THE BUG: TestRunner_RunAll_UnsupportedAction
      required a step to be skipped AND the challenge reported passed. Reconciled
      to assert the load error, not deleted.
      HONEST GAP, loudly reported: 28 challenges (16 benchmark, 12 chaos) are
      skipped because this harness ships no load generator or fault injector.
      Each names its reason, is counted, and makes Verify exit non-zero.
      NOT FIXED, as instructed: the 83 real assertion failures. All are genuine
      failures against real responses — no transport errors. Dominated by
      http_status_ok (52), contains (11), status/one_of (14). Top failing banks:
      developer-coding-workflows 12, known-bug-regression 10,
      owasp-top10-security 9, llamacpp-only 8.
    severity: CRITICAL
    status: FIXED — 83 real failures now visible and awaiting their own work

  - id: SECURITY-2
    where: submodules/helix_llm/internal/discovery/trust.go:212 + discover.go:193,459
    what: >-
      PROOF RELAY — A HOST HOLDING NO SECRET AT ALL BECOMES TRUSTED AND RECEIVES
      THE PROMPT AND CREDENTIALS. Reproduced on the wire by the security review,
      in two variants.
      ROOT CAUSE: the proof is HMAC(secret, nonce) with NO CHANNEL BINDING —
      nothing ties it to the endpoint we dialed. Any host that can reach one
      genuine instance answers our challenge by forwarding it.
        - REDIRECT: hostile /attest answers 302 to the honest instance. New()
          builds &http.Client{Timeout} with NO CheckRedirect, so the honest
          instance's proof returns as the hostile endpoint's response.
          trusted=true, then Send posted the prompt to the hostile endpoint.
        - ACTIVE PROXY: hostile fetches the honest attest itself, rewrites `host`
          and `models`, returns it. trusted=true, ServedOptions carried an
          attacker-chosen model under a host label colliding with the real one,
          and Send delivered prompt AND credentials.
      WHY THE EXISTING TEST PASSED: its hostile server returns a GARBAGE proof
      rather than relaying a real one. The test proves an unauthenticated host is
      refused; it never tested a host that obtains a valid proof by other means.
      So FR-024/FR-025 hold only against an attacker alone on the network.
      FIX DIRECTION (from the review, not yet applied): bind the dialed endpoint
      into the MAC input — HMAC(secret, nonce || endpoint) — and set CheckRedirect
      to refuse on both probe and Send.
    severity: CRITICAL (security)
    status: "closed - see SECURITY-2-3-4-resolved (previously - open — reproduced, fix dispatched)"

  - id: SECURITY-3
    where: submodules/helix_llm/internal/discovery/discover.go:305
    what: >-
      LocalHost mode never verifies that an endpoint IS loopback, yet the mode is
      exempt from FR-024 by construction. Reproduced: LocalHostEndpoints set to a
      LAN address, no secret, attest with no proof field -> trusted=true, and Send
      delivered the prompt. The "beyond the current host" boundary is enforced by
      configuration trust rather than by code, so a hostile .env or a hostname
      that resolves off-box exports user content unauthenticated.
    severity: HIGH (security)
    status: "closed - see SECURITY-2-3-4-resolved (previously - open — fix dispatched)"

  - id: SECURITY-4
    where: submodules/helix_llm/internal/selection/placement.go:426,633 + fit.go:112
    what: >-
      The fleet ledger accounts SYSTEM memory but never ACCELERATOR memory.
      Reproduced on the SingleAccelerator fixture (23 GiB VRAM): three placements
      of a RequiresAccelerator model each needing 90% of VRAM were ALL GRANTED,
      and reported accelerator memory stayed 23 GiB. The same VRAM was promised
      three times.
      NOT a race — the locking is correct and -race is clean. The axis is simply
      absent from the ledger, which is the same class as the over-commitment
      defect the placement work was built to prevent, on the axis it did not cover.
    severity: HIGH
    status: "closed - see SECURITY-2-3-4-resolved (previously - open — fix dispatched)"

  - id: SELF-8
    where: my commit 064dd75
    what: >-
      I RAN `git add -A -- internal/ cmd/ ...` WHILE AN AGENT WAS STILL
      VERIFYING, and absorbed its seven harness files into a 46-file commit
      alongside ~39 unrelated ones. The operator had asked for the harness fix as
      its OWN commit. It is now entangled with a config/gateway/video batch.
      This is the §11.4.84 working-tree-quiescence hazard — the one I had
      described in three separate reports and warned other agents about, then
      caused myself. I checked quiescence by counting RUNNING AGENTS and read
      "one left" as safe, when the correct check is whether any agent is still
      writing to the paths I am about to stage.
      Verified rather than assumed: no mutation residue reached the commit
      (git grep over 064dd75 for the marker set is empty — the only hits repo-wide
      are governance docs QUOTING the marker list as prose), and nothing was
      pushed (HEAD 064dd75, upstream 035b134, four local-only commits).
      Recoverable with `git reset --soft` and re-staging, but that rewrites three
      other agents' commits, so it needs an operator decision rather than my
      judgement.
    severity: MEDIUM
    status: recorded — recovery is an operator decision

  - id: CRITICAL-5
    where: internal/selection/fit.go:320,373 · internal/runtime/choose.go:334 · placement.go:426
    what: >-
      SELECTION NEVER READS ACCELERATOR MEMORY. Every accelerator-required model
      is fitted against SYSTEM RAM. Verified by grep: `Accelerator.MemoryAvailable`
      and `.MemoryTotal` are referenced ZERO times across internal/selection,
      internal/runtime and both boot binaries; the same files reference the
      HOST's MemoryAvailable six times. `supports()` checks only
      len(Accelerators)==0 — presence, never capacity.
      THE FIGURES BEING COMPARED ARE NOT THE SAME QUANTITY. The catalogue's
      memory_required_bytes for accelerator entries is a VRAM figure —
      vision_image.yaml records 4338987008 annotated "4138 MiB actual GPU peak
      for the VLM alone, co-resident with a live 30B coder on one RTX 5090". That
      VRAM number is compared against host RAM.
      REPRODUCED: a host with 48 GiB RAM free and a card with 7 GiB free was
      OFFERED an entry requiring 24 GiB of VRAM.
      THE LIVE CORROBORATION IS THE WORST PART: imagegen-boot on THIS host
      withheld qwen-image:fp8 as "memory short by 1918MiB (needs 15258MiB,
      13339MiB available after 4646MiB held back)". 13339 MiB is RAM-minus-
      reserve. The card has 11775 MiB. The refusal was correct BY COINCIDENCE OF
      RAM SIZE, for the wrong axis. On a 64 GiB-RAM / 12 GiB-card host the same
      model would print PLAN-OK and then fail at the broker, misattributed to
      coder co-residency.
      THREE TESTS CERTIFY THE BUG:
        - selection_test.go TestEveryOfferedOptionFitsTheMeasuredHost re-derives
          "fits" from host.MemoryAvailable — the oracle inherits the
          implementation's exact blind spot.
        - refusal_test.go:313 asserts a 24 GiB accelerator model "runs perfectly
          well" on a fixture card with 23.2 GiB free.
        - choose_test.go manufactures "does not fit" by shrinking RAM to 12 GiB
          while the card still has 23 GiB — certifying that the streaming
          fallback is decided from RAM for an accelerator-required entry.
      FOUND INDEPENDENTLY TWICE: the US1 reviewer via the fit path, the security
      reviewer via the fleet ledger (SECURITY-4, same VRAM promised three times).
      Two symptoms, one root cause.
      This is the central premise of the feature — measure the host, offer what
      fits — failing on the axis that matters for every accelerator model.
    severity: CRITICAL
    status: "closed - see CRITICAL-5-6-resolved (previously - open — fix dispatched)"

  - id: CRITICAL-6
    where: internal/vrambroker/budget.go:20,43 + both boot compose files
    what: >-
      ADMISSION BINDS THE ACCELERATOR BY ENUMERATION POSITION. nvidiaSMIArgs
      carries no --id=<uuid>, and parseSMICSV is documented as parsing "the FIRST
      GPU row" and returns on the first line. selection.Option carries no device
      identity at all; needBytesFor admits a bare byte count. The compose files
      pass nvidia.com/gpu=all with no CUDA_VISIBLE_DEVICES pin, and the service
      calls torch.cuda.get_device_name(0).
      REPRODUCED: two nvidia-smi rows (24576, 12288) gave budget total 24576;
      reversed, 12288. The budget depends on enumeration order.
      THE IRONY WORTH RECORDING: DeviceIdentity was made a distinct type
      specifically so an index could not be assigned to it — §11.4.111 enforced
      by the compiler. That worked. And then nothing downstream represents the
      identity at all, so the guarantee stops at the package boundary. CUDA and
      nvidia-smi order differ by default, so on a two-card host admission and
      execution can target different cards.
    severity: CRITICAL
    status: "closed - see CRITICAL-5-6-resolved (previously - open — fix dispatched)"

  - id: EX-28
    where: cmd/imagegen-boot/main.go:102
    what: >-
      The storage axis is measured on the WORKING DIRECTORY, not the filesystem
      that will hold the weights: decide(ctx, family, "", ...) passes an empty
      WeightsDir, so StoragePathForWeights falls back to os.Getwd(). Weights
      actually land in a podman named volume under the container graphroot. The
      vision lane passes weightsDir correctly; imagegen does not.
      REPRODUCED: plan from /dev/shm reports storage_available=15484MiB; from the
      repo directory, 1189382MiB. Same host, same volume, 77x different answer.
      The named axis is right and the number is for the wrong filesystem.
    severity: HIGH
    status: open — fix dispatched

  - id: SELF-9
    title: my ordering change broke the videogen lane; two precision lists disagreed
    found_by: T084 Fable review, confirmed by running the lane
    detail: |
      The cheapest-first reorder I dispatched (user round-2 decision "make Go
      match Python") exposed a pre-existing disagreement. Two independent lists
      answer the same question:
        cmd/videogen-boot/modelchoice.go  servingPrecisions      said gguf-q4 servable
        services/videogen/videogen_server.py _UNIMPLEMENTED_PRECISIONS refuses gguf-q4
      Nothing noticed while most-capable-first happened to rank a servable fp8
      build first. Cheapest-first put wan2.2-t2v-a14b:gguf-q4-480p (8 GiB) ahead
      of the servable wan2.2-ti2v-5b:fp8-480p (10 GiB).
      REPRODUCED: `go run ./cmd/videogen-boot plan` chose the gguf-q4 build. The
      lane would admit the VRAM, compose up, 503 for the full health timeout,
      exit 4 — holding the lease throughout. A correct-looking plan to a dead
      container.
      FIXED: removed gguf-q4 from servingPrecisions (the service is authoritative
      — it is the thing that loads weights). Lane now chooses fp8-480p again.
      GUARDED: internal/serviceconfig/precision_agreement_test.go parses both
      files and fails if the sets intersect. The reviewer noted the existing
      test_manifest_catalogue_agreement.py reads servingBackends but never
      servingPrecisions — that is the hole this closes.
    severity: HIGH
    status: fixed + guarded
    lesson: |
      PATTERN-1 again, in a new shape. Two sources of truth for one question,
      and the disagreement was invisible because an unrelated ordering property
      masked it. Changing the ordering did not cause the defect; it removed the
      accident that hid it.

  - id: CRITICAL-7
    title: the production HTTP path never resolves the published model identifier
    found_by: T055 Fable review (US2 naming and export)
    detail: |
      REPORTED WITH RUNTIME PROOF by the reviewer (throwaway test, since deleted):
      cmd/helixllm/main.go:461 wires gateway.RouterOptions{Brain: fallbackChain}.
      ResolveModelName is called ONLY from Brain.Complete / CompleteStream
      (internal/brain/brain.go:191,273) — neither is on the request path.
      /v1/chat/completions, /v1/completions, /v1/messages and /ws all reach
      fallback.Chain, which OVERWRITES req.Model with entry.ModelID — set by
      discoverProviderModels to each provider's FIRST model.
      Reviewer's captured runs:
        client asked helixllm-gpu-01-qwen2-5-7b-0123456789ab -> answered by chutes,
          cloud saw model=deepseek-chat, local saw nothing
        local-only, client asked qwen2.5:7b (2nd served model) -> local got llama3:8b
      So the toolkit --apply path writes a derived id that production answers with
      whichever provider scores highest. SC-019 is unmet. The whole
      ResolveModelName guard and its two tests exercise a path production does
      not use; no gateway/cmd test sends an identifier over HTTP.
    severity: CRITICAL
    status: "closed - see CRITICAL-7-resolved (previously - open — not yet fixed)"
    note: |
      NOT independently reproduced by me yet. Recorded as REPORTED with the
      reviewer's cited file:line and captured runs, per 11.4.6 — the probe test
      was deleted, so I owe my own reproduction before the fix.

  - id: OPEN-14
    title: four fix agents died on the session rate limit
    detail: |
      SECURITY-1 (JWT placeholders), SECURITY-2/3/4 (discovery hardening),
      CRITICAL-2 (catalogue row split), CRITICAL-5/6 (accelerator memory axis)
      and the 83-failure triage all terminated with HTTP 429 "session limit,
      resets 10:20pm Europe/Belgrade" — not with findings.
      Their partial last words are in the task outputs; none of them committed.
      Working tree is clean of their work (they were dispatched, not landed).
    severity: MEDIUM
    status: "closed - all four were redispatched after the rate limit reset and completed (previously - open — redispatch after the limit resets)"

  - id: SECURITY-1-helix_code
    title: unexpanded ${...} in a credential was used verbatim as the credential
    detail: |
      REPRODUCED then FIXED in helix_code/internal/config (distinct from the
      helix_llm instance, which is dispatched separately).
      A dead agent had left a complete RED suite and no implementation. All 11
      credential fields accepted an unexpanded placeholder, plus the
      ${VAR:default}, ${VAR:-default} and embedded prefix-${VAR}-suffix forms.
      Worst case is auth.jwt_secret: with HELIX_AUTH_JWT_SECRET unset, every JWT
      was signed with the literal string "${HELIX_AUTH_JWT_SECRET}" — a constant
      committed to this repository, readable by anyone — while startup, health
      checks and tests all reported success.
      FIXED: checkSecretPlaceholders runs first in validateConfig and refuses,
      naming both the field and the variable to export.
      SCOPE is deliberately narrow (an explicit credential list, not a
      reflective walk): non-secret fields legitimately carry ${...} in normal
      operation — config/config.yaml ships ${HELIX_LLM_ENDPOINT:...} and
      ${HELIX_QA_HOME}/banks — so a blanket scan would refuse the project's own
      shipped config. The test pins this via NonSecretFieldsAreNotScanned.
      A bare $VAR is NOT refused: it is not a form this loader expands, so it is
      a real secret, and refusing it would lock a user out of their own config.
      EVIDENCE: 25 assertions across 5 tests; paired §1.1 mutation proven by
      diff (regex neutered -> FAIL; restored -> PASS).
    severity: CRITICAL
    status: fixed + guarded

  - id: SELF-10
    title: my own fix panicked on a nil optional section; a sibling gate caught it
    detail: |
      Config.Verifier is a POINTER and nil whenever the optional verifier block
      is absent. secretFields read through it unconditionally, so validateConfig
      SEGFAULTED on any minimal config — turning a missing optional section into
      a startup crash, a worse failure than the one I was fixing.
      My own SECURITY-1 tests did NOT catch it: their fixture populates Verifier.
      TestBcryptCostValidationGuard caught it, because it builds a minimal config.
      FIXED: nil guard, with the reason recorded at the call site.
      LESSON: 11.4.120 says investigate before touching a gate your fix broke.
      Investigation said the gate was right and my fix was wrong. Had I assumed
      "stale gate" and reconciled it, I would have shipped a segfault.
    severity: HIGH
    status: fixed

  - id: OPEN-15
    title: shipped config carries unexpanded ${...} in NON-secret fields at runtime
    detail: |
      Noticed while scoping SECURITY-1, NOT fixed — out of scope and pre-existing.
      config/config.yaml sets llm provider endpoint to
      "${HELIX_LLM_ENDPOINT:http://localhost:8081}", and replica-8081.yaml:178
      records that expansion covers EXACTLY five fields, which do not include
      provider endpoints. So that value is used as a literal URL — a URL that is
      not a URL. It does not reach my check by design (see the scope note above).
      Worth its own decision: either expand those fields too, or stop writing a
      shell-expansion form into a field nothing expands.
    severity: MEDIUM
    status: "closed - see OPEN-15-CORRECTED (previously - open — reported, not fixed)"

  - id: OPEN-16
    title: helix_code desktop GUI cannot build in this environment
    detail: |
      `go build ./...` fails only on github.com/go-gl/glfw: X11/Xlib.h missing.
      Pre-existing, environmental (absent X11 dev headers), unrelated to any
      change in this feature. Every non-GUI package builds. Recorded so a later
      run does not mistake it for a regression.
    severity: LOW
    status: open — environmental

  - id: NOTE-2
    title: the two SECURITY-1 fixes chose OPPOSITE scopes, and both are correct
    detail: |
      Recorded because a future reader will otherwise read this as an
      inconsistency and "fix" one of them.
      helix_llm  — scans EVERY string field reflectively.
      helix_code — scans an explicit list of credential fields ONLY.
      The difference is a property of each codebase, not a disagreement:
        * In helix_llm NOTHING expands anything. digital.vasic.config/pkg/env is
          a plain os.Getenv and loadFromFile is a plain json.Unmarshal, so a
          value arrives substituted or it does not. Any surviving ${...} is
          therefore a bug in any field, and a broad scan has no false positives
          to speak of.
        * In helix_code expandShellDefaults DOES expand, but for EXACTLY five
          fields, and the shipped config deliberately carries ${...} in
          non-secret fields it never expands (see OPEN-15). A broad scan there
          would refuse the project's own config on startup.
      Each codebase got the scan its expansion semantics support.
    severity: INFO
    status: recorded

  - id: SECURITY-1-helix_llm
    title: hot-reload path never validated at all, so a reload could install a placeholder secret
    detail: |
      Fixed by subagent (helix_llm 5b68ffa). Beyond the reported defect it found
      that loadFromFile in the ConfigWatcher hot-reload path never called
      Validate() — so even with a correct startup check, a live config reload
      could install "${HELIXLLM_JWT_SECRET}" as the signing key.
      Reproduced on BOTH entry points with captured output, both configs valid
      in every other respect so nothing could short-circuit the check.
      Three paired mutations, each diff-verified as applied, including one that
      makes the guard reject unconditionally — which fails every acceptance
      test, proving the suite cannot be satisfied by refusing everything.
      Pre-existing unrelated failures were PROVED unrelated via
      `go list -deps -test` showing zero dependency edges, not merely asserted.
      LEFT OPEN by the agent, correctly: a rejected placeholder is not the same
      as a MISSING secret. HELIX_AUTH_JWT_SECRET="" still loads. That is an
      adjacent policy nobody has decided, so it invented nothing.
    severity: CRITICAL
    status: fixed + guarded

  - id: CRITICAL-7-resolved
    title: CONFIRMED then fixed — published identifiers were decorative
    detail: |
      The T055 review's report held in every particular, including both captured
      runs. Independently reproduced at the HTTP layer against the real wiring
      main.go builds, with fake providers recording what they were asked for:
        client asked helixllm-gpu-01-qwen2-5-7b-60ef9191efd0
          -> answered by "chutes", which was asked for [deepseek-chat]
          -> the serving provider received []
        client asked qwen2.5:7b -> provider received [llama3:8b]
      Cause: ResolveModelName was reachable only from Brain.Complete/CompleteStream;
      main.go wired the Completer to fallback.Chain, which overwrites req.Model
      with each provider's FIRST model.
      FIXED: Brain.PinModel resolves an identifier (or raw served name) to the
      provider that serves it, honouring the identity's HOST so one machine's
      identifier cannot land on another machine's copy of the same model name.
      fallback.Chain consults the pinner BEFORE the entry loop — resolving after
      would resolve something already discarded.
      Unavailable named host: explicit error, never substitution. The
      fall-through fires exactly when the named thing is gone, which is the worst
      moment to silently answer with something else.
      3 mutations, each diff-verified. One is notable: dropping the pin from
      Complete only left STREAMING passing — proving the two call sites are
      independently guarded rather than sharing one accidental path.
      BEHAVIOUR CHANGE worth knowing: a request naming a model a registered
      provider serves is now pinned, so `gpt-4o` no longer cross-falls-back to
      another provider on failure. Intended (a named model is a choice, not a
      hint) but it affects named-model requests generally, not only identifiers.
    severity: CRITICAL
    status: fixed + guarded

  - id: CRITICAL-5-6-resolved
    title: CONFIRMED then fixed — selection never read VRAM; the broker bound to GPU row 0
    detail: |
      CRITICAL-5: supports() checked only len(p.Accelerators)==0 — EXISTENCE, not
      capacity — and memoryAxis compared the requirement against host RAM.
      Strongest evidence: ResourceAccelerator was DECLARED in option.go and
      matched nowhere else in the repo. The axis was designed and never wired.
      This matters because video.yaml records its memory figure as "the value
      actually passed to vrambroker.Acquire" — for accelerator-required entries
      that number IS the VRAM requirement, and it was being checked against RAM.
      CRITICAL-6: budget.go parsed "the FIRST GPU row" — its own words.
      Reproduced on a two-GPU fixture with the small card first: reported
      free=1192 MiB and refused a 6 GiB request while the host's other card had
      12689 MiB free. A test asserted `// Multi-GPU output: first row is used` —
      a test blessing the defect (11.4.120).
      FIXED: device axis picks the card with most free memory, ties broken by
      stable Identity and never by position; the broker now queries
      uuid,pci.bus_id, parses every row, and resolves by stable identity with
      explicit ErrDeviceAmbiguous / ErrDeviceNotFound.
      HONEST MEASUREMENT: on the dev host (RTX 3060, 11781 MiB free) NOTHING
      changes today — every entry clearing RAM also clears this card. Stated
      rather than overclaimed. On the common 6 GiB laptop shape, 14 of 15
      accelerator-required entries are now correctly withheld naming the device.
    severity: CRITICAL
    status: fixed + guarded

  - id: OPEN-17
    title: selection uses zero accelerator headroom; the broker keeps 2 GiB
    detail: |
      Left open deliberately by the CRITICAL-5/6 agent, and correctly so.
      Selection asks "does it fit as measured"; vrambroker reserves 2 GiB. So a
      10 GiB model on an 11.5 GiB card is OFFERED by selection and then REFUSED
      by the broker — the user is shown something that cannot start.
      A seam exists (Reserve.AcceleratorHeadroomBytes) but nothing sets it.
      Importing the broker's constant into pure selection is a design decision
      the agent declined to make unilaterally. Needs a decision.
    severity: HIGH
    status: "closed - see OPEN-17-18-resolved (previously - open — needs a decision)"

  - id: OPEN-18
    title: Fleet.Available draws down host RAM only, never device memory
    detail: |
      placement.go: Commitment has no accelerator dimension, so concurrent
      placements do not reduce available VRAM. Two placements can each be told
      the same card is free. Found while fixing CRITICAL-5/6; out of its scope.
    severity: MEDIUM
    status: "closed - see OPEN-17-18-resolved (previously - open)"

  - id: OPEN-19
    title: two integration model-listing tests fail at baseline
    detail: |
      TestIntegration_OpenAI_ListModels and TestLlamaCppOnly_ModelsEndpoint fail
      with "0 is not greater than 0: should return at least one model".
      THREE independent agents each proved these pre-date their work — one by
      path-limited stashing and re-running at baseline, one via `go list -deps
      -test` showing zero dependency edges. Believed to need live services, but
      that is the assumption, not a finding: they sit in the same feature area as
      the identifier fix and nobody has actually diagnosed them.
    severity: MEDIUM
    status: "closed - see OPEN-19-resolved (previously - open — needs diagnosis, not assumption)"

  - id: SECURITY-2-3-4-resolved
    title: attestation proof was not bound to who answered; now channel-bound and address-pinned
    detail: |
      Two rounds, both reproduced before fixing.
      ROUND 1 (3efc367). Proof was HMAC(secret, nonce) — it answered "do you hold
      the secret" and said NOTHING about who answered. Any host able to reach a
      genuine instance could obtain a real answer to OUR OWN nonce and present it
      as its own, by redirecting us to one or by proxying our challenge to one.
      Both reproduced with the user's prompt, an opened file's contents, and the
      upstream credential arriving at a hostile host.
      FIX: HMAC(secret, nonce || 0x00 || audience) where the audience is the
      SOCKET ADDRESS and is NEVER TRANSMITTED — the responder signs its own
      accepted-connection LocalAddr, the client checks its own RemoteAddr. Two
      ends of one socket, so an honest direct instance agrees by construction.
      That is what defeats the proxy specifically: it may forward every byte we
      send and still cannot make an honest instance sign an address that is not
      its own. A Host-header-derived audience would NOT have covered a
      transparent byte-forwarding TCP relay; this does.
      Redirects are refused outright during attestation — the party answering the
      challenge must be the party we send content to.
      ROUND 2 (6e278e8). The binding was established on the PROBE's connection;
      Send opened a new one and re-resolved the name, so a name pointing at the
      attested instance during the probe and elsewhere at send time was not
      caught. Reproduced: all three canaries at a host that never held the secret.
      FIX: Instance.VerifiedAddr carries the verified peer forward (following
      PeerLoopback's precedent) and Send dials it through a client copy with a
      fixed DialContext, so the URL keeps its scheme/host/path and the Host
      header and TLS SNI still use the configured name.
      Two silent bypasses of a fixed dialler are REFUSED rather than ignored: a
      transport with DialTLSContext (never calls DialContext, so https would
      re-resolve) and a proxy that actually applies (the proxy picks the peer).
      An Instance with no verified address is refused BEFORE the body is
      marshalled — a silent fallback to re-resolution IS the hole, and it would
      look like it worked.
      ALSO FIXED en route: the local-host attestation exemption was granted on
      which CONFIG LIST an endpoint appeared in, not on whether the address was
      really loopback. A LAN address in LocalHostEndpoints turned the requirement
      off for that host. Now decided from the actually-connected peer, with a
      pre-dial filter so a host we could never authenticate does not even learn
      we are looking.
    severity: CRITICAL
    status: fixed + guarded

  - id: OPEN-20
    title: pinning is per-Instance, so a re-addressed instance needs a fresh Discover
    detail: |
      Behaviour change from the Send pinning fix, flagged by the agent rather
      than buried: an instance that legitimately changes address (DHCP lease,
      container restart) will not be reached by Send until it is discovered
      again. That is the correct trade — the alternative is re-resolution, which
      is the hole — but operators should know.
      Also noted: Send has no redirect policy of its own the way attestation
      does. Not a hole (the body already reached the pinned peer, and the
      redirect is dialled through the same pinned transport), but asymmetric.
    severity: LOW
    status: recorded — behaviour change, not a defect

  - id: FR-017-018-resolved
    title: the HelixCode and OpenCode exporters had no caller at all
    detail: |
      CONFIRMED. ExportHelixCode, ExportOpenCode and the Merge* functions were
      defined, unit-tested, and referenced by NOTHING outside internal/naming.
      The only non-Go references were two snippets in docs/guides that tell a
      user to import internal/naming — not importable from outside the module.
      So the artifacts were genuinely unobtainable by any user, and the
      requirement that users can obtain their configuration on demand was met
      only for the Claude Toolkit.
      GIT HISTORY CHECKED FIRST per 11.4.124: `git log -S` across all history
      shows they arrived in 1b66afb WITH their unit tests and no caller was ever
      added. Never-completed wiring, not wiring that rotted — so completing it
      was right, and removing it would have been wrong.
      FIXED: served from the same gateway the toolkit already reads models from
      — GET /v1/config/{helixcode,opencode} and POST /v1/config/{consumer}/merge
      (body in, merged content back, never writes to disk). The base URL handed
      out is the address the request arrived on, upgraded on X-Forwarded-Proto
      and never downgraded. Multiple serving hosts produce a 400 naming them
      rather than a silent pick.
      The test drives the real gin router end to end — testing the exporter
      function in isolation is what proved nothing about reachability.
    severity: HIGH
    status: fixed + guarded

  - id: SC-015-resolved
    title: the published identity named 127.0.0.1, so it named no machine and collided across machines
    detail: |
      CONFIRMED, with one sub-claim REFUTED.
      ServingHost() returned the base URL's hostname, and the embedded
      llama-server path forces 127.0.0.1 — the normal case. So the identity was
      helixllm/127.0.0.1/<model>: it names no machine, and it is IDENTICAL on
      every machine. The collapse is real and was verified in the toolkit
      source: claude-providers.sh does `group_by(.provider_id) | map(.[0])`
      over MULTIPLE enumerated hosts, so the second host is silently dropped.
      Captured RED on the pre-fix tree:
        ServingHost() for http://127.0.0.1:8080 = "127.0.0.1", want "anton"
        127.0.0.1 derives helixllm-127-0-0-1-llama3-41cc1e4fdbc7
        localhost derives helixllm-localhost-llama3-9235d4f540fa
      REFUTED sub-claim: "OpenAI-compatible local backends get no identity and
      are silently dropped." The filter is real but DELIBERATE — in this codebase
      the OpenAI-compat path serves only remote vendors, and stamping identities
      on proxied vendor models is what the design explicitly forbids. Left alone.
      FIXED: loopback and wildcard hosts resolve to os.Hostname(); a base URL
      already naming a real machine is untouched; a machine that cannot say what
      it is called yields the URL host rather than a fabricated name.
      COST STATED PLAINLY: every identifier for a locally served model changes
      ONCE. Judged worth paying because those names were wrong (they pointed at
      no machine and collided), it is the LAST re-mint of this class (both
      loopback spellings now converge, where flipping the env var used to re-mint
      silently and repeatedly), and the naming SCHEME is untouched — the golden
      file pinning it needed no change.
    severity: HIGH
    status: fixed + guarded

  - id: CHALLENGES-triage
    title: all 83 challenge failures classified; 5 fixed; none converted to skips
    detail: |
      Measured 20 passed / 83 failed / 28 skipped — the remembered 83 is right.
      The 28 skips are honest (benchmark/chaos steps with no executor, already
      surfaced as a non-zero exit).
      ALL 83 accounted for in 8 root-cause groups, none left unclassified:
        G1  51  no LLM backend reachable          environmental
        G2a 11  no semantic request validation    REAL DEFECT
        G2b  3  auth expects 401, server open     config
        G3   5  provider outage reported 500      REAL DEFECT — FIXED
        G4   4  /v1/models returns data: null     REAL DEFECT
        G5   6  /health 503, no provider          environmental
        G6   1  p99 latency budget                environmental / test
        G7   1  models/:id 404 (downstream of G4) environmental
        G8   1  RAG ingest shape                  UNDETERMINED, honestly
      The environmental class was PROVEN, not assumed: nothing listening on
      :50052, llama-server present but zero .gguf files anywhere under $HOME.
      NONE were converted to skips — that would have rebuilt the always-passing
      harness defect this session just removed.
      The previous agent's dying lead ("every malformed and unauthenticated
      request reaches provider dispatch") was CONFIRMED for malformed and
      REFUTED for auth: open-access is documented as by-design, so those 3
      challenges assume a keyed deployment they never declare.
      FIXED G3 only — the highest-impact real defect with a spec the project's
      own banks already declare ([404,503]). Suite 83 -> 79.
      One apparent NEW failure (500_concurrent_model_list) was TESTED rather
      than assumed: across three runs the UNFIXED binary fails it more often and
      more severely. A host-load-sensitive flake that was passing by luck.
    severity: HIGH
    status: triaged; 1 of 3 real-defect groups fixed
    next: G4 (smallest), then decide the G2a validation policy once for both handlers

  - id: HXA-netaddr-1
    title: an unpaired bracket in a configured host silently becomes a connection to ::1
    detail: |
      Found while accounting for uncommitted work in helix_agent. The failure is
      on COMMITTED code — proven not to be anyone's in-flight work: netaddr is
      unmodified in the tree, and `go list -deps -test` shows zero dependency
      edges to the packages that do have uncommitted changes.
      The test asserts url.Parse REFUSES the URL built from host "[[::1]".
      Probed the stdlib directly on go1.26.0:
        "http://[[::1]:6379"  PARSED  hostname="::1" port="6379"
        "http://[[::1]]:6379" REFUSED
      So Go SILENTLY DISCARDS the stray leading bracket and yields a real,
      connectable destination.
      The test's stated danger — "a mutant that makes it parse invents a
      destination" — is exactly right. Its assumption that url.Parse would catch
      it is wrong. Nothing catches it: an operator who mistypes a bracket in a
      configured host gets a silent connection to ::1 instead of an error.
      A gate that is RIGHT about the danger and WRONG about the mechanism.
    severity: HIGH
    status: "closed - see HXA-netaddr-1-resolved (previously - open — dispatched)"

  - id: SELF-11
    title: I nearly misread my own filtered output as a finding
    detail: |
      I backgrounded `go test ./internal/... | grep -E '^(FAIL|--- FAIL)'` and
      later counted '^ok' in the log. It reported "0 packages ok", which reads
      like a catastrophe. There were no ok lines because MY OWN FILTER had
      removed them.
      PATTERN-1 a fourth time, and this time against me rather than a tool:
      absence of output is ambiguous between "clean" and "you did not capture
      it". The actual result was one failing package.
    severity: LOW
    status: recorded — lesson

  - id: OPEN-17-18-resolved
    title: selection and the broker now agree on headroom; commitments charge the card they were weighed against
    detail: |
      Both reproduced with captured output before fixing.
      GAP 1: card free=11776 MiB, model needs=10240 MiB, broker headroom=2048.
        SELECTION offers: true.  BROKER admits: false.
        A user was shown a model, chose it, and met the refusal afterwards.
      GAP 2: two 16 GiB models onto one 24 GiB card — after placement 1 the
        fleet still reported 24576 MiB free, and placement 2 succeeded.
        32 GiB committed to a 24 GiB card.
      WHERE THE HEADROOM IS STATED: internal/runtime, in SelectionReserve(),
      which READS vrambroker.HeadroomBytes rather than copying it. Pure
      selection keeps no runtime import. internal/runtime/lease.go was already
      the right seam — its own doc comment describes this exact defect ("a lane
      could offer a model the broker would then refuse, and the user met that
      refusal after choosing") and it already imports both packages.
      The composition roots are PLURAL (videogen/imagegen/visiongen-boot), so
      stating it in one would leave the others disagreeing with the gate; they
      state it by calling, not by repeating.
      THE ANTI-DRIFT TEST IS NOT A HARDCODED 2 GiB — which was the trap. It
      drives BOTH real deciders (the broker's own arithmetic, and selection
      under the reserve the binaries pass) across a sweep whose points are all
      derived from HeadroomBytes, requiring agreement at every one. Change the
      constant and the fixtures move with it; leave selection uninformed and the
      answers diverge inside the band.
      DEVICE IDENTITY: fits() records the stable DeviceIdentity the offer was
      weighed against; commit() reads it off the offer rather than re-deriving,
      so the card judged able to hold the model is the card charged, and Release
      credits the same one. Per-device rather than host-wide also buys the
      honest positive: a two-card host still takes a second model the first card
      could no longer hold.
      4 paired mutations, each diff-verified — including one that charges
      Accelerators[0] instead of the recorded identity, which fails the
      order-invariance test. That is the 11.4.111 defect reintroduced and caught.
    severity: HIGH
    status: fixed + guarded

  - id: NOTE-3
    title: how a flake was ruled out properly, worth copying
    detail: |
      internal/lifecycle/TestEvict_NeverUnloadsUnderAnInFlightRequest_Concurrent
      failed once in ~6 full-suite runs during the headroom work.
      The agent did NOT assume it was unrelated — it noted that lifecycle DOES
      import vrambroker, which is exactly the dependency edge that would make it
      its own fault. It then: reproduced the failure deterministically under
      GOMAXPROCS=1; read the failing assertion (the test's own LIVENESS
      precondition, serving-refusals=0 — "the contention window never opened" —
      while the actual FR-047 correctness invariant held at 0 violations); and
      ran the same test under the same conditions in an isolated worktree at the
      commit BEFORE its own, getting an identical failure with identical
      counters. Its whole diff was 23 insertions inside a function lifecycle
      never calls.
      That is the standard: a dependency edge exists, so prove it rather than
      asserting it. The assertion is genuinely flaky under CPU starvation and
      deserves a ticket in its owning area.
    severity: INFO
    status: recorded — and lifecycle's flaky liveness assertion is worth a ticket

  - id: OPEN-21
    title: Fleet has no production caller, so the device ledger is correct but unexercised
    detail: |
      Left open by the headroom agent. Fleet is exercised only by tests today,
      so the per-device commitment accounting it now does correctly is not yet
      reached by a shipping binary. If a production caller appears it must pass
      runtime.SelectionReserve() for the same reason the boot roots do, or it
      will reintroduce Gap 1 for its own path.
      Also noted: agentgen-boot does not use selection at all — it acquires
      against a config-derived figure — so there was nothing to wire there.
    severity: MEDIUM
    status: open

  - id: SELF-12
    title: I gave an agent a false premise and it correctly refused to act on it
    detail: |
      I framed the empty-secret decision to the operator as "an empty JWT
      signing key is as dangerous as a placeholder one, arguably worse — it
      looks intentional." The operator decided on that framing. The framing was
      WRONG for helix_llm, and the agent established it rather than implementing
      what I asked.
      What it found, with citations: every credential in that codebase is
      SELF-GATING — presence IS the enable-switch and absence is a documented
      off-state (9 provider keys gated `!= ""`; empty APIKeys means open access
      by design; RedisPassword gated by RedisHost). And Auth.JWTSecret has ZERO
      consumers: no JWT import outside tests, golang-jwt not a direct dependency,
      the auth submodule replace-only and never imported. Its own configuration
      doc says "Leave empty to disable JWT auth."
      So "must be supplied" would have refused EVERY current deployment,
      including this repo's own .env.example, which ships both auth vars blank.
      That is the outage I warned it about, and my own premise was the thing
      pointing at it.
      IT IMPLEMENTED THE CORRECT RULE INSTEAD: must be non-blank ONCE SUPPLIED.
      Not vacuous — it catches a live defect. Measured: APIKeys="" -> 200,
      APIKeys="   " -> 401 on EVERY request including the correct key,
      APIKeys="real-key" -> 200. Neither open nor protected: silent total
      lockout, from a config that validated cleanly.
      It stated prominently, not buried, that HELIX_AUTH_JWT_SECRET="" still
      loads — so the operator can see their decision was narrowed and why.
      LESSON FOR ME: I wrote a brief asserting a danger I had not verified in
      that codebase, and asked the operator to decide on it. The brief should
      have said "verify this holds here first" — which, fortunately, its
      discipline section did.
    severity: MEDIUM
    status: recorded — decision was narrowed on evidence, operator should know

  - id: HXLLM-blank-credential
    title: a whitespace-only credential locked out every request while validating cleanly
    detail: |
      Found by the empty-secrets agent while deriving required-vs-optional.
      APIKeys set to "   " passes validation, then rejects every request
      including the correct key — neither open access nor protected access, and
      nothing anywhere says why.
      FIXED: a credential must be non-blank once supplied.
      Mutation D is worth recording: echoing the OFFENDING value initially
      PASSED the whole suite, because the no-leak test only checked NEIGHBOURING
      secrets. Caught by an independent review round, fixed, re-run.
      The agent also reported hitting a U+00A0 non-breaking space that made an
      earlier assertion check for a string that never appears — the same
      false-pass class, from an invisible character.
    severity: HIGH
    status: fixed + guarded

  - id: OPEN-22
    title: Auth.JWTSecret is declared, configured, documented — and read by nothing
    detail: |
      Established while deriving the required-secret set. Declared since it was
      introduced, never read; the auth submodule is present in go.mod as a
      replace-only entry and never imported.
      NOT acted on, correctly — 11.4.124 requires investigating how a thing
      became unwired before removing it, and that investigation has not been
      done. If JWT auth is wired later, the blank-credential guard already
      covers the field.
    severity: MEDIUM
    status: open — investigate before removing or wiring

  - id: OPEN-15-CORRECTED
    title: my OPEN-15 finding was wrong; the endpoint is discarded, not mis-resolved
    detail: |
      I recorded that config/config.yaml's provider endpoint
      "${HELIX_LLM_ENDPOINT:http://localhost:8081}" was "used as a literal URL —
      a URL that is not a URL", and framed the operator's decision on it.
      That was WRONG, and the agent established it with a captured probe:
        REPRO-D  Config declares llm.providers: false
                 -> the value never reaches any Config field at all
      LLMConfig has no `providers` field, which is why `llm.providers` sits in
      inertConfigKeys. The block is DISCARDED, not mis-resolved. Expansion
      cannot reach a field that does not exist, so there was nothing there to
      fix; declaring that block is a separate change.
      The genuine expansion set turned out to be the NOTIFICATION CHANNEL
      credentials, which the notifications wiring made reachable in the same
      change — the two tasks interlocked.
      SECOND premise of mine an agent has corrected today (see SELF-12). Both
      times the agent verified rather than implementing what I asserted, which
      is the only reason neither shipped.
    severity: INFO
    status: corrected — the original finding was mine and it was wrong

  - id: HXC-notif-wired
    title: the notifications block now works, and os.Expand was eating dollar signs
    detail: |
      The block was reachable by nothing: no field on Config, so viper discarded
      it whole, and only HELIX_* environment variables ever produced a channel.
      Reproduced against a config valid in every other respect.
      NOW WIRED, with environment variables still winning and existing env-only
      deployments unchanged (verified, not assumed).
      FOUND EN ROUTE: the expander used os.Expand, which eats `$$`. Harmless
      while the five expanded fields were hostname-ish; not harmless the moment
      credentials came into scope — an SMTP password `pa$$word` came out as
      `pa`. Moved to a ${...}-only regex, which also makes the code match
      secret_placeholder.go's existing claim that a bare $VAR is never expanded.
      10 paired mutations, all diff-verified.
      Mutation M10 is worth recording: deleting a consumed field left the
      completeness guard GREEN, because the inert register downgrades that key
      regardless of the struct. That is the register working as designed, not a
      hole — but it was recorded in the test so nobody later assumes coverage
      that is not there.
    severity: HIGH
    status: fixed + guarded

  - id: HXC-prodconfig
    title: production-config.yaml had an AI transcript pasted into it and 210 lines YAML was already discarding
    detail: |
      The file was not valid YAML at all: a stray closing fence at line 569 with
      no opening fence, then "Now let me create the Phase 3 test
      implementation:" and a truncated `<function_calls>invoke<invoke name=`
      fragment. An agent's own narration, including the start of a tool call,
      written into a shipped config.
      Beneath that, six duplicate top-level blocks (workers, tasks, llm,
      logging, notifications twice). YAML keeps only the LAST occurrence, so
      210 lines were already being discarded — and they are superseded drafts,
      not stale pastes: the surviving copies carry extra keys.
      REPAIRED to exactly the extent that needed no judgement. Removal proven
      safe by the parsed configuration being BYTE-IDENTICAL before and after:
      213 lines removed, zero change to what the file configures.
      WHAT IS LEFT IS AN OPERATOR CALL AND IS NOT MADE: 22 keys — including the
      whole `environments`, `features`, `performance` and `security` sections —
      are configured at length and read by NOTHING (no Config field at any
      depth, zero direct viper reads in non-test code). Registered as inert with
      per-key reasons rather than deleted (which would discard intent nobody
      authorised discarding) or declared (which would claim the build reads
      settings it does not). Startup now says so out loud.
      Worth singling out: `logging.file` is inert while `logging.output` IS
      read, so the two can disagree and an operator editing the wrong one sees
      no effect and no explanation.
    severity: HIGH
    status: fixed to the unambiguous extent; 22 keys await an operator decision

  - id: HXA-netaddr-1-resolved
    title: a named production host was silently discarded and the connection went to loopback
    detail: |
      Far worse than the failing test row suggested, and the agent found the
      severe shape that was NOT in the test table:
        jobmanager_host="db.prod.internal[::1"
          -> "http://[db.prod.internal[::1]:8082"  -> hostname "::1"
      An operator names a production host, and the connection lands on loopback
      with no error anywhere. Proven reachable end to end from real config
      through flink.Config.GetRESTURL; JobManagerHost is a plain yaml/json
      field validated only for != "".
      MECHANISM, read from stdlib source rather than inferred: net/url's
      parseHost locates the IP-literal with strings.LastIndex(host, "[") and
      DISCARDS everything before it. And the reason a stray "[" is quiet at all
      is the other half — net.JoinHostPort brackets any colon-bearing host,
      MANUFACTURING the "]" whose absence was the only thing making "[" loud.
      Both halves are needed; neither alone bites.
      Same shape reached BaseURL from qdrant, chroma, the vectordb client,
      internal/ports and HELIX_LLM_HOST. Dial-side callers were never affected —
      SplitHostPort already rejects it — so the fix is URL-side only.
      The source carried the false claim itself: encodeGenDelims' comment listed
      "[" among bytes "already loud and deliberately left alone". That audit had
      been run on the raw host, never on the composed authority.
      FIXED by adding [ and ] to encodeGenDelims — the package's own documented
      mechanism, NOT the balanced-pair trick I had flagged as available.
      The test was reconciled, not weakened: its load-bearing assertion is now
      the invariant itself ("a malformed host must never become a WORKING
      DESTINATION — refuse, or hand back the operator's own host; never a third
      state"), with no reference to stdlib behaviour. The old url.Parse
      expectation survives demoted to a recorded go1.26 measurement.
      The third mutation is the one that matters: reverting the fix AND every
      wantURL so the other four assertions agree, leaving the new invariant to
      catch the defect alone. It does.
      281 packages ok, 0 FAIL.
    severity: HIGH
    status: fixed + guarded

  - id: HXLLM-lsp-cancel
    title: a request cancelled before it started could still complete, and was written off as flaky twice
    detail: |
      TestLSPClient_ContextCancellation was dismissed as a flake by TWO separate
      agents this session. It was not flaky. It was reporting a real coin flip.
      The test cancels the context BEFORE calling References, so an error is the
      only correct answer — there is no timing in the scenario at all. But
      `call` consulted ctx only in the select at the bottom, racing ctx.Done()
      against the response channel. With the context ALREADY cancelled and a
      response ALREADY waiting, both cases are ready and Go picks at random.
      MEASURED before: 39 PASS / 1 FAIL over 40 runs. After: 40/40 and 30/30.
      Mutation (remove the pre-check): back to 59/60. Both diff-verified.
      So an abandoned request was still written to the server, still registered
      a pending entry, and still returned its result as a success.
      THE DISTINGUISHING QUESTION, worth carrying: not how OFTEN a test fails,
      and not whether it passes in isolation — both were true here — but
      whether the scenario contains any LEGITIMATE timing. A context cancelled
      before the call contains none, so a random outcome could only be a defect.
    severity: MEDIUM
    status: fixed + guarded

  - id: OPEN-23
    title: the lifecycle flake is a different shape and needs its own fix
    detail: |
      internal/lifecycle/TestEvict_NeverUnloadsUnderAnInFlightRequest_Concurrent
      fails occasionally under CPU contention. Applying the distinction above:
      unlike the LSP case, this scenario DOES contain legitimate timing — the
      failing assertion is the test's own LIVENESS precondition
      (serving-refusals=0, "the contention window never opened"), while the
      actual FR-047 correctness invariant held at 0 violations throughout.
      So it is a genuine flake, but a badly-written one: a precondition that
      did not hold should retry until it does or SKIP with an explicit reason,
      not FAIL and imply the invariant broke. As written it generates false
      signal for every agent that runs the suite, and it already cost two of
      them time this session.
      Not fixed — it belongs to that package's owner and is out of scope here.
    severity: LOW
    status: "closed - 66131b8, the liveness precondition retries then SKIPs with a reason (previously - open — worth a ticket in its owning area)"

  - id: TASKS-audit
    title: the 89 tasks marked done all have their named deliverables
    detail: |
      Mechanically audited our own tracking rather than trusting it: extracted
      every backticked file path from the 89 tasks marked [x] — 73 references —
      and resolved each against the meta-repo, the two submodules, the spec
      directory and the sibling claude_toolkit checkout.
      ZERO genuine misses. The first pass reported 8, all of which were my
      resolver's blind spots (a sibling repo and the spec-relative research
      directory), not absent files. Recorded because reporting those 8 as
      findings would have been a false alarm of exactly the kind this ledger
      exists to prevent.
    severity: INFO
    status: verified

  - id: FR-056-agentgen-resolved
    title: agentgen-boot took its model from configuration and its VRAM figure from a second env var
    detail: |
      The lane decided what to run from AGENTGEN_MODEL_GGUF (configuration NAMING
      the model, which FR-056 forbids) and AGENTGEN_NEED_BYTES (a hand-maintained
      duplicate of a fact about that model). Its own doc comment was candid:
      "the two env vars MUST be changed together... never assume a bigger model
      fits the smaller default."
      REPRODUCED on the real host (RTX 3060, 11781 MiB free), pre-fix binary:
        19.5 GiB model named, NEED_BYTES untouched -> need=9216MiB, ADMIT-OK, exit 0
        same binary, same card, told its real figure -> ErrBudgetExceeded, exit 10
      The only difference was whether a human kept the second variable in step.
      Post-fix the 27B is withheld "short by 7289MiB" and nothing starts.
      The catalogue DID already cover the family (6 FamilyText entries), so the
      agent selected from them and added no entries and invented no figures.
      ENV VARS NOT SILENTLY DROPPED: AGENTGEN_MODEL_GGUF became an OUTPUT
      (reported as IGNORED-CONFIG), NEED_BYTES is reported and ignored,
      MODEL_DIR stays an input, and deliberate naming moved to --pin, validated
      against the catalogue and refused with the short resource named.
      A new guard walks the boot roots ON DISK and requires each selecting one
      to state the broker's margin, with a floor of 4 so a broken glob cannot
      pass vacuously. It found all four. The previous test asserted this only of
      the function, which would not have caught a lane that never called it.
    severity: HIGH
    status: fixed + guarded

  - id: OPEN-24
    title: the lane's three former candidates are not in the catalogue, so they can no longer run
    detail: |
      NEEDS AN OPERATOR DECISION — this is a real narrowing of what the lane can
      serve, and it is the flip side of the FR-056 fix.
      Mistral-Nemo-Instruct-2407, GLM-4.7-Flash and DeepSeek-Coder-V2-Lite were
      the lane's configured candidates. None is in the catalogue. The agent's
      framing is right: that IS the finding rather than an obstacle — they had
      no measured footprint anyone could check, which is precisely what the
      static 9 GiB placeholder stood in for.
      CONSEQUENCE: an operator who has a working Mistral-Nemo GGUF today will
      now be REFUSED rather than served. The commit says so plainly.
      The agent could not add them honestly: this host has no ~/models directory
      and therefore no GGUF to size, and it declined to guess (correctly — see
      SELF-7, where a guessed measurement was written into a data file).
      RESOLUTION NEEDS ONE OF: measure those GGUFs on a host that has them and
      add catalogue entries with recorded provenance; or accept the narrowed
      candidate set.
    severity: HIGH
    status: open — operator decision

  - id: OPEN-25
    title: five of six text entries skip the device axis, then hand a host-RAM figure to a VRAM broker
    detail: |
      Flagged by the agentgen agent rather than changed, correctly — altering
      selection semantics inside a change about one lane would have been the
      wrong place for it.
      Those entries carry requires_accelerator: false, so selection skips the
      accelerator axis entirely and the figure that reaches vrambroker.Acquire
      is a host-RAM measurement. That is the CRITICAL-5 shape again in a
      narrower form: a number checked against one kind of memory being spent
      against another.
      The vision lane already has the same shape, so this is not new and not
      isolated — which is the argument for fixing it as its own change.
    severity: MEDIUM
    status: open

  - id: OPEN-26
    title: modelchoice.go is now the fourth near-identical copy of one decision
    detail: |
      catalogueDir through refusalError is BYTE-IDENTICAL across the videogen,
      imagegen, visiongen and agentgen lanes.
      The agent declined to extract it, and gave the right reason: doing so
      would rewrite three currently-correct lanes inside a change about a
      fourth, with other agents live in the same checkout. It recorded the count
      in the file instead, so the case keeps growing visibly rather than
      silently.
      Worth doing when the checkout is quiet. Four copies is where a divergence
      becomes likely — and this feature has already been bitten once by two
      copies of one fact disagreeing (SELF-9, the precision lists).
    severity: MEDIUM
    status: open — deliberate deferral, not an oversight

  - id: TOOLKIT-1-2-3
    title: all three toolkit findings confirmed; two of their severity claims refuted
    detail: |
      FINDING 1 — CONFIRMED, severity reasoning REFUTED. jq's @tsv escapes only
      backslash, tab, newline and CR — NOT "/" or ":". Verified: both
      "helixllm/h/org/llama3:8b" and "hf.co/TheBloke/x:q4" pass through
      BYTE-IDENTICAL. So the reviewer's claim that this "breaks the common case"
      of org/model and hf.co names was wrong; it bit backslash- and
      control-character-bearing names only. Real, narrower.
      FINDING 2 — CONFIRMED, sub-claim REFUTED, and the practical impact is
      different from the report. The discovery secret is read by NO HTTP
      middleware — but neither is it "consumed by placement logic": placement
      consumes a pre-computed Trusted boolean, and the secret signs OUTBOUND
      attestation on a different path with its own nonce/proof headers.
      The important correction: the server's DEFAULT is open access, so the
      common case was never the reported 401. It was the request SUCCEEDING
      while the secret was disclosed to every configured host for nothing.
      FINDING 3 — CONFIRMED exactly as reported. A withdrawn model's .env and
      alias survived and stayed invocable.
      DELETION SAFETY is the part worth keeping. A record is retired only when
      three things hold: it carries a marker written by exactly ONE line in
      --apply (so hand-authored records and sync-written facade providers are
      structurally invisible), its host ANSWERED THIS RUN, and that answering
      host no longer lists it.
      The middle gate is the one that matters, and the reasoning is PATTERN-1
      applied to deletion: an unreachable host and a host that stopped serving
      everything produce IDENTICAL empty results, so silence is never treated
      as evidence. The mutation proves the danger — disabling that gate lets an
      unreachable host wipe the user's config. Removal reuses cmd_remove, which
      moves the directory aside rather than deleting.
    severity: HIGH
    status: fixed + guarded (commit pending final suite)

  - id: NOTE-4
    title: reviews produce leads, not facts — three severity claims have now failed verification
    detail: |
      Worth stating as a pattern, because it has held every time and it changed
      what got built.
        * T055 claimed OpenAI-compat local backends are "silently dropped" —
          REFUTED: deliberate design, and fixing it would have stamped
          identities on proxied vendor models, which the design forbids.
        * T055 claimed the @tsv escaping "breaks the common case" of org/model
          and hf.co names — REFUTED: @tsv does not escape "/" or ":" at all.
        * T055's Bearer-secret finding predicted 401s — REFUTED in practice:
          the default is open access, so requests SUCCEEDED while leaking the
          secret. Worse than reported, in a different way.
      In every case the agent verified before acting, and in every case the fix
      that shipped differs from the fix the report implied. A review finding is
      a place to look, not a thing to implement.
    severity: INFO
    status: recorded

  - id: OPEN-19-resolved
    title: the integration tests never needed live services; the endpoint was never wired to the mock
    detail: |
      Both failures came from ONE missing line in the test framework, and no
      service of any kind was required.
      /v1/models is served from RouterOptions.ModelBrain, a SEPARATE option from
      Brain — the listing handlers need the concrete *brain.Brain for Models(),
      while Brain is an interface that may be a fallback.Chain. The framework
      set Brain and left ModelBrain nil, so the handler took its no-backend
      branch and listed nothing. The tests predate that split and the framework
      was never updated when /v1/models moved off Brain.
      Meanwhile the framework registers a mock provider reporting FOUR models
      and answering Available() true. It was simply never connected.
      FIXED with `ModelBrain: b`. Both pass; removing it fails both with the
      original message. helix_llm now has no failing package.
      WHY THIS LASTED, and it is the useful part: THREE separate agents hit
      these tests in this session. Every one did the right thing — proved the
      failures pre-dated its own work, one via `go list -deps -test` showing
      zero dependency edges, another by re-running at pristine HEAD in an
      isolated worktree. Then each stopped, because "needs live services" is a
      plausible story for a red integration test and nobody had cause to doubt
      it. I recorded that assumption myself in OPEN-19, flagging it as an
      assumption — and still did not check it for hours.
      PROVING A FAILURE IS NOT YOURS IS NOT THE SAME AS FINDING OUT WHAT IT IS.
      The first is a defensive check and the whole fleet did it correctly. The
      second is the actual question, and nobody asked it.
    severity: MEDIUM
    status: fixed + guarded

  - id: OPEN-27
    title: the RouterOptions doc comment describes a hardcoded model list that no longer exists
    detail: |
      RouterOptions.ModelBrain's comment says "When nil the endpoints return the
      built-in hardcoded model list."
      That list is gone — it was removed as a fabricated three-entry list
      advertising models the server could never serve (the i18n keys that
      replaced it say so). The endpoints now return an empty list with a reason.
      Not fixed here only because another agent is live in that package right
      now. Small, and worth doing: a stale comment describing removed behaviour
      is how the ModelBrain wiring gap stayed invisible.
    severity: LOW
    status: "closed - fe87cf1, the RouterOptions comment now says what nil actually does (previously - open)"

  - id: TOOLKIT-uncommitted
    title: the whole helixllm-export feature was uncommitted, so none of its defects ever shipped
    detail: |
      MATERIAL CORRECTION to how these three findings were framed, including by
      me. The agent checked before assuming: `git log -S` shows the feature
      appears in NO commit, and `git show HEAD:scripts/claude-providers.sh |
      grep -c helixllm-export` returns 0.
      So the corrupted identities, the misdirected credential and the add-only
      --apply were defects in work-in-progress, never in anything a user ran.
      That changes the severity, not the fixes — and the commit necessarily
      lands the feature together with the fixes, because the fixed lines were
      themselves uncommitted.
      Committed to a BRANCH (fix/helixllm-export-review-findings), main left
      untouched at 75d25ab. Merging it lands the whole feature, not just the
      fixes, which is a decision rather than a formality.
      Also worth recording: the agent caught its own bad assertion. It had
      written "no request went out unauthenticated", which counted the test's
      OWN plain-curl evidence probes and would have passed for the wrong
      reason. Replaced with a scoped window comparing total-vs-keyed counts.
      And it reconciled rather than weakened the credential assertions: the old
      control needle asserted the secret WAS sent — it certified the defect —
      and now asserts the gateway key arrives while the secret reaches no server
      and no file.
    severity: MEDIUM
    status: fixed on a branch; merging is an operator decision

  - id: MILESTONE-green
    title: helix_llm is fully green — 53 packages, 0 failures
    detail: |
      First time this session. The two integration failures that three agents
      had written off as needing live services were the last red thing, and they
      needed one line in the test framework.
      Measured on a quiet tree: `go test -count=1 ./...` -> 53 ok, 0 FAIL.
      Not a claim that the feature is finished — 8 tasks remain open, four of
      them reviews that must iterate to a zero-finding GO, and roughly 20
      findings are still open. It is a claim that nothing in the module is
      currently failing, which was not true at any earlier point today.
    severity: INFO
    status: recorded

  - id: T055-LOW-verdict
    title: the "withheld vocabularies disagree" finding is a false alarm; the real defect is adjacent
    detail: |
      REPORTED (T055, LOW): helix_llm publishes provider-unavailable /
      unnameable-model / identifier-conflict / instance-unreachable /
      model-unavailable, while helix_agent's WithheldReason.Known() accepts only
      insufficient_resources / unsupported_configuration /
      excluded_by_usage_terms — "vocabularies disagree across the wire".
      REFUTED. They are not two vocabularies for one field. They are two
      vocabularies for two DIFFERENT fields, and they never meet:
        * the five hyphenated reasons are assigned to opt.Reason on
          naming.Offer/Instance (internal/brain/naming.go, internal/naming/
          export.go) and appear in the export artifacts' comment lines.
        * api.Model.WithheldReason — the WIRE field — is NEVER ASSIGNED
          anywhere in non-test code. Zero writers.
      That is the FOURTH severity claim from this one review that did not
      survive verification.
      THE REAL FINDING, which the report pointed near but did not state:
      helix_agent READS withheld_reason off the wire (adapters/helixllm/
      types.go:167) and VALIDATES it (catalog/helixllm_source.go:71 calls
      Known()). helix_llm never writes it. The consumer is fully prepared for a
      field the producer never sends, so that validation path cannot fire and
      the withheld branch is dead code awaiting a producer.
      This is the same shape as the exporters-with-no-caller finding: declared,
      documented at length, and unreachable.
      A LANDMINE WORTH RECORDING for whoever wires it: the wire doc says
      withheld_reason is "one of the three recorded machine keys" — helix_agent's
      three. A helix_llm developer wiring this would naturally reach for the
      FIVE naming reasons already defined in that codebase, which are the wrong
      vocabulary for this field and would fail Known() on arrival. The two sets
      even use different separators (hyphen vs underscore), so a mistake here
      would look deliberate.
      NOT changed: populating or removing a documented wire field is a contract
      decision, and 11.4.124 requires investigating how it came to be unwired
      first. Recorded rather than guessed.
    severity: LOW
    status: reported finding refuted; adjacent real finding recorded, open

  - id: SELF-13
    title: staging by path does not scope a commit — I swept 1424 lines of another agent's work
    detail: |
      My commit 52c827f is titled "docs(faq): the agent lane refuses models it
      used to run" and contains NINE files: my 32-line FAQ addition, and 1424
      lines of the request-validation agent's in-flight gateway work
      (requestvalidate.go, requestvalidate_test.go, upstream_error.go,
      anthropic.go, openai.go, websocket.go, i18n.go, and its design doc).
      I did NOT use `git add -A`. I staged exactly one file, by path. That was
      not enough, and this is the part worth learning:
        `git add <path>` adds to the index. `git commit` commits the ENTIRE
        index — including whatever another agent staged in the same checkout
        before I got there.
      This is SELF-8 recurring in a subtler form. After SELF-8 I adopted "stage
      explicitly by path", wrote it into the resumption file as a house rule,
      and put it in every agent brief. It is a necessary rule and I proved it is
      not a sufficient one.
      THE CORRECTED DISCIPLINE, in a shared checkout:
        * `git commit -- <paths>` (pathspec-limited) commits ONLY those paths
          regardless of what else sits in the index; or
        * inspect `git diff --cached --stat` IMMEDIATELY BEFORE committing and
          confirm it lists only your files.
      I did run that check before several other commits today. I skipped it on
      this one, and that is the whole difference.
      NOT REWRITTEN. Nothing is lost, nothing is pushed, and the agent handled
      it correctly: it declined to rewrite history and made 834971c the
      properly-titled record that says what happened. Rebasing now, with agents
      live in this checkout, would risk far more than a misleading commit title
      costs. The title is wrong; the record is complete.
    severity: MEDIUM
    status: recorded — not rewritten, deliberately

  - id: G2A-resolved
    title: malformed requests were told the server broke, and the error named the backend's address
    detail: |
      CONFIRMED and fixed. Reproduced live first: messages:[], max_tokens:-1,
      max_tokens:0+stream, role:null, and unicode/long/traversal model names all
      returned 503 with a body carrying "dial tcp 127.0.0.1:50052: connect:
      connection refused".
      ONE CORRECTION TO THE TRIAGE'S FRAMING, and it matters: 503 is WORSE than
      500 here. It tells a client to retry with backoff, and a retry of
      messages:[] can never succeed — so a correct client retries forever.
      SCOPE was larger than triaged: not two handlers and five error sites but
      THREE body-taking handlers and EIGHT error sites. /v1/agents/chat and
      /v1/agents/coordinate were found by its own review — mounted on the same
      engine, wrapping brain.Complete with %w and returning it raw. The
      WebSocket had neither redaction nor translation.
      The policy is recorded per-endpoint with citations, and the
      deliberately-NOT-validated list is as carefully argued as the validated
      one (message content, because assistant tool_calls have empty content;
      the role enum; model EXISTENCE, because that is a 404; and auth, because
      the validator never reads the Authorization header and therefore cannot
      collapse a 401 into a 400 — which keeps G2b genuinely untouched).
      Nine mutations. M9's first form left an unused import, so the package did
      not build and the test printed no failure line — the agent caught that a
      BUILD FAILURE IS NOT A PASSING GUARD and re-ran it properly. That is
      PATTERN-1 again, in yet another shape.
      MEASURED: 25/78/28 -> 34/69/28, with an exact set diff showing ZERO new
      failures. Nothing weakened, nothing skipped.
    severity: HIGH
    status: fixed + guarded

  - id: SECURITY-5
    title: the security manual documented JWT authentication that does not exist
    detail: |
      Found by running the 11.4.124 investigate-before-remove work on OPEN-22
      (Auth.JWTSecret declared and read by nothing). The field itself is minor.
      What the investigation surfaced is not.
      docs/manual/security.md had a "JWT Authentication" section telling
      operators to set HELIX_AUTH_JWT_SECRET, stating "when set, the system can
      issue and validate JWT tokens for session-based access", rating the
      variable "High — token signing key" in its severity table, and listing
      "Set a strong HELIX_AUTH_JWT_SECRET" on the PRODUCTION HARDENING
      CHECKLIST. docs/manual/modules.md made the same claim in its module table.
      There is no JWT anywhere in the server.
      THE COMBINATION IS THE DEFECT. An operator could set that secret, tick the
      checklist item, and believe the server was protected — while
      HELIX_AUTH_API_KEYS sat empty and the server was in open-access mode,
      answering anyone who could reach it. The document written to prevent that
      outcome was the thing producing it.
      VERIFIED, with the instrument sanity-checked against a module that IS
      imported (digital.vasic.containers, 12 real import sites):
        * `git log -G 'jwt\.(Parse|NewWithClaims|SigningMethod)' --all` -> 0.
          No commit in the entire history ever added a JWT library call.
        * golang-jwt does not appear in go.mod at all.
        * digital.vasic.auth is a `replace` directive at go.mod:110 with NO
          matching `require`, imported by no Go file. A replace without a
          require does nothing.
        * the only auth enforced is APIKeyAuth — a string comparison, open
          access when the key list is empty.
      FIXED at the documentation layer: the section says NOT IMPLEMENTED and
      states what authentication actually exists; the severity row now reads
      "None today"; the checklist item is STRUCK THROUGH rather than deleted, so
      an operator who followed the old list can see it was withdrawn and go
      re-check that HELIX_AUTH_API_KEYS is genuinely set.
      The config field is KEPT — removing it would break .env files that set it,
      and it is where an implementation would land.
      §11.4.124 VERDICT on OPEN-22: this is not wiring that rotted. It was never
      built. The field arrived in 48a6479 ("feat: add HelixConfig with env
      loading and validation") in a commit containing only config.go and its
      test, and no implementation ever followed. Whether to build JWT auth or
      drop the field is now an INFORMED decision rather than a blocked one.
    severity: HIGH
    status: documentation fixed; whether to implement JWT auth is an operator decision

  - id: T055-REREVIEW-NOGO
    title: the re-review is NO-GO — three of four findings were created by the fixes
    detail: |
      This is the most important result of the session, and it corrects reports
      I already gave the operator as successes.
      Every finding was CONFIRMED BY RUNNING CODE, not by reading.
      F1 (HIGH) — the identity re-mint means a user holding an OLD identifier is
        SILENTLY ANSWERED BY THE WRONG MODEL. Captured:
          stale "helixllm-127-0-0-1-qwen2-5-7b-ba85a3230a59" -> HTTP 200
            model="deepseek-chat" ... answered by chutes
            cloud received [deepseek-chat] ; local received []
        That is the original CRITICAL, reproduced exactly, for the population of
        identifiers the batch ITSELF created. PinModel misses, and the chain
        falls through to the score-ordered loop. The fix's rationale ("a caller
        who wants any available model names no model") does not describe a
        string carrying our own helixllm- prefix.
      F2 (HIGH) — --apply retirement FAILS OPEN and deletes a user's whole
        config for a host that is merely restarting. Gateway up + backend
        loading gives /health 503 -> unavailable options dropped -> /v1/models
        answers 200 with an empty data array -> the toolkit reads "answered"
        -> gate 3 retires EVERY record -> cmd_remove rm -f's the .env and alias.
        ~30 seconds after a restart is enough.
        The toolkit's own test LOCKS IT IN at line 474: "a host that answers
        with an EMPTY list still converges … its last provider is retired too".
        Note the shape: the agent that wrote that sweep articulated exactly the
        right principle — "silence is never evidence" — and then drew the line
        one state too far, reasoning an empty answer is a real answer. It is
        not, when the backend is still loading.
      F3 (MEDIUM) — the pin fix put a SYNCHRONOUS /health probe on every
        request's hot path, including cloud requests that never touch the local
        backend. Measured: a hung /health adds 2.008s to a deepseek-chat request
        that completes in 423us when healthy. The route test could not see it
        because its stub's Available() is a bool.
      F4 (MEDIUM) — a raw name served by two providers pins to the
        alphabetically-first EVEN WHEN IT IS DOWN, refusing while a serving copy
        is up. Before the batch that request was served.
      W1 (LOW) — pinned-unavailable returns 500 where the chain returns 503.
      WHAT THIS MEANS FOR ME: I reported F1's fix, F3's fix and F4's fix to the
      operator as successes. They were real fixes and they each created a new
      defect. §11.4.134's iterate-to-GO exists precisely because a first pass
      that "addressed the findings" is not the same as a batch that works, and
      this is the clearest demonstration of it in the session.
    severity: CRITICAL
    status: "closed - see F1-F4-W1-resolved, F2-resolved and WITHHELD-on-wire (previously - NO-GO — fixes dispatched for all five)"

  - id: LANEBOOT-extracted
    title: 1008 duplicated lines removed, and the extraction proved itself by interleaved binary diff
    detail: |
      The four-way byte-identical span confirmed independently before any edit
      (the agent's first digest differed only by a trailing newline, which it
      diagnosed rather than shrugged at). It also measured the duplication I had
      flagged as unmeasured: the 28-line helper block and defaultCatalogueDir
      are identical in all four; sortedKeys in two, normalise in three.
      ~1008 duplicate lines gone; the four modelchoice.go files 1977 -> 586.
      LEFT PER-LANE ON EVIDENCE, not taste: exit code 24 keeps its lane-specific
      NAME — exitNotServable and exitWeightsNotPresent mean different things —
      and the agent verified the shared span never reaches it.
      THE PROOF METHOD IS THE PART WORTH KEEPING. Package tests passing is not
      proof a lane still DECIDES the same way, so it built the pre-extraction
      binaries from a pristine worktree and diffed 12 flows per lane, 48
      comparisons, all byte-identical — with 11 of 12 flows producing DISTINCT
      output so the comparison could not be vacuous.
      Two methodology traps it hit and fixed rather than waved through:
        * the first worktree ran from /tmp (12 GiB free vs 1.19 TiB), which
          flipped a storage-bound videogen model — environmental, not
          behavioural;
        * a later comparison showed a difference after a COMMENT-ONLY edit,
          because host memory had dropped mid-session and flipped the binding
          constraint from accelerator to memory. Re-running both binaries
          INTERLEAVED under one host state gave identical output.
          For a binary that measures the live host, a stale baseline is invalid.
      THE ANTI-DRIFT GUARD FAILED AS DESIGNED: extraction moved the only
      selection.Select out of cmd/, its floor-of-4 hit zero, and it refused to
      pass vacuously. Reconciled and made STRONGER — one call site permits a
      check four copies could not state: no cmd/ file may call selection.Select
      directly, and laneboot must hold EXACTLY ONE.
      And a finding about the old guard itself: its precise-looking first
      pattern "Reserve:       runtime.SelectionReserve()," NEVER MATCHED
      ANYTHING, because gofmt writes a single space. Only its loose fallback
      ever did any work.
      Its own review found two false-negatives in its new gate, one of which its
      comment had already claimed was closed — which the agent called "a bluff
      in my own words until fixed". Both reproduced and fixed.
    severity: INFO
    status: landed

  - id: OPEN-28
    title: the toolkit baseline may have moved from 2 failures to 3 — unsettled on purpose
    detail: |
      The re-review's final suite pass measured claude_toolkit at 61 files /
      58 passed / 3 FAILED, one worse than the 59/2 baseline every prior agent
      recorded.
      The third is test_alias_file_concurrency.sh, a lock/storm timing suite.
      The reviewer did NOT attribute it, and gave its reasons: the branch's diff
      against main does not touch scripts/lib.sh at all, that test file was last
      changed in v1.24.0, and it ran while two Go suites and another agent's
      build shared the host. Marked SUSPECT host-load, explicitly UNCONFIRMED.
      That is the right posture, and it is also actionable: re-run it in
      isolation.
      NOT DONE YET, DELIBERATELY. An agent is live in that repo right now, and
      re-running a contention-sensitive test under the very contention that
      probably caused it would settle nothing — the §11.4.119 single-owner point.
      Settle it once that agent lands and the host is quiet.
      Apply today's discriminator when it is settled: does the scenario contain
      LEGITIMATE timing? A lock/storm concurrency suite does, unlike the LSP
      cancellation case, so load-sensitivity is plausible here in a way it was
      not there. Plausible is not measured.
    severity: LOW
    status: "closed - see OPEN-28-resolved (previously - open — settle on a quiet host)"

  - id: HXA-testharness-1
    title: five integration tests need a gitignored BUILD ARTIFACT, and fail because nobody built it
    detail: |
      The re-review described helix_agent's 22 failures as "environment-bound
      and out of scope". I checked rather than inheriting that, because the same
      description was wrong for helix_llm's integration tests earlier today
      (OPEN-19 — they needed one line in the test framework, not a service).
      ESTABLISHED BY READING: bin/helixagent is gitignored (.gitignore:29), i.e.
      it is a BUILD ARTIFACT, and five integration tests require it:
        tests/integration/cli_schema_validation_test.go
        tests/integration/opencode_config_regression_test.go
        tests/integration/opencode_test.go
        tests/integration/mcp_sse_endpoint_test.go
        tests/integration/ensemble_handler_integration_test.go
      A test that fails because nobody ran `make build` first is a HARNESS gap,
      not an environment gap. The distinction matters: an environment gap is
      something the machine cannot provide, and this is something the test could
      provide for itself, or name in a SKIP, or the harness could build.
      NOT YET VERIFIED by building — a baseline run was in flight and building
      mid-run would have invalidated it, which is the stale-baseline trap the
      laneboot agent hit and diagnosed. Verify by building and re-running.
    severity: MEDIUM
    status: open — established by reading; build-and-rerun verification owed

  - id: HXA-testharness-2
    title: a test reads the operator's personal config and asserts a fact about their machine
    detail: |
      tests/integration/cli_schema_validation_test.go:166,
      TestOpenCodeSchemaValidation, does:
        configPath := filepath.Join(os.Getenv("HOME"), ".config", "opencode", "opencode.json")
        data, err := os.ReadFile(configPath)
        if err != nil { t.Fatalf(...) }
      It reads the DEVELOPER'S REAL PERSONAL CONFIG and fails hard when absent.
      Three things are wrong with that, in increasing order of importance:
        * it fails on any machine where the operator has not installed and
          configured OpenCode — which is not a defect in this project;
        * a PASS asserts "this machine's OpenCode config is schema-valid", a
          fact about the operator's machine rather than about this codebase.
          The test's subject is wrong, so neither outcome says anything about
          the code under test;
        * it reads whatever the operator has in that file.
      The surrounding table (agentConfigs()) builds ConfigPath under homeDir for
      several third-party CLI agents, so the file's intent looks like "validate
      the installed agents' configs". If that is the intent, the absence of an
      optional third-party tool must be a SKIP naming what is missing, never a
      Fatalf. If instead it is meant to validate a config THIS project
      generates, it should generate one into a temp dir and validate that.
      Either reading makes the current form wrong.
    severity: MEDIUM
    status: open — needs a decision on which of the two it is meant to be

  - id: HXA-npm-1
    title: generated user configs reference two npm packages that do not exist
    detail: |
      Found by running down TestMCPPackageExistence, one of the helix_agent
      failures described as "environment-bound and out of scope".
      This project writes THREE DIFFERENT sqlite MCP package names into configs
      across four files. Verified against the live npm registry, with the
      instrument sanity-checked (the third lookup RESOLVED, so the two 404s are
      real answers and not a network failure):
        @modelcontextprotocol/server-sqlite   DOES NOT EXIST
          scripts/cli-agents/configs/generated/opencode/opencode.json:45
          scripts/cli-agents/configs/opencode.template.json:66   <-- the TEMPLATE
        @anthropic-ai/mcp-server-sqlite       DOES NOT EXIST
          scripts/cli-agents/configs/generated/crush/crush.json:40
        mcp-server-sqlite-npx                 exists, 0.8.0
          configs/cli-agents/crush.json:128
      A user who takes the generated opencode or crush config gets an MCP server
      that fails to start on an npm 404. And because the TEMPLATE carries a
      non-existent name, every future generation is wrong too.
      NOT FIXED BY ME, deliberately. The substitution is not a rename: the
      working package takes different arguments (`["-y",
      "mcp-server-sqlite-npx"]` with no --db-path, versus `["-y", "<pkg>",
      "--db-path", "..."]`). Choosing the package and its invocation needs
      research into what these servers actually are, and guessing it is how
      SELF-7 happened.
    severity: HIGH
    status: "closed - see HXA-npm-1-resolved (previously - open — dispatched)"

  - id: HXA-testpath-1
    title: the spec-kit tests look for a directory four levels above where they run
    detail: |
      TestGitHubSpecKitSubmoduleVerification and its three siblings fail with
        stat cli_agents/spec-kit: no such file or directory
      github_speckit_integration_test.go:29 uses the BARE RELATIVE PATH
      "cli_agents/spec-kit". Go runs a test from its own package directory, so
      that resolves to
        submodules/helix_agent/tests/integration/cli_agents/spec-kit
      The directory genuinely exists — at the META-REPO ROOT,
      /home/milosvasic/Projects/helix_code/cli_agents/spec-kit, four levels up.
      So four tests were failing because they look in the wrong place, and were
      being written off as environmental. Nothing about the machine is missing.
    severity: MEDIUM
    status: "closed - see HXA-testpath-1-resolved (previously - open — dispatched)"

  - id: NOTE-5
    title: the label 'environment-bound' was doing a lot of work, and none of it was true
    detail: |
      Three separate labels of this kind have now been checked and none held:
        * helix_llm's two integration failures — "needs live services". They
          needed one line in the test framework (OPEN-19).
        * helix_agent's spec-kit failures — "environment-bound". A bare relative
          path resolving four levels below the directory it wants.
        * helix_agent's TestMCPPackageExistence — "environment-bound". The
          registry lookup was CORRECT and the project's own generated configs
          name packages that do not exist.
      In every case the label was plausible, nobody had reason to doubt it, and
      it was wrong. The pattern is not that the agents were careless — each
      correctly proved the failure was not its own work. It is that proving a
      failure is not yours ends the inquiry, and the label supplies a reason
      that feels like an answer.
    severity: INFO
    status: recorded

  - id: F2-resolved
    title: --apply no longer deletes a config for a host whose backend is still loading
    detail: |
      Reproduced with captured output before the fix, and BOTH runs committed as
      evidence (scripts/tests/proof/97b-...):
        pre-fix  -> "env file ... -> GONE ; alias 0 entries remain ; VERDICT: LOST"
        post-fix -> KEPT
      POSITIVE SERVING EVIDENCE chosen: at least one listing entry carrying a
      non-empty model_identity whose availability is "serving" or absent —
      factored into ONE shared jq predicate used BOTH to build records and to
      judge the host, "so the two can never disagree about the same listing".
      That is the SELF-9 lesson (two copies of one fact drifting apart) applied
      preventively rather than after the fact.
      A SIDE DOOR I HAD NOT CONSIDERED, found by the agent: vendor passthroughs
      deliberately omit model_identity and stay reachable while the local
      backend is down — so a vendor-only listing is ANOTHER FACE of the loading
      host, and counting it as evidence would have reintroduced the bug through
      a different route. Now its own test.
      IT READ THE `reason` FIELD AND REJECTED IT AS EVIDENCE, correctly:
      HandleListModels attaches the same string for ANY empty listing with a
      backend present, so it reads identically whether the backend is mid-load
      or has genuinely stopped serving everything. It distinguishes neither.
      Echoed to the user in the warning, and trusted for nothing.
      CONSERVATIVE COST STATED: a host that genuinely empties itself never
      converges until it serves something again.
      Test line 474 RECONCILED, not deleted — rewritten to assert the opposite,
      with a comment recording that the old assertion WAS THE DEFECT, PINNED.
      Two mutations: reverting gate 2 fails 5 assertions and the repro prints
      LOST; making gate 2 reject everything fails 6, proving the survival
      assertions cannot be satisfied by quietly disabling retirement.
      It also caught its own suite regression (test_sandbox_hygiene) and
      diagnosed it as its own: a grep nested in a command substitution reads
      statically like a call. Suite back to the 61/59/2 baseline.
    severity: HIGH
    status: fixed + guarded

  - id: SELF-14
    title: an agent verified in the wrong repository, and I caught it because it contradicted me
    detail: |
      The F2 agent reported: "Neither WithheldReason nor withheld_reason occurs
      anywhere in helix_agent's Go sources, so if such a branch exists it keys
      on something else and I did not find it." It recorded the reviewer's claim
      as unconfirmed rather than fact — the right instinct, wrong result.
      RE-VERIFIED, and it is not close: 56 occurrences in helix_agent, including
        internal/adapters/helixllm/types.go:167  the wire field
        internal/catalog/catalog.go:90           the catalog field
        internal/catalog/helixllm_source.go:71   rejects unless Known()
        internal/router/helixllm_catalog_source_test.go:277
              "withheld_reason":"insufficient_resources"
        internal/router/helixllm_catalog_source_test.go:311
              if withheld.WithheldReason != catalog.ReasonInsufficientResources
      There is already a TEST feeding the consumer the exact wire shape and
      vocabulary the producer change is being asked to emit.
      THE CAUSE IS VISIBLE IN ITS OWN WORDS: it described what it found as "the
      unrelated selection.WithheldReason type" — and `selection` is a HELIX_LLM
      package. It searched the wrong tree. Not carelessness; a scope slip, and
      an easy one, because BOTH repos define a type of that name and only one of
      them is the wire type.
      WHY IT WAS CAUGHT: it contradicted something I had verified myself and
      then written into a live agent's brief. Had I not held that fact, the
      running agent would have been told its premise was unconfirmed and might
      have narrowed its work. I sent it the correction with the citations.
      This is the third premise-error in this programme caught by someone
      holding independent evidence — two of mine caught by agents, one of an
      agent's caught by me.
    severity: MEDIUM
    status: corrected; running agent informed

  - id: OPEN-29
    title: the second reported instance is server-side only, not a toolkit exposure
    detail: |
      The re-review reported that GET /v1/config/opencode returning "models":{}
      during loading, with MergeOpenCode replacing the user's entry wholesale,
      was a smaller instance of the same data-loss root.
      CORRECTED by the F2 agent, with evidence: the toolkit has NO consumer of
      /v1/config/* at all (zero hits across the whole repo), and MergeOpenCode
      is called only server-side at internal/gateway/consumer_config.go:204,
      where the SERVER writes the user's opencode.json. The toolkit's own
      claude-opencode-sync.sh builds that file from the toolkit's provider
      records, not from HelixLLM's export.
      So the exposure is real but SERVER-SIDE ONLY, and the toolkit fix neither
      covers it nor needs to. Still open on the server.
    severity: MEDIUM
    status: open — server-side

  - id: G8-resolved
    title: the one undetermined challenge failure was a gap between two correct things
    detail: |
      G8 was the single failure of 83 that the triage left explicitly
      UNDETERMINED rather than guessing at — the right call, and it turned out
      to be the most interesting one.
      rag-ingestion/ingest-document failed with "field body.chunks is not
      countable". Neither side was wrong:
        * knowledge.IngestResult reports Chunks as an INT, so the response is
          {"chunks": 3} and not {"chunks": [...]} — a perfectly reasonable API;
        * min_count asks for a LENGTH, and an integer has none.
      The assertion vocabulary had NO WAY to express a numeric minimum at all.
      So the challenge could not state its own assertion. Not a defect in the
      server, not a defect in the challenge — a gap between them, which is
      exactly the kind of thing "undetermined" should be reserved for.
      FIXED by adding min_value. Its refusal for a collection NAMES the other
      assertion, so the next person who reaches for the wrong one is told which
      is right.
      PROVED AGAINST THE PRODUCTION TYPE, not a JSON literal:
      TestMinValueMatchesTheRealIngestResult marshals knowledge.IngestResult and
      runs the shipped challenge's exact assertion over those bytes. If Chunks
      ever becomes a slice that test starts failing with "use min_count" — the
      CORRECT failure, since the challenge would then need the other assertion.
      A hand-written fixture would have gone on passing while the real response
      changed underneath it.
      Negative controls both directions: min_count must still refuse an integer
      (or min_value is redundant), and min_value must refuse a collection, a
      non-numeric scalar, and an absent field.
      The mutation was better than expected: unregistering min_value fails the
      HARNESS INTEGRITY tests, because a shipped bank would then declare an
      unknown assertion type. That guard already existed and the change is
      caught by it.
      HONEST BOUNDARY: the live challenge run is not done — it needs a running
      server. The chain is closed at the type level instead, which for this
      particular question is stronger than a live run would be: it pins the
      assertion to the production struct rather than to one observed response.
    severity: MEDIUM
    status: fixed + guarded

  - id: OPEN-28-resolved
    title: the toolkit's third failure was host load — settled, baseline unchanged at 2
    detail: |
      test_alias_file_concurrency.sh run three times in isolation on a quiet
      host (load average 2.17): 3 PASS, 0 FAIL.
      So the 61/58/3 the re-review measured was contention, not a regression,
      and the baseline stands at 61 files / 59 passed / 2 failed
      (test_claude.sh, test_providers.sh).
      The reviewer's posture was right on both counts: it declined to attribute
      the failure AND it said what would settle it. Deliberately not run while
      an agent was working in that repo — re-running a contention-sensitive test
      under the contention that probably caused it settles nothing.
      Today's discriminator applied cleanly: this scenario DOES contain
      legitimate timing (a lock/storm suite), unlike the LSP cancellation case,
      so load-sensitivity was plausible here. Plausible then measured.
    severity: LOW
    status: resolved — host load

  - id: F1-F4-W1-resolved
    title: the four pin-path regressions and the status mismatch are fixed
    detail: |
      All four reproduced first with output matching the reviewer's.
      F1 — a retired identifier now returns 404, not 503.
        STATUS CHOSEN WITH A REASON: 404 rather than 410, because this gateway
        already answers 404 for every other "the name you gave identifies
        nothing here" condition — unknown consumer, unknown host, model id
        absent from the listing. A second spelling would make a client's
        handling depend on which endpoint replied.
        The retired set is DERIVED, not hardcoded: each retired host is rendered
        through the same sanitise() the derivation uses and terminated by the
        ruleset separator.
        NON-GENERALISATION PROVEN, including the substring hazard I would have
        worried about: a live identifier on host "localhosting" stays 503 both
        when serving and when down. Dropping the trailing separator makes that
        case wrongly 404 — which is mutation 3.
      F3 — resolution is registry-only; no Available() and no network on the
        request path. A hung local backend no longer adds 2s to a cloud request.
      F4 — a host-less raw name now prefers an available provider among those
        serving the exact name, still never a different model.
      W1 — typed sentinel so the pinned path maps to 503 like its sibling, with
        ErrRetiredIdentifier deliberately NOT wrapped in it so the new 404 stays
        distinct.
      Five mutations, each diff-verified. The agent also flagged a gap IT
      introduced: registry-only resolution plus the new strictness means an
      unregistered valid identifier would be REFUSED. Every existing test
      fetched its identifier via Brain.ModelOptions(), which populates the
      registry as a SIDE EFFECT — so all of them would have passed with startup
      registration deleted. Its new test derives the identifier independently
      via naming.Derive, never touching the Brain.
    severity: HIGH
    status: fixed + guarded

  - id: WITHHELD-on-wire
    title: withheld models now reach the wire, and the vocabulary was extended rather than mapped
    detail: |
      THE AGENT DISAGREED WITH THE MAPPING OPTION, correctly and with a reason
      worth keeping: mapping `provider-unavailable` onto
      `unsupported_configuration` ("more memory does not help") would state the
      OPPOSITE of the truth about a loading backend, and destroy the exact
      signal the toolkit needs to tell loading from withdrawn.
      So the wire set grew by exactly two — provider_unavailable,
      identifier_conflict — with helix_agent's Known() admitting them IN THE
      SAME CHANGE. The hyphen/underscore trap I flagged is real and proven:
      mutation M3 shows the raw hyphenated token arriving at helix_agent as
      withheld_reason="" and being silently discarded.
      It declined to add unnameable_model, because such an option keeps its RAW
      REJECTED NAME as its id and safeToPublish drops it — "adding a key nothing
      writes would repeat the defect being fixed". The gap is stated in the
      code rather than papered over.
      NON-CONSUMABILITY PROVEN AGAINST REAL CONSUMERS, not stand-ins:
        * the toolkit's serving filter reproduced verbatim and run by REAL jq
          against the real body — selects nothing, while the same filter selects
          two served models (the vacuity guard);
        * helix_agent decoding the ACTUAL EMITTED BYTES: enabled=false,
          availability="withheld", withheld_reason="provider_unavailable",
          usable=false, alongside a served model usable=true.
      Five mutations including one that labels a withheld model "serving" and
      fails ALL THREE consumer checks.
    severity: HIGH
    status: fixed + guarded

  - id: SELF-15
    title: my commit blocked another agent's amend, leaving a wrong cross-repo SHA in its message
    detail: |
      helix_llm 29d8c87's body cites the consumer half as "commit be54764
      there". That SHA is helix_agent's PREVIOUS HEAD, not the commit. The real
      one is 8cb9b93a.
      The agent wrote the message before committing the helix_agent side,
      intended to amend, and by then I had committed 15a87d9 on top — so
      amending was unsafe and it correctly left it and reported it instead.
      Recorded here because the ledger is the durable record and a wrong
      cross-repo pointer is a navigation problem for whoever follows the trail.
      CORRECT READING: helix_llm 29d8c87 <-> helix_agent 8cb9b93a.
      This is the cost of a shared checkout with concurrent commits, and it is a
      smaller cost than the alternative — the agent was right not to rewrite
      history under another commit.
    severity: LOW
    status: recorded — correct pairing stated here

  - id: OPEN-30
    title: the retired-host set is exactly two, deliberately
    detail: |
      naming.RetiredHosts = {"localhost", "127.0.0.1"} — the two the documented
      setup actually produced.
      0.0.0.0 and ::1 are DELIBERATELY ABSENT. Widening the set turns more
      identifiers permanently dead, which is the one direction this change
      cannot be walked back from cheaply, so it is a decision to make with
      evidence that those forms were ever published. The comment on RetiredHosts
      says so.
      Worth revisiting only if someone finds a deployment that published them.
    severity: LOW
    status: open — deliberate, revisit only on evidence

  - id: HXA-npm-1-resolved
    title: 27 of 92 npm packages in shipped configs did not exist, and a whole scope was fictional
    detail: |
      Far larger than the single sqlite case that surfaced it. Every npx package
      name in every JSON in the config tree was run through the registry:
      27 OF 92 DO NOT EXIST. The instrument is proven — 65 resolved, so the 404s
      are real answers.
        * THE ENTIRE @anthropic-ai/mcp-server-* SCOPE IS FICTIONAL — 17 of 17
          return 404. The official scope is @modelcontextprotocol/. That is the
          whole MCP block of generated/crush/crush.json.
        * four more under the official scope 404 (server-sqlite, server-docker,
          server-linear, server-sentry);
        * five invented bare names (mcp-server-{asana,atlassian,datadog,
          google-drive,mongodb}).
      THE ROOT IS A CATEGORY ERROR worth remembering: the official sqlite MCP
      server is a PYTHON package on PyPI, invoked `uvx mcp-server-sqlite
      --db-path <p>`, and it is ARCHIVED. It was never published to npm under
      that scope. Someone took the Python/uvx invocation and pasted its
      --db-path flag into an npx call under a scope that does not exist. The
      Node equivalent takes the path POSITIONALLY — so both the name and the
      argument list were wrong, exactly as predicted, and a name-only
      substitution would have produced a differently-broken config.
      Every replacement was confirmed with real registry metadata; no name
      guessed; nothing needed removing. One honest provenance caveat recorded by
      the agent: for atlassian it used npm mcp-atlassian v2.1.0, a DIFFERENT
      project from the Python sooperset/mcp-atlassian the awesome-list cites —
      a substitution, not the upstream, and it said so.
      THE GENERATOR IS THE REAL SOURCE: generate-all-configs.sh emits the
      generated/ tree from inline heredocs, so the heredocs were fixed and the
      tree REGENERATED by running the generator rather than hand-edited.
      THE TEST'S OWN GAP was the reason this survived: it checked a
      HAND-MAINTAINED list of 14 names, missing 78 shipped packages, and
      asserted that @modelcontextprotocol/server-sqlite EXISTS — enshrining the
      defect. It now DERIVES the list from the configs, with a control probe
      that distinguishes a missing package from an unreachable registry (SKIP,
      not a false FAIL).
    severity: HIGH
    status: fixed + guarded

  - id: OPEN-31
    title: the same broken names live in Go source, ~55 sites, invisible to the new guard
    detail: |
      Reported precisely rather than silently absorbed, and left out of scope:
        internal/mcp/config/generator.go, generator_full.go
        internal/mcp/validation/validator.go
        internal/mcp/extended_packages.go
        internal/config/agent_configs.go
        cmd/helixagent/main.go (~3537-4749 carry a dozen broken names)
      Four of them carry @modelcontextprotocol/server-sqlite WITH --db-path —
      both halves of the mistake.
      The new config-derived guard CANNOT see these, because they are Go
      literals rather than JSON. That is the important part: the guard that now
      covers the configs would leave this surface exactly as exposed as before.
      One thing already correct and must stay so: main.go:3492, the shipped
      -generate-opencode-config path, already uses the working package and is
      marked VERIFIED — so the binary's primary output is fine.
      DISPATCHED.
    severity: HIGH
    status: "closed - see OPEN-31-resolved (previously - open — dispatched)"

  - id: HXA-testpath-1-resolved
    title: the spec-kit tests now resolve by marker, and a second failure was hiding behind the first
    detail: |
      Resolution is by MARKER rather than by counting ../: walk up for a
      .gitmodules that DECLARES the submodule, matching the whole `path =` value
      so a sibling like cli_agents/spec-kit-extras cannot satisfy it.
      Why the old lookup failed quietly: helix_agent has its OWN cli_agents/
      directory (analysis docs, no spec-kit), so the bare relative path landed
      somewhere plausible and empty rather than obviously nowhere.
      STANDALONE CHECKOUT PROVEN, not asserted: the agent compiled the test
      binary and ran it from /tmp with no .gitmodules in any ancestor, and got
      SKIP with a message naming what is absent and where it was expected.
      A SECOND, INDEPENDENT FAILURE was hiding behind the path bug: the test
      hardcoded git@github.com:github/spec-kit.git, but the meta-repo pins
      git@github.com:vasic-digital/caf-spec-kit.git — the org's own fork,
      consistent with the whole cli_agents/caf-* family. The test would have
      stayed red after the path fix. Reconciled to read the URL declared for
      THAT submodule path rather than grepping the whole file, which any
      unrelated entry could have satisfied.
      MY READING on the flagged question: pinning the org's fork is plainly
      deliberate — the caf-* naming is the org's convention for its CLI-agent
      forks — so reading the URL from .gitmodules is right and the hardcoded
      upstream was the error.
      AND A LATENT BUG THE OLD CODE WAS MASKING: removing the process-global
      os.Chdir exposed TestGitHubSpecKitAgentRegistry, which reads a
      HELIX_AGENT path, not a meta-repo one. It is now anchored separately.
      A chdir that makes several tests pass by accident is its own hazard.
      Breadth checked: one file, not a pattern. submodule_sync_test.go looks
      similar but enumerates helix_agent's own .gitmodules dynamically and is
      correct as written.
    severity: MEDIUM
    status: fixed + guarded

  - id: T055-ROUND3-NOGO
    title: round 3 is NO-GO — two new findings, and one refutes a premise I endorsed
    detail: |
      All five round-2 items HOLD, verified. Both new findings sit exactly in
      the seams the brief asked the reviewer to attack, which is the argument
      for asking specifically rather than asking generally.
      F-1 (MEDIUM) — THE RETIRED 404 FIRES FOR IDENTIFIERS A DEPLOYMENT IS
      PUBLISHING RIGHT NOW, and the premise it rests on is false twice over:
        * llamacpp.go:84-87 returns the LOOPBACK LITERAL VERBATIM when
          thisMachineName() is "" — which happens whenever os.Hostname() is
          `localhost` or `localhost.localdomain`, the DEFAULT ON MANY VMs AND
          LIVE IMAGES. With the embedded server forcing 127.0.0.1, such a
          machine publishes helixllm-127-0-0-1-… live. That also re-opens the
          two-hosts-collide defect f63b96f fixed, on that class of machine.
        * namesNoMachine accepts localhost.lan, localhost-2 and
          localhost.<anything>, which sanitise into the retired prefix.
      Reproduced across five hosts for the SAME event (model unlisted, provider
      up, host unchanged): 127.0.0.1 / localhost / localhost.lan /
      localhost.localdomain all give 404 with the message "the serving host was
      renamed" — FALSE — while gpu-01 gives 503. The same event maps to a
      permanent or a temporary answer depending only on the machine's hostname.
      The route test's only near-miss case was `localhosting`, which is why it
      survived.
      MY PART IN THIS: I recommended the bounded-set approach to the operator on
      the argument that the retired renderings are "an exactly identifiable set
      the server knows it used to publish and no longer does". The second half
      of that is what is false. I took derive.go's stated premise as established
      rather than checking whether ServingHost could still emit those hosts —
      the same class of error as SELF-12 and OPEN-15, and the third time this
      programme has been bitten by a premise asserted in a comment.
      THE REAL REPAIR is the reviewer's first step: make ServingHost never
      return a loopback literal, so the premise is true BY CONSTRUCTION rather
      than by assertion. The registry-aware check is the belt to that braces.
      F-2 (MEDIUM) — THE TOOLKIT GUARD CAN BE DELETED AND ITS OWN REPO NOTICES
      NOTHING. _CMA_HELIXLLM_SERVING_JQ does two jobs (what gets exported, and
      which hosts count as serving for the retirement gate), and NO test in that
      repo exercises it against a withheld entry. The reviewer removed the
      availability clause on a throwaway copy and the suite returned 71 passed,
      0 failed. With it gone, withheld entries — which now carry
      model_identity — are exported as usable AND count as serving evidence, so
      the data-loss gate reopens through the front door.
      The only assertion on that filter is a COPIED LITERAL in the other repo
      (withheld_models_route_test.go:170). A copy in another repository is not a
      test of the script; it is a second statement of the same fact that drifts
      silently. Third time this programme has met that shape — after the two
      precision lists and the two withheld vocabularies.
      Both dispatched.
    severity: HIGH
    status: "closed - see F-1-resolved and F-2-resolved (previously - NO-GO — both fixes dispatched)"

  - id: NOTE-6
    title: what round 3 confirms about iterating to GO
    detail: |
      Round 1 found 7. Round 2 found 4, THREE OF THEM CREATED BY ROUND 1'S
      FIXES. Round 3 found 2, both new, neither a regression of round 2.
      The count is falling and the character is changing — round 2's were
      consequences of fixes, round 3's are premises that were never checked.
      That is what §11.4.134's iterate-to-a-clean-GO is for, and it would have
      been easy to stop after round 2 with "all findings addressed", which was
      true and would have shipped a 404 that fires for live identifiers.
      Also worth recording: the reviewer verified all five round-2 items rather
      than assuming its own prior findings were fixed, and it separated [run]
      from [read] for every claim.
    severity: INFO
    status: recorded

  - id: F-1-resolved
    title: the retired-404 premise is now true by construction, not by assertion
    detail: |
      Reproduced the five-host table independently before fixing, and confirmed
      both halves of the false premise directly.
      THE PRIMARY REPAIR: ServingHost's `return host` became `return ""`. The
      provider then publishes un-prefixed, exactly as it already does for an
      unparseable base URL and as every remote provider does. That makes
      derive.go's premise true BY CONSTRUCTION. The agent also rewrote that
      comment to say so AND to record that it had previously been an assertion —
      which is the right thing to leave behind, since an asserted premise is how
      this happened.
      WHY IT SURVIVED, and this is worth keeping: the pre-existing
      loopback_serving_host_test.go SKIPS precisely on machines called
      `localhost`. The test skipped exactly the case it existed to check. Add it
      to the PATTERN-1 family — a skip that lands on the interesting case is
      indistinguishable from a pass until someone asks what it skipped.
      The agent added a `machineName` seam because the branch is otherwise
      unreachable without renaming the host (`unshare -Uru` is denied here), and
      captured the RED baseline against unchanged behaviour first.
      IT DECLINED TO WIDEN namesNoMachine, correctly: localhost.lan and
      localhost-2 name real, distinct machines, and blanking them would lose a
      genuine host. Their collision is a retired-PREFIX ambiguity, which is the
      registry step's job.
      IT IMPROVED ON THE REVIEWER'S PLAN, which I had passed through unchanged.
      The reviewer said "no live identity has a sanitised host BEGINNING WITH
      the retired rendering". On a box now called localhost.lan that predicate
      would make a pre-rename helixllm-localhost-… NON-retired too, losing the
      round-2 404. Matching the live host's FULL prefix keeps
      helixllm-localhost-lan-… live AND helixllm-localhost-… retired. Both
      asserted.
      AND IT MEASURED THE OPEN QUESTION RATHER THAN REASONING ABOUT IT. Asked
      whether the chained-older-instance case was covered, it probed: cached
      list -> 503 (covered), but EMPTY list -> still 404. The registry-aware fix
      covered it only partly, because the live-host set was built from
      registered IDENTITIES (one per model) while the question is about the
      HOST. It fixed that rather than justifying it (Registry.NoteLiveHost), and
      noted the same residual hits any local runtime between start-up and first
      model load.
      Six mutations, each diff-verified. RED polarity re-confirmed after
      `go clean -testcache`. 53 packages ok, zero FAIL.
    severity: HIGH
    status: fixed + guarded

  - id: NOTE-7
    title: a skip that lands on the interesting case
    detail: |
      loopback_serving_host_test.go SKIPS when the machine running it is called
      `localhost` — which is the exact condition under which the defect it was
      written to catch occurs.
      It is not a fail-open skip in the usual sense: it names its reason and it
      is honest about not running. But its reason and the defect's precondition
      are the same fact, so the test could never see the bug, and a green suite
      never showed a gap.
      Worth carrying as a check on future skips: when you write one, ask whether
      its condition is correlated with the failure you are guarding against. If
      it is, the skip is not a neutral absence of information — it is a blind
      spot aimed at the target.
    severity: INFO
    status: recorded

  - id: F-2-resolved
    title: the toolkit guard is now testable at home, and the drift is stated rather than hidden
    detail: |
      The agent VERIFIED the reviewer's claim rather than assuming it: on a
      throwaway copy, baseline 71 passed / 0 failed, then removed the
      availability clause (diff-verified) and got 71 passed / 0 failed —
      byte-identical. Every pre-existing case exercised only the model_identity
      clause, so nothing in that repo could tell the two versions apart.
      FIXED with a fixture carrying availability:"withheld" and three assertion
      groups, including the one that matters most: a SERVING SIBLING on the same
      host is still exported and can still license retirement — so "it passed
      because the filter rejects everything" cannot satisfy the suite. Two
      mutations: deleting the clause fails 12, rejecting everything fails 34
      including the selectivity control.
      THE TEST NOW READS THE FILTER OUT OF THE SCRIPT AT RUN TIME rather than
      restating it, so the local assertion cannot drift from the local
      definition, and renaming the variable fails loudly instead of vacuously.
      IT DECLINED TO REACH INTO THE SIBLING CHECKOUT, with the right reason: a
      cross-repo assertion would hardcode another project's path AND would
      SKIP wherever that checkout is absent — "a guard that vanishes when absent
      is the same class of problem as the one being fixed". That is the same
      lesson as NOTE-7, arrived at independently an hour later.
      I CLOSED THE REMAINING THREAD ON THE HELIX_LLM SIDE by changing what that
      test asserts. Its copied filter is now explicitly a SNAPSHOT that states
      what it cannot prove, and the load-bearing assertion became the invariant
      the PRODUCER controls: a withheld entry must carry an EXPLICIT
      availability, never an absent one. Every consumer filter defaults absent
      to serving, so omitting the marker is not a weaker signal — it is the
      OPPOSITE signal. Mutation: stamping "" fails the new invariant AND makes
      the toolkit filter SELECT the withheld model. Three assertions fall
      together.
    severity: HIGH
    status: fixed + guarded

  - id: SELF-16
    title: a restore that applied, followed by a test that reported the mutated result
    detail: |
      During the mutation above: I restored brain.go, `diff` confirmed it
      byte-identical, and the immediately-following `go test -count=1` reported
      FAIL. Re-running after `go clean -testcache` gave ok.
      So the restore HAD applied and the test still reported the pre-restore
      outcome. This is the INVERSE of the hazard already recorded as SELF-3 (a
      mutation that silently fails to apply and reports "ok"), and it is just as
      misleading in the other direction: it would send someone hunting a fix
      that was never broken.
      `-count=1` disables the result cache but does not settle this; the F-1
      agent independently hit it and used `go clean -testcache` before trusting
      its RED-polarity check.
      PRACTICAL RULE for the paired-mutation discipline everyone here is using:
      `diff` proves the FILE changed. It does not prove the TEST BINARY did. On
      restore, clean the test cache before believing a PASS or a FAIL.
    severity: MEDIUM
    status: recorded — method note

  - id: OPEN-31-resolved
    title: 113 broken package sites in Go source, not the ~55 reported
    detail: |
      The sibling's report gave ~55 sites in six files. A go/ast scan found 113
      in SEVEN, and the two it missed are instructive:
        * five more in internal/config/agent_configs.go, hidden behind a shape a
          `"npx","-y","<pkg>"` text scan cannot see — Command: "npx" and
          Args: []string{...} are SEPARATE STRUCT FIELDS. That shape is exactly
          why the new guard parses the AST rather than grepping.
        * one in challenges/codebase/go_files/kilocode_generator/, not in the
          report at all.
      47 of 123 distinct names 404'd, control-probed (npm and
      @modelcontextprotocol/server-filesystem resolved 200, so the 404s are
      answers). 3 were deliberate test fixtures; 44 production.
      EACH REPLACEMENT VERIFIED AGAINST THE REGISTRY *AND* ITS CLAIMED UPSTREAM
      REPO, on the principle that name similarity is not provenance. Linear
      looked salvageable — linear-mcp-server claims repo
      modelcontextprotocol/linear-server — but that repo is 404, so the claim is
      unverifiable and the entry was removed rather than trusted.
      35 NAMES REMOVED RATHER THAN GUESSED. Community packages exist for many,
      and the agent declined to use them: "making a stranger's package a shipped
      default is a supply-chain decision, not a name fix". Flagged, not made.
      GENERATORS AND CONFIGS DID DISAGREE, and nothing compared them: the JSON
      said `mcp-server-sqlite-npx <path>` while the generator said
      `@modelcontextprotocol/server-sqlite --db-path <path>` — which config a
      user got depended on HOW THEY GENERATED IT. The new test compares FULL
      ARGV, so a right name with a wrong flag still fails, and it refuses to
      pass if zero servers overlapped.
      GATE RECONCILIATION WITH THE BEST OBSERVATION OF THE DAY: an assertion
      `len >= 30` was replaced with distinctness and well-formedness checks,
      because A SIZE FLOOR IS SATISFIABLE BY ADDING FICTIONAL ENTRIES — which is
      exactly how these got in. The guard was rewarding the defect.
    severity: HIGH
    status: fixed + guarded

  - id: SECURITY-6
    title: four config entries point at npm security-holding packages
    detail: |
      Found by the agent and VERIFIED INDEPENDENTLY by me against the live
      registry:
        mcp-server-figma      0.0.1-security  "security holding package"
        mcp-server-postgres   0.0.1-security  "security holding package"
        mcp-server-redis      0.0.1-security  "security holding package"
        mcp-server-supabase   0.0.1-security  "security holding package"
      Control: @modelcontextprotocol/server-everything is 2026.8.31, so the
      lookups work and these versions are real answers.
      A security-holding package is what npm publishes after SEIZING A NAME —
      typically because it was used maliciously or squatted. These names EXIST,
      so every existence check passes, and they install nothing.
      Two consequences worth separating:
        * functionally, `npx -y mcp-server-figma` gets a dead stub, so the
          integration silently does nothing;
        * historically, these are names npm had to quarantine, which is a
          different and more uncomfortable fact than "the package is missing".
      The new registry guard CANNOT see these — it checks existence, and they
      exist. A version-shape check (reject 0.0.1-security) would catch them.
      NOT FIXED — out of the dispatched scope, and recorded rather than absorbed.
    severity: HIGH
    status: open

  - id: OPEN-32
    title: the hawkscan post-commit hook reported a commit from a different repository
    detail: |
      Reported by the agent: the hook announced SHA 1f97cf1 in its session,
      including once when it had committed nothing, and that SHA does not match
      its actual commit (ce9c4eb9).
      1f97cf1 is MY commit, in helix_llm. The agent was working in helix_agent.
      So the hook attributed a commit from one repository to a session working
      in another. If its scans are keyed on that SHA, they are being associated
      with the wrong change.
      Environmental rather than a defect in this feature, and not investigated
      further — recorded because a tool that mis-attributes evidence is worth
      knowing about before anyone relies on its output.
    severity: LOW
    status: open — environmental

  - id: OPEN-25-resolved
    title: the device axis now applies when the LANE binds VRAM, not when the entry flags it
    detail: |
      ESTABLISHED FIRST, with citations, rather than assumed:
      `requires_accelerator: false` means "an accelerator is not REQUIRED"
      (entry.go:343-345 — "mandatory for acceptable service, as opposed to
      merely faster"), not "this never touches a GPU". And the figures on those
      entries are RAM-SOURCED — qwen3-0.6b records '"~2 GB, usable" (CPU-only)';
      qwen3.6-35b-a3b's summary says "a no-GPU host with generous RAM serves it".
      So the flags and figures are CORRECT AS RECORDED. Neither was changed.
      THE ASYMMETRY, measured: video.yaml states its figure IS "the value
      actually passed to vrambroker.Acquire(ClassVideo)". text.yaml makes no
      such claim — grep for vrambroker/vram in it returns ZERO matches. Its
      figures are RAM figures with no VRAM counterpart anywhere.
      TRACE: recorded as host RAM -> checked by memoryAxis against
      p.MemoryAvailable (host RAM) -> spent by agentgen-boot at
      broker.Acquire(ClassAgent) against VRAM. The device axis was applied only
      `if e.RequiresAccelerator`, so for these five it never ran.
      LIVE, reproduced against the SHIPPED catalogue on a 44 GiB-RAM host with a
      4 GiB card: four entries offered beyond the card, up to 24576 MiB.
      Reachable three ordinary ways — --pin, AGENTGEN_FORBID_MODELS, or simply
      the small model's weights being absent.
      THE CATCH WORTH KEEPING: one of those figures is 19968 MiB — THE SAME
      19.5 GiB that agentgen-boot's own doc comment cites as the forensic case
      it was written to close. Its claim that the lane "cannot offer a model it
      will then refuse to start" held for exactly one of six live text entries.
      A comment describing a fix, sitting above code where the fix did not
      reach.
      SHAPE CHOSEN: the device axis applies when the LANE binds VRAM, not when
      the ENTRY flags it. Alternatives rejected with reasons — flipping flags
      would contradict the catalogue's own sourced provenance, and splitting the
      schema into RAM and VRAM figures needs measurements that exist nowhere in
      the research corpus and could not be taken here. No figure invented.
      HONEST BOUNDARY, in the code and under test: on a host where NO
      accelerator was measured the answer is unchanged. Selection has nothing to
      check and will not invent a refusal; the broker's fail-closed path stands
      there instead.
      54 packages ok — the delta from 53 is exactly internal/laneboot, which had
      no test files before, verified by diffing the two ok-lists.
    severity: MEDIUM
    status: fixed + guarded

  - id: OPEN-33
    title: the vision lane has the same RAM-vs-VRAM question, unexamined
    detail: |
      vision_image.yaml carries five requires_accelerator: false entries whose
      figures were NOT re-examined for whether they are RAM- or VRAM-sourced.
      The selection fix reaches them automatically through laneboot, so they can
      no longer be offered beyond the card — but whether their recorded numbers
      mean what text.yaml's mean is a separate question, and the answer
      determines whether anything further is needed.
      Same class, own pass.
    severity: MEDIUM
    status: open

  - id: SELF-17
    title: this ledger's own status field was stale for 21 findings
    detail: |
      I recorded resolutions as NEW entries (CRITICAL-7-resolved, and so on)
      rather than updating the original's status. So the original kept saying
      "open" while a later entry said it was fixed, and anyone querying the
      ledger for open work got 40 items when the real number was 21.
      That is the same defect class this session has been finding everywhere
      else: a status field that stops tracking reality, a comment describing
      removed behaviour, a test asserting a fact that has moved. The ledger
      built to catch it had it.
      CORRECTED: 21 entries now point at what closed them, carrying their
      previous status forward in the text rather than discarding it. Only
      closures I could establish from this session's evidence were touched;
      OPEN-1 through OPEN-5, EX-28 and CRITICAL-2 predate my visibility and were
      left alone rather than guessed at.
      Two method notes from doing it. The first attempt wrote `(was: <old>)`
      into the status, and a colon-space inside an unquoted YAML scalar reads as
      a mapping — the whole file stopped parsing. THE GATE CAUGHT IT AND
      REVERTED, which is the discipline from SELF-13 working: this time the
      parse check was a condition on the write, not a statement before it.
    severity: MEDIUM
    status: corrected

  - id: OPEN-29-resolved
    title: the config endpoint no longer hands back an empty document that erases a user's models
    detail: |
      REPRODUCED, captured: a backend up but loading answers /health with 503, so
      it reports itself unavailable while still listing its configured models.
      The user's file went in holding two working models under our key and came
      back with `models: {}` — under a 200, with nothing in the returned
      document saying why. Their theme and their own providers survived; their
      two selectable models did not.
      AND THE REASONS WERE ALREADY THERE: `provider-unavailable`, in a sibling
      `withheld` field the merge never consulted.
      THE REVIEWER'S FRAMING WAS CORRECTED, and the correction narrowed the fix:
      "wholesale" overstates it. MergeOpenCode is ADDITIVE BY KEY at both levels
      — the operator's own providers and every other top-level field are copied
      through byte-for-byte. What it replaces totally is the entry under OUR
      key, which is enough to cause the loss but means the whole document was
      never at risk.
      CHOSE THE THIRD BEHAVIOUR: the endpoint now tells three states apart
      instead of collapsing two — serving (200, unchanged), offering models but
      able to serve none (503 naming host, count and the distinct withheld
      reasons), offering nothing at all (404, as before).
      The replacement rule had assumed withheld meant WITHDRAWN. Since withheld
      options now carry reasons, that assumption is unnecessary: an empty roster
      backed by reasons is the export declining to describe the instance, not a
      description of an instance with no models.
      REFUSED AT TWO INDEPENDENT SEAMS, because either alone leaves a way
      through — MergeOpenCode returns ErrNothingServable so no in-process caller
      can erase an entry, and both endpoints refuse before producing anything so
      a caller doing GET + jq never receives the dangerous document. M2 and M3
      prove neither seam masks the other.
      THE TOOLKIT'S PRICE IS NOT PAID HERE, and this is asserted rather than
      assumed (M4): the toolkit fix accepted permanent non-convergence as the
      cost of safety. Here the refusal fires on "has offers, can serve none" and
      never on "has nothing", so a genuinely-empty instance still merges through
      and clears whatever was stale. The withheld reasons are what make the two
      emptinesses distinguishable.
      DELIBERATELY NOT FIXED, and stated: the partial case (some servable, some
      withheld) still replaces, so a user can temporarily lose options that have
      not come back — a degraded entry, not an unusable one. Separating
      transient from permanent withholding needs a reason taxonomy that does not
      exist, since withheldReason passes an instance's own string through and
      the set is open.
      Vacuity guards included, so "it refuses" is not true of something that
      refuses everything.
    severity: HIGH
    status: fixed + guarded

  - id: OPEN-34
    title: a cmd/helixllm test fails intermittently in parallel runs — reported, isolated, mechanism unconfirmed
    detail: |
      TestChatCompletions_HostWithNoListedModelsIsStillLive/host_127.0.0.1
      fails intermittently under parallel full-suite runs.
      ISOLATED PROPERLY by the reporting agent rather than assumed: it
      reproduces with that agent's change entirely removed from the tree, passes
      3/3 alone and 3/3 for its own package, and does not appear under `-p 1`.
      Mechanism marked UNCONFIRMED — the provider's /health probe has a 2s
      timeout and the verdict depends on the host being seen live, which would
      make it load-sensitive.
      WORTH A SECOND LOOK RATHER THAN THE LABEL, for a specific reason: the F-1
      fix made identifier resolution registry-only with NO network on the
      request path. If this test's verdict still depends on a /health probe
      being observed, that is either a path that still probes or a test asserting
      through one — and today's LSP case is the standing reminder that "flaky"
      and "real race" are indistinguishable from the failure rate alone.
      Apply the discriminator: does the scenario contain LEGITIMATE timing? A
      live-host probe does, unlike a context cancelled before the call. So a
      genuine flake is plausible here in a way it was not there — but plausible
      is not measured.
    severity: LOW
    status: open — worth measuring before labelling
  - id: BOOT-1
    title: the ./helix facade refused to run on podman, the runtime this project mandates
    detail: |
      check_docker() required the literal `docker` binary and check_docker_compose
      required docker-compose. This host has podman + podman-compose and no docker
      at all, which is what CONST 11.4.161 and CLAUDE.md require. Every facade
      command exited 1 with "Docker is not installed or not in PATH", so the
      sanctioned boot path was dead on the sanctioned runtime.
      The script bound to a tool NAME rather than resolving the capability, and
      repeated that binding at seven call sites.
      FIXED d5158bd3 by resolving once into CONTAINER_CLI (podman preferred,
      docker still a fallback) and routing ps/exec/info through it, plus a
      matching compose resolver. ./helix status now exits 0.
    severity: HIGH
    status: fixed
  - id: BOOT-2
    title: three Dockerfiles pinned Go 1.24 while the module they build requires 1.26
    detail: |
      Dockerfile, Dockerfile.worker and Dockerfile.test copy helix_code/go.mod
      (which declares go 1.26) into a golang:1.24-alpine builder under
      GOTOOLCHAIN=local, so `go mod download` failed with
        go: go.mod requires go >= 1.26 (running go 1.24.13; GOTOOLCHAIN=local)
      docker/Dockerfile.helixqa was already on 1.26 — these three were stragglers,
      which is why the mismatch survived.
      FIXED d5158bd3.
    severity: HIGH
    status: fixed
  - id: BOOT-3
    title: a hardcoded grantee in test/init.sql aborted postgres startup in the deployment stack
    detail: |
      helix_code/test/init.sql ended with GRANT ... TO helixcode. That file is
      mounted into /docker-entrypoint-initdb.d by TWO compose files that create
      DIFFERENT roles — docker-compose.test.yml uses POSTGRES_USER helixcode,
      docker-compose.helix.yml uses helix. In the deployment stack the role does
      not exist, so init aborted and postgres exited 3 before accepting any
      connection.
      The block's own comment already said "Grant permissions to the configured
      user" — the code contradicted the comment. Same class as BOOT-1, a
      hardcoded name where the capability should be resolved.
      FIXED d5158bd3 via current_user in a DO block with %I quoting. Verified on
      a clean volume in BOTH real configurations - deployment (helix + both
      scripts) and test (helixcode + test script only) each reach Up with 0 init
      ERRORs and 15 tables. Before the fix the deployment case was Exited (3).
      NOTE postgres-init.sql hardcodes `helix` symmetrically, but it is only ever
      mounted alongside POSTGRES_USER=helix, so it is latent, not live. Left
      alone deliberately rather than widening the change.
    severity: HIGH
    status: fixed
  - id: BOOT-4
    title: the container build still cannot resolve the 43 replace directives in helix_code go.mod
    detail: |
      With BOOT-2 fixed the build gets further and then fails on
        go: dev.helix.agent (replaced by ../submodules/helix_agent) reading
            /submodules/helix_agent/go.mod - no such file or directory
      helix_code/go.mod carries 43 `replace` directives pointing at
      ../submodules/*, i.e. OUTSIDE the helix_code/ directory the Dockerfile
      copies. All 43 targets exist locally and total 4.2 GB, dominated by
      helix_qa (2.5 GB) and helix_agent (2.0 GB), most of which is vendored
      third-party trees such as signoz and chroma.
      Fixing it needs the image to mirror the repo layout plus a carefully
      scoped .dockerignore so the build context is not multi-GB. That is its own
      piece of work and was NOT attempted here.
      WORKAROUND IN USE - the native path, which Rule 4 names first
      (make build -> ./bin/<app>). The server auto-boots its own postgres and
      redis per CONST 11.4.76, so the native path needs no compose at all.
    severity: MEDIUM
    status: open — needs a scoped .dockerignore and a layout change
  - id: TEST-1
    title: the perf-scenario stability test measured one cold-cache run and called the harness noisy
    detail: |
      TestRunner_StableAcrossThreeRuns failed every full-suite run with
      CV=159% against its own 35% bound. The verdict was wrong. The test
      generates a 600-file fixture into a fresh t.TempDir() then measures
      immediately, so run 0 pays one-time filesystem cache population.
      REPRODUCED DETERMINISTICALLY - run 0 is the outlier in 3 of 3 trials,
      never runs 1 or 2, which is what distinguishes a cold cache from load
      jitter: [2079.6, 38.6, 43.0] [2114.6, 40.0, 40.6] [1989.6, 43.9, 40.6].
      Runs 1 and 2 alone give CV of 7.6%, 1.0% and 5.5% — single digits, exactly
      what the test's own comment predicts.
      FIXED 00b28c8e by discarding one real warmup RunAll. The 35% threshold was
      NOT loosened; weakening the assertion would have been the CONST 11.4.120
      forbidden fix.
      KNOWN REMAINING - the test is genuinely load-sensitive. It passed 3/3 on a
      quiet host and measured CV=37.2% while a container build had the host at
      load 28 on 16 CPUs. It wants an uncontended host, which is a property of
      timing measurement, not something to paper over.
    severity: MEDIUM
    status: fixed
  - id: TEST-2
    title: the whole tests/memory suite silently skips unless a server is already running
    detail: |
      getTestBaseURL() defaults to http://localhost:8080 and every test in
      tests/memory begins with a /health probe followed by
        t.Skip("Server not available, skipping memory leak test")
      marked SKIP-OK #legacy-untriaged. With no server up the package reports
      "ok ... 0.077s" — a green result that exercised nothing.
      MEASURED THIS SESSION - with a server running the same package took 23s
      and TestMemory_LeakDetection_ConcurrentRequests produced a rising live-heap
      signal (signed-R2 0.6565, slope 36187 B/sample, rise 51.57% of mean).
      That signal is NOT yet called a defect. It was taken on a host at load 44
      with four suites running, the absolute heap is under 1 MB, and the shape is
      flat for 7 waves then a late jump (539504 548112 555008 555936 568304
      566512 568704 653600 852112 907656), which is as consistent with HTTP
      transport idle-pool growth or GC lag under scheduling pressure as with a
      leak. It needs a clean uncontended re-run before anyone labels it.
      THE COVERAGE GAP IS THE REAL FINDING - a suite that reports ok while
      testing nothing is the CONST 11.4.238 failure mode, and it is why nobody
      had seen this signal before.
      RESOLVED ON A QUIET HOST 2026-09-03 14:00. Re-run at load ~7 with the
      server up: the test RAN and PASSED in 0.08s.
        quiet   [747112 722320 722120 739784 750480 786376 833392 872328
                 815680 801504]  signed-R2 0.6332  rise 15.48% of mean  PASS
        loaded  [539504 ... 653600 852112 907656]
                                 signed-R2 0.6565  rise 51.57% of mean  FAIL
      The verdict needs BOTH R2 >= 0.50 AND rise >= 25% of mean. On a quiet host
      the trend SHAPE survives (R2 0.63) but the MAGNITUDE collapses to 15.5%
      and the tail actually falls (872328 -> 815680 -> 801504) instead of
      climbing. So the leak signal does NOT reproduce and the 51.57% rise was a
      load artifact — GC lag and scheduling pressure at load 44, not retained
      memory. The two-part guard did exactly its job, separating trend-shaped
      noise from material growth.
      WHAT REMAINS OPEN is only the coverage gap: the suite still reports
      "ok ... 0.077s" having exercised nothing whenever no server is up, which
      is how this went unseen. The leak half is closed.
    severity: MEDIUM
    status: leak signal refuted on a quiet host; the skip-when-absent coverage gap stays open
  - id: METHOD-1
    title: running four test suites concurrently manufactured false failures in three of them
    detail: |
      Recorded as a lesson, not a product defect. Dispatching helix_llm,
      helix_code, helix_agent and claude_toolkit suites at once took the host to
      load 60 on 16 CPUs and produced three distinct false-failure modes:
        - timing: perf scenarios failed again despite the TEST-1 fix, and
          helix_llm's TestBaselineDiscardsConnectionWarmup failed because its
          inner step self-skipped on load detection ("host too loaded to measure
          concurrency ... re-run on a quieter host") while the wrapper expected
          "passed"
        - port exhaustion: helix_agent's TestAdapter_HealthCheckTCP_Success got
          "listen tcp 127.0.0.1:0 bind address already in use" — binding to port
          ZERO, i.e. ephemeral exhaustion
        - live-server interference: a running server on 8080 changed outcomes in
          BOTH directions — it woke the vacuous tests/memory suite (TEST-2) and
          it broke a challenge asserting that a CLOSED port must report SKIPPED,
          because the port was no longer closed
      This is CONST 11.4.119 (single resource owner) applied to CPU, ephemeral
      ports and localhost port space. Timing and port-binding suites need
      exclusive host ownership; a pass/fail verdict taken under self-inflicted
      contention is not evidence either way.
    severity: LOW
    status: recorded — method note
  - id: OBS-1
    title: GET /api/v1/auth/me is registered but answers 404
    detail: |
      The route appears in the router registrations scraped from
      internal/server, and register/login/refresh all work end-to-end (register
      201 with a persisted UUID, login 200 with a 287-char JWT, and that token
      opens /api/v1/tasks, /api/v1/workers and /api/v1/system/stats which are 401
      without it). But GET /api/v1/auth/me returns 404 with a valid bearer token.
      Not investigated. Recorded so it is not mistaken for an auth failure.
    severity: LOW
    status: open — not investigated
  - id: TOOLKIT-1
    title: a Claude Toolkit test asserts against the operator's live HOME instead of a sandbox
    detail: |
      scripts/tests/test_claude.sh line 7 hardcodes
        ALIAS_FILE="$HOME/.local/share/claude-multi-account/aliases.sh"
      and the file sources only lib/assert.sh, never lib/sandbox.sh — even
      though lib/sandbox.sh exists and exports a sandboxed ALIAS_FILE for
      exactly this purpose.
      It therefore asserts on the OPERATOR'S live machine state. Measured:
        poe       present, uses cma_run_provider   PASS
        deepseek  present, uses cma_run_provider   PASS
        xiaomi    ABSENT from the live alias file  FAIL
      So "✗ 1 failed, 10 passed" reflects the operator's machine, not the code.
      REFINED after watching a live `claude-providers sync` (the first reading
      was close but not right): xiaomi is NOT unconfigured — a xiaomi key var
      EXISTS and resolves to a provider, but the sync reports
        provider 'xiaomi' FAILED verification — alias NOT activated
      so no alias is written and the test cannot find one. `poe` FAILED
      verification in the same run yet the test found `poe` present, because
      aliases from earlier syncs persist in the file until pruned. The assertion
      is therefore reading a mixture of current and historical state.
      That makes it worse than "operator-specific", not better: the test can
      fail for a transient upstream outage, and can PASS on a stale alias for a
      provider that no longer verifies. Neither outcome says anything about the
      code under test.
      SAME CLASS as helix_agent 913d1f02 "validate the OpenCode config this repo
      ships, not the operator's" — the pattern recurs across repos.
      Fix direction: drive the assertions from a sandbox alias file generated
      from a known provider set, per lib/sandbox.sh. NOT fixed here — it is a
      test-harness change on an unmerged branch and deserves its own work.
    severity: LOW
    status: open — test reads operator state; needs sandboxing
  - id: TOOLKIT-2
    title: eleven providers-semantic assertions fail together, one root cause not eleven defects
    detail: |
      scripts/tests/test_providers.sh, the providers-semantic block. Every
      failure has the SAME shape — the judge returns skip / exit 2 where a real
      verdict was expected:
        pass -> verified     want=verified   got=skip   (exit want=0 got=2)
        fail -> unverified   want=unverified got=skip   (exit want=1 got=2)
        same-endpoint judge independence WARNING   want=0 got=1
      and every assertion that EXPECTS skip passes:
        no key -> skip  PASS      Go exit 3 (infra) -> skip  PASS
        skip -> adapter exit 2  PASS
      Two more look for evidence at the REAL repo path while the run was
      sandboxed:
        missing /home/milosvasic/Projects/claude_toolkit/.local-cache/semantic-last.json
        missing /tmp/cma-test.NchYzI/scv-argv.txt
      Note the driver binary DID build and cache in the sandbox (that assertion
      passes), so this is not a build failure.
      READ AS ONE CAUSE - the semantic judge cannot actually run in this
      environment, so it skips, and every downstream assertion that needed a
      real verdict or an emitted artefact fails behind it. Counting it as 11
      defects would overstate it.
      NOT diagnosed further and NOT fixed - it is on the unmerged branch
      fix/helixllm-export-review-findings and wants its own focused look.
    severity: MEDIUM
    status: open — one root cause, needs diagnosis on a host where the judge can run
  - id: BOOT-5
    title: helixllm ran for 16 hours on a stale binary still serving the data-null defect
    detail: |
      The live helixllm (pid 30808, up 16h) was answering
        {"object":"list","data":null}
      on https://127.0.0.1:8443/v1/models — the exact defect ab34fa8 ("a listing
      of nothing is an empty list, not null") had ALREADY fixed in source.
      Binary mtime Sep 2 22:13, process start Sep 2 22:13:40, HEAD Sep 3 12:48.
      The source was green and the running system was wrong: the §11.4.108
      SOURCE -> RUNTIME gap in its plainest form. No gate catches it, because
      every gate reads the source.
      FIXED by rebuild + restart; it now returns a real list.
      LESSON worth keeping: before trusting ANY live probe, compare
      `ps -o lstart= -p <pid>` against `git log -1`. A 200 from a stale process
      is evidence about the stale process, nothing more.
    severity: MEDIUM
    status: fixed by rebuild+restart; the general trap is documented in RESUME 2b-2
  - id: BOOT-6
    title: helixagent boots its whole dependency stack then stops on a missing JWT_SECRET
    detail: |
      A real run gets remarkably far and then refuses:
        Container adapter initialized via Containers module   runtime=podman
        compose up via podman-compose: postgres, redis, chromadb
        Service health check passed (chromadb, postgresql, redis)
        Skills loaded successfully  categories=16 total=1163 triggers=4236
        MCP servers starting in background (32 servers on ports 9101-9999)
        Liveness probe listening (status=starting) addr=":8111"
        ...
        Failed to initialize auth middleware: JWT secret key is required
      So the containerised boot chain (§11.4.76 + §11.4.161 rootless podman)
      works end to end; the only blocker is one unset variable. `.env` is
      gitignored there (.gitignore lines 3/38/51) so it is safe to place.
      NOT COMPLETED THIS SESSION - the safety classifier for shell writes was
      unavailable for an extended window, and a JWT secret must come from
      `openssl rand -hex 32`, never from a hand-authored string. Deliberately
      left undone rather than weakened.
      VERIFIED 2026-09-03 — the blocker is gone. helixagent is serving on
      :7061 (pid 315821, `active running`), a JWT secret is configured in
      BOTH `.env` files the unit reads (`helixagent.service:85-86`
      EnvironmentFile lines; key names only — HELIX_AUTH_JWT_SECRET and
      JWT_SECRET, both non-empty), and a grep of the whole 9814-line journal
      for `JWT|auth middleware` returns ZERO hits. `GET /v1/models` answers
      200 with 5 model ids. The "one command away" was run.
    severity: MEDIUM
    status: closed - the JWT secret is configured and helixagent serves on 7061; zero JWT errors in the journal, ledger corrected
  - id: BOOT-7
    title: helixagent discovers zero providers because the operator keys live where it does not look
    detail: |
      Its LLMsVerifier startup pipeline runs to completion and reports
        Provider discovery order determined  total=44
        Discovered providers  count=0        Verified providers  verified=0
        Failed to select complete debate team: no verified providers available
      It checks 44 candidate environment variables and finds none set. The
      operator's keys live in ~/api_keys.sh, which claude_toolkit reads and
      helixagent does not.
      This is why "expose HelixAgent models to Claude Code" currently yields
      nothing from the HelixAgent side, independently of BOOT-6.
      DECISION NEEDED, not a defect: either feed helixagent the same key source
      the toolkit uses, or accept that its model surface is HelixLLM-only.
      VERIFIED 2026-09-03 — the decision was taken and implemented; the
      finding is stale. The live boot logs
      `[exec-with-api-keys] sourced /home/milosvasic/api_keys.sh (+83
      exported vars)`, then `Discovered providers count=32` and
      `Verified providers verified=32` (was count=0 / verified=0), and there
      is no `Failed to select complete debate team` in the current boot.
      The key source WAS fed to helixagent — see ROOT-2 for the mechanism.
    severity: LOW
    status: closed - helixagent was given the operator key file and now discovers 32 / verifies 32 providers, ledger corrected
  - id: TOOLKIT-3
    title: helixllm-export reports a healthy host as unreachable when the cert is self-signed
    detail: |
      Two independent gotchas, both of which make a WORKING host look dead:
      (1) `_cma_helixllm_fetch_models` appends `/models` to the given base, so
          `--host https://host:8443` requests `/models` and misses `/v1/models`.
          The base must already contain `/v1`.
      (2) It fetches with a bare `curl -sf` carrying no `--cacert`. Against
          helixllm's self-signed cert that is **curl exit 60**, and the only
          user-visible message is
            "host ... did not answer with a model listing"
          which reads as "the host is down" when the host is answering fine.
      NO CODE CHANGE NEEDED - `CURL_CA_BUNDLE=<cert.pem>` makes curl verify;
      the generated cert covers localhost, 127.0.0.1 and 192.168.0.241.
      Worth considering: the message conflates "unreachable" with "reachable but
      untrusted", and distinguishing them would have saved the whole diagnosis.
      With both fixed the chain works, and correctly exports NOTHING, because
      the single catalogued model is withheld (see BOOT-8).
    severity: LOW
    status: open — works around cleanly; the misleading message is the real finding
  - id: BOOT-8
    title: the local-model default names a model this host physically cannot run
    detail: |
      helixllm catalogues exactly one model,
        helixllm/anton/Llama-3.1-70B-Instruct-Q4_K_M
        availability: withheld   withheld_reason: provider_unavailable
      That withholding is CORRECT: `anton` IS this host (hostname = anton), no
      GGUF weights exist on it, and no llama.cpp server is running. The toolkit
      then rightly declines to export it.
      The finding is the DEFAULT itself. `HELIX_LLM_LOCAL_MODEL` defaults to
      that 70B Q4_K_M (internal/shared/config/config.go:47), needing ~40 GB, on
      a host with 30 GB RAM (14 free) and an RTX 3060 / 12 GB VRAM. The machine
      cannot run its own configured default — precisely the offer-what-cannot-
      start mismatch feature 002's capability-driven selection exists to
      prevent, sitting in the default value rather than in the selector.
      TO MAKE A MODEL GENUINELY USABLE: place a GGUF this host can run (an 8B
      Q4_K_M at ~4.9 GB fits 12 GB VRAM comfortably), point
      HELIX_LLM_LOCAL_MODEL at it, run llama-server (/usr/bin/llama-server is
      installed; ollama is NOT), then re-run the export.
      NOT DONE UNILATERALLY - which model to download is the operator's call and
      it is a multi-GB fetch.
      NARROWED 2026-09-03 — the operator decision was taken and this row is
      now stale in every part EXCEPT its original point. Stale: GGUFs exist
      (`~/models/qwen2.5-coder-{1.5b,3b}-instruct-q4_k_m.gguf`), llama-server
      IS running on :18434 serving the 3B (pid 3662629, `--ctx-size 4096
      --n-gpu-layers 0`), helixllm now reports `availability: serving` (was
      `withheld / provider_unavailable`), and `.env:89` sets
      HELIX_LLM_LOCAL_MODEL=qwen2.5-coder-3b-instruct-q4_k_m.
      STILL TRUE, and the whole of what remains open: the hardcoded default
      at `submodules/helix_llm/internal/shared/config/config.go:47` is still
      `default:"Llama-3.1-70B-Instruct-Q4_K_M"` — ~40 GB on a 30 GB / 12 GB
      VRAM host. It bites only a fresh checkout with no `.env`, which is
      exactly the offer-what-cannot-start mismatch feature 002 exists to
      prevent, sitting in a default value.
    severity: MEDIUM
    status: open — narrowed to the hardcoded 70B default in config.go 47; the host now serves a model it can run
  - id: BOOT-9
    title: all three services agree — the plumbing works and there is no local inference backend
    detail: |
      Probed end to end on 2026-09-03 with all three services up. Each reports
      the SAME underlying gap in its own honest way, which is the useful part:
        helix_code   8 providers listed, but POST /api/v1/llm/generate returns
                     502 with
                       dial tcp 127.0.0.1:11434: connect: connection refused
                       provider: ollama
                     (the full auth round trip works first — register 201,
                     login 200, 269-char JWT, and the 401 without a token)
        helixllm     1 catalogued model, availability withheld,
                     withheld_reason provider_unavailable
        helixagent   5 model names exposed on /v1/models, but
                     POST /v1/chat/completions returns 503
                       "All providers failed: no provider in the chain was able
                        to handle the request"
      NOT THREE DEFECTS — one condition seen from three angles. Nothing is
      wrong with the request paths; there is no local inference backend running.
      ollama is NOT installed on this host (127.0.0.1:11434 refused), and while
      /usr/bin/llama-server and /usr/bin/llama-cli ARE installed, no llama-server
      process is running and no GGUF weights exist anywhere on the box.
      WORTH SAYING PLAINLY: every one of those three responses is CORRECT
      behaviour. None of them pretends to serve. A system that answered a
      generate request with something plausible here would be the bluff; three
      honest refusals naming their own cause is what the anti-bluff work was
      for.
      TO CLOSE IT: place a GGUF this host can run (RTX 3060 / 12 GB — an 8B
      Q4_K_M at ~4.9 GB fits), start llama-server, point HELIX_LLM_LOCAL_MODEL
      at it, and re-run the toolkit export. That is an operator decision (a
      multi-GB fetch and a model preference), deliberately not taken here.
      NARROWED 2026-09-03 — the PREMISE is refuted and two of the three
      symptoms are gone. There IS a local inference backend now: llama-server
      on :18434 serving qwen2.5-coder-3b (pid 3662629) with real GGUFs on
      disk. helixllm reports `availability: serving`, not withheld. helixagent
      returns a REAL completion — POST /v1/chat/completions with model
      `helix-llm` answered `{"content":"OK"}`, usage 8+2 tokens, journal
      `[Provider Chain] Fallback provider succeeded provider=codestral`,
      `200 | 12.8s` — not the 503 this row recorded.
      AMBIGUOUS, the one part not settled: the helix_code 502 naming
      `provider: ollama` could not be re-probed, because
      POST /api/v1/llm/generate answers 401 `Authorization header required`
      and getting a token means registering a user (a mutation, deliberately
      not performed). What IS fact: 127.0.0.1:11434 still refuses (ollama is
      still absent), `helix_code/config/config.yaml` now has
      `ollama: enabled: false` but `local:` still points at
      `http://localhost:11434` with `enabled: true`, and
      /api/v1/llm/providers still lists ollama as `status: available` — a
      config-derived status, not a probed one, since the port refuses.
    severity: MEDIUM
    status: open — narrowed to one unprobed symptom; the backend now exists and 2 of 3 symptoms are refuted
  - id: ROOT-1
    title: the toolkit points all three Helix providers at ports nothing listens on
    detail: |
      OPERATOR REPORT: `claude-providers list` shows 21 verified providers and
      NOT ONE HelixAgent or HelixLLM entry.
      Root-caused via superpowers:systematic-debugging Phase 1 rather than
      guessed. Two facts compose into the symptom.
      (1) `list` is verified-only BY DESIGN —
            scripts/claude-providers.sh:2084  cmd_list() { _list_rows verified; }
          so anything not `verified` is simply invisible there. `list-all` does
          show all three, as `unverified` / layer `existence`.
      (2) They can never BECOME verified, because their configured endpoints are
          dead. Measured, configured-vs-reality:
            helixagent         http://127.0.0.1:18434/v1   curl -> 000
            helixagent-native  http://127.0.0.1:18435      curl -> 000
            helixllm-gateway   http://127.0.0.1:18435/v1   curl -> 000
          while `ss -ltnp` shows what actually listens:
            helixagent  *:7061  (OpenAI-compatible, /v1/models 200, 5 models)
            helixagent  *:8111  (liveness probe only)
            helixllm    *:8443  (HTTPS, self-signed)
          helixllm-gateway is doubly wrong — wrong port AND wrong scheme; plain
          http against :8443 returns "400 Client sent an HTTP request to an
          HTTPS server".
          Corroboration: there is no `*_verified.json` for any of the three in
          the providers dir, while 20+ other providers each have one. The
          verification step never produced a result at all.
      The values are hardcoded in TRACKED config:
        scripts/providers/helixagent.json
        scripts/providers/helixagent-native.json
        scripts/providers/helixllm-gateway.json
        scripts/claude-providers.sh ~427, ~455, ~556 (fallback defaults)
        scripts/lib.sh:2189 (operator-facing message naming 18434)
      DRIFT, not ignorance: the rest of the tree already knows the real ports —
        submodules/challenges/.../agentic_subagents_challenge.sh:48
          HELIXAGENT_ENDPOINT:-http://localhost:7061/v1/chat/completions
        constitution/scripts/helix_code/helix_code_services.sh:145
          "HelixLLM gateway (:8443)"
      so the toolkit's own provider definitions are the only place carrying the
      wrong numbers. Same class as §11.4.111 — a hardcoded value where the
      endpoint should be resolved or configured.
      HONEST BOUNDARY, and it matters: fixing the ports is necessary but NOT
      sufficient for the operator's actual goal. Even reachable, neither backend
      can complete — helixagent 503s with 0 upstream providers and helixllm
      withholds its one model for want of weights (BOOT-9). Correct endpoints
      turn "cannot connect" into "connected, cannot serve", which is progress
      and is honest, but a provider only flips to `verified` once something
      genuinely completes a request.
      NARROWED 2026-09-03 — the tracked values are UNCHANGED (all three files
      clean in git), so the core finding stands: `helixagent.json` 18434/v1,
      `helixagent-native.json` 18435, `helixllm-gateway.json` 18435/v1,
      `claude-providers.sh:400,428`, `lib.sh:2189`. But "ports nothing
      listens on" is no longer accurate for :18434 — it now answers 200, and
      that makes the finding WORSE not better: what answers there is
      llama-server (`owned_by: llamacpp`, model qwen2.5-coder-3b), NOT
      helixagent, so `helixagent.json` now points at a LIVE BUT WRONG
      service and will connect and mis-serve rather than fail loudly.
      :18435 remains genuinely dead (curl 000 on both paths). The real
      listeners this row names — 7061, 8111, 8443 — are all confirmed
      present.
      ONE SUB-CLAIM NOT REPRODUCIBLE: the corroboration "no *_verified.json
      for any of the three while 20+ other providers each have one" cannot be
      re-checked — `ls scripts/providers/*_verified.json | wc -l` is 0 for
      EVERY provider, so the contrast no longer exists. The absence for the
      three Helix entries holds; the "20+ others have one" half does not.
    severity: HIGH
    status: open — narrowed; the three tracked base_urls are unchanged and 18434 now resolves to the wrong live service
  - id: ROOT-2
    title: helixagent discovers zero providers because 33 of 34 matching key vars are absent from its environment
    detail: |
      The second half of why nothing is usable. helixagent returns
        503 {"type":"no_provider_available",
             "message":"All providers failed: no provider in the chain was able
                        to handle the request"}
      and its startup pipeline logs "Discovered providers count=0 / Verified
      providers verified=0" after checking 44 candidates.
      I first recorded this (BOOT-7) as "the operator's keys live somewhere
      helixagent does not look", implying a NAME mismatch. That was wrong, and
      the correction matters because it changes the fix from "teach it new
      names" to "give it the file".
      MEASURED deterministically (LC_ALL=C; the default locale's collation
      silently mis-sorted the first attempt and comm warned about it — worth
      remembering, an unsorted comm quietly produces a wrong intersection):
        helixagent scans      137 candidate provider env-var names
                              (impl: internal/verifier/startup.go)
        operator keys file has 46 key vars
        INTERSECTION          34
        present in the running helixagent process env:  1
        ABSENT:                                        33
      So the names ALREADY AGREE. helixagent looks for exactly what the
      operator has. The 33 absent ones include DEEPSEEK_API_KEY, GROQ_API_KEY,
      MISTRAL_API_KEY, OPENROUTER_API_KEY, NVIDIA_API_KEY, HUGGINGFACE_API_KEY,
      GEMINI_API_KEY, CHUTES_API_KEY, CEREBRAS_API_KEY, FIREWORKS_API_KEY,
      KIMI_API_KEY, ZAI_API_KEY — and the Claude Toolkit independently verifies
      providers backed by those same keys as working, so they are live.
      CAUSE IS MINE: I started helixagent by hand with only the .env I had just
      created for it (JWT_SECRET). The provider keys live in the keys file the
      toolkit reads ($HOME/api_keys.sh by default, CMA_KEYS_FILE overridable),
      and helixagent never saw it.
      FIX: the helixagent unit needs BOTH env sources. Routed to the systemd
      stream, with the caution that systemd EnvironmentFile parses KEY=value
      only and does NOT execute shell — if the keys file uses `export` or any
      interpolation, it must be sourced via an ExecStart wrapper or converted,
      not pointed at naively.
      WHY IT MATTERS BEYOND helixagent: the toolkit can only mark a provider
      `verified` once something genuinely completes a request. ROOT-1 fixes
      "cannot connect"; this fixes "connected but nothing upstream to serve".
      Both are needed before the operator's `claude-providers list` shows a
      Helix entry.
      VERIFIED 2026-09-03 — the routed fix landed, and it landed the careful
      way this row asked for. `helixagent.service:118` runs
      `ExecStart=@HELIX_ROOT@/scripts/systemd/exec-with-api-keys.sh
      .../bin/helixagent` — a SOURCING WRAPPER, not a naive EnvironmentFile,
      exactly the caution recorded above (`exec-with-api-keys.sh:53`:
      `readonly KEYS_FILE="${CMA_KEYS_FILE:-${HOME}/api_keys.sh}"`). All 12
      key names this row listed as ABSENT are now present in the running
      process environment read from `/proc/315821/environ` (48 distinct
      *_KEY/*_TOKEN names in total; names only, never values). The counters
      moved with it: `Discovered providers count=32`,
      `Verified providers verified=32`.
    severity: HIGH
    status: closed - the sourcing-wrapper unit landed and all 12 named-absent keys are in the running env, ledger corrected

  - id: OPEN-3-resolved
    title: the agent lane was migrated after OPEN-3 was written; the finding was stale, not the code
    detail: |
      OPEN-3 asked for an operator decision between two options — migrate the
      lane, or fix only the compose default. The decision is MOOT: the fuller
      option was already taken. Commits 0c43b11 ("stop making an operator keep
      the model and its VRAM claim in step by hand") and abbc0d2 ("one shared
      model-selection decision, not four copies") migrated the lane, and the
      ledger was never updated. Every specific claim in OPEN-3 is now false,
      checked one at a time against the current source rather than inferred:
        "NO modelchoice.go at all"
            -> cmd/agentgen-boot/modelchoice.go exists and is tracked.
        "main.go:118 seeds AGENTGEN_MODEL_GGUF"
            -> main.go:102 declares it as `weightsKey`, a compose
               interpolation OUTPUT; main.go:234 applyChoice() OVERWRITES any
               inherited value and prints IGNORED-CONFIG naming it. The
               decision itself is main.go:181, laneboot.Decide().
        "compose.agent.yml:44 bakes Mistral-Nemo-Instruct-2407-Q4_K_M.gguf"
            -> compose.agent.yml:46 is now
               `- /models/${AGENTGEN_MODEL_GGUF}   # DECIDED — no default`.
               The only surviving occurrence of that filename in the lane is
               compose_model_default_test.go:18, where the removed default is
               QUOTED as the defect the test guards against.
      SO THE LANE IS NOT A FOURTH COPY EITHER: the decision moved into
      internal/laneboot, which all four boot lanes now call (agentgen,
      visiongen, imagegen, videogen modelchoice.go + main.go). The lane keeps
      only the half that is genuinely its own — locating a chosen build's GGUF
      (model id AND variant, so a q8 file is never admitted for a q4 decision,
      and an mmproj is never mistaken for text weights) and its own
      weights-not-present refusal, exit 24.
      EVIDENCE THAT IT SELECTS, RATHER THAN THAT A FILE EXISTS.
      `go test ./cmd/agentgen-boot/... -count=1` passes, and the run prints the
      refusals rather than merely returning ok:
        TestStaticallyNamedFileIsNeverBooted   — measured host=anton, 5 options
          WITHHELD on memory with per-option shortfalls, 1 servable, its
          weights absent -> CANNOT-CHOOSE, nothing started.
        TestDecisionOverwritesConfiguredNames  — AGENTGEN_MODEL_GGUF=
          "statically-named-model.gguf" and AGENTGEN_NEED_BYTES=99999999999
          both reported IGNORED-CONFIG and replaced by the measured choice.
        TestPinIsAConstraintNotABypass         — the pin is measured first and
          REFUSED (unsupported_configuration, remedy=different-approach).
        TestMeasurementFailureIsReportedAndRefused — unmeasured host exit 20,
          30s-old reading exit 21. No fixed default in either path.
        TestProjectorIsNeverMistakenForWeights, TestWithheldReasonsStayDistinct,
        TestEnvironmentDoesNotChangeTheDecision, TestNoFixedDefaultWhenThe
        HostCannotBeChosenFor — all PASS.
      THE GUARD IS FALSIFIABLE, PROVEN BY §1.1 PAIRED MUTATION rather than
      asserted. Re-introducing the EXACT pre-fix default from OPEN-3 into
      compose.agent.yml:46 makes all three compose-layer guards FAIL:
        TestComposeSuppliesNoModelDefault        FAIL (":-" default)
        TestComposeNamesNoModelArtefact          FAIL (names the .gguf)
        TestComposeDefaultsNothingThatSelectsAModel FAIL
      Mutation restored the same turn, tree verified clean and residue-scanned
      before any commit (§11.4.84); guards re-run GREEN on the restored file.
      WHY NO RED TEST WAS AUTHORED. §11.4.115 requires a RED that reproduces
      the defect on the CURRENT artifact. This defect is already fixed, so a
      RED cannot reproduce it — and per §11.4.115's own honest boundary, a RED
      that does not fail is a finding, which is exactly this entry. The
      standing guards landed with the fix ARE the polarity pair: their doc
      comments record the pre-fix defect, and the mutation above is the proof
      they still catch it.
      THE REAL DEFECT THIS ENTRY FIXES is the stale ledger row, and it was not
      harmless: a finding asserting "NOT FIXED / operator decision required"
      about migrated code dispatched an agent to redo completed work — the
      §11.4.60 redundant-work and false-confidence failure mode, at the ledger
      layer rather than the git layer.
      STILL GENUINELY OPEN, and NOT closed by this entry: OPEN-24 (the lane's
      three former candidates — Mistral-Nemo-Instruct-2407, GLM-4.7-Flash,
      DeepSeek-Coder-V2-Lite — are not in the catalogue, so an operator holding
      a working Mistral-Nemo GGUF is now REFUSED rather than served; that is a
      real §11.4.122 capability narrowing and needs the operator, not an
      agent), and OPEN-25 (five of six text entries carry
      requires_accelerator: false, so a host-RAM figure reaches a VRAM broker).
      OPEN-25 is visible in this lane's own test output — the host measures
      accelerators=1 while options are withheld on MEMORY — but it is a
      catalogue/selection-semantics change owned elsewhere and deliberately
      untouched here.
    severity: HIGH
    status: "closed - the lane was already migrated (0c43b11, abbc0d2); verified by test run + paired mutation, ledger corrected"

  - id: OPEN-2-resolved
    title: the ranking divergence was already fixed 43 minutes before OPEN-2 was written; the two paths are now bound by a test
    detail: >-
      OPEN-2 asked for a decision it framed as outstanding. The decision is
      MOOT: the fix had already landed. Every specific claim is now false,
      checked against current source and then PROVEN BY RUNNING BOTH
      IMPLEMENTATIONS rather than inferred from reading.
      "Go's selection.Select returns offers in CATALOGUE order"
        -> internal/selection/family.go:114 sortOffered() sorts
           memory, then storage, then the catalogue identity. It landed in
           064dd75 at 2026-09-02 22:02:46; OPEN-2 was recorded in 44ff9b10 at
           22:45:09 — 43 minutes LATER. The finding was written against a
           stale reading of the code, not against the code.
      "leaves preference to the lane — videogen-boot's prefer() sorts
      MOST-CAPABLE-first"
        -> no prefer() exists anywhere under cmd/. The per-lane ordering was
           removed when the ordering moved into selection.
      "the same host picks q4_k_m through the Python path and f16 through the
      Go path"
        -> REPRODUCTION ATTEMPTED AND FAILED, which is the finding. Both
           implementations were run over the SAME catalogue YAML
           (internal/catalogue/data/*.yaml, which both read) and the same
           processor-only host (44 GiB free RAM, 1024 GiB disk, personal
           usage): Go via catalogue.Load + selection.Select, Python via
           load_catalogue + evaluate + select. All EIGHT families came back
           in BYTE-IDENTICAL order. The distinguishing input the finding
           names does exist — the embedding family carries the same model in
           two variants, nomic-embed-text-v1.5:q4_k_m (cheap) and
           nomic-embed-text-v1.5:f16 (dear) — and BOTH paths choose q4_k_m.
      THE DIRECTION WAS SETTLED THE OTHER WAY, and this matters because
      OPEN-2 quotes the gate's header ("Go is the source of truth") as
      settling it toward Go's ranking. Go's ranking was the defect. The gate's
      docstring records the resolution: GO WAS CHANGED TO MATCH THIS MODULE,
      because the cheapest option that genuinely runs is the one that leaves
      headroom for CO-RESIDENT models — a host serves a coder model beside a
      vision or video one on the same accelerator (internal/vrambroker), and
      taking the largest thing that fits is how it ends up unable to load the
      second. The docstring explicitly warns against "fixing" the ranking
      back to a most-capable-first lane. Go remains authoritative in general
      AND now carries the correct policy, so there is no conflict and nothing
      on the Python side needed to change.
      WHAT WAS GENUINELY MISSING was a mechanical binding. THREE places
      asserted the two agree IN PROSE — sortOffered's doc comment,
      offer_order_test.go's header ("It mirrors select() in
      container/helix_model_gate.py"), and the gate's module docstring — and
      prose does not fail a build when someone edits one side. That is the
      same drift seam internal/serviceconfig/precision_agreement_test.go
      closes for precisions, and this entry closes it for the ranking.
      FIXED with internal/selection/python_gate_agreement_test.go
      (TestGoOfferOrderingAgreesWithThePythonGate), following that
      precedent's mechanism: parse the other language's source for the
      authoritative facts, compare, fail loudly. It extracts the ordered
      comparison AXES and, on each axis, the DIRECTION, from both
      family.go's sortOffered chain and the gate's sort key. It prints the
      comparison it made:
        go sortOffered=[memory:asc then storage:asc then identity:asc]
        python gate select()=[memory:asc then storage:asc then identity:asc]
        (agree)
      DIRECTION IS THE LOAD-BEARING HALF. Most-capable-first is not a
      different set of axes, it is the same axes with the inequality flipped,
      so a test comparing only memory-then-storage-then-identity would have
      passed throughout the original defect. The test reads Go's < vs > and
      Python's reverse=True / field negation.
      FALSIFIABLE, PROVEN BY THREE §1.1 PAIRED MUTATIONS, each restored
      byte-identical with git diff --exit-code and a go clean -testcache
      before the restored re-run:
        1. gate reverse=True (the historical most-capable-first defect)
           -> FAIL: py [memory:desc ...] vs go [memory:asc ...]. And the
              mutated gate REALLY DOES change what is served: re-running the
              differential under mutation, the gate chose
              nomic-embed-text-v1.5:f16 where Go chooses q4_k_m — the exact
              divergence OPEN-2 described. So the test guards behaviour, not
              a string.
        2. gate memory/storage axes swapped -> FAIL: py [storage:asc first].
        3. Go memory '<' changed to '>' -> FAIL: go [memory:desc]. The
           binding is SYMMETRIC — it catches drift on either side, not only
           in the Python.
      A parser that stops recognising either side's shape calls t.Fatal
      naming what it could not read, rather than parsing nothing and
      reporting agreement.
      WHY NO RED TEST WAS AUTHORED. §11.4.115 requires a RED that reproduces
      the defect on the CURRENT artifact; this defect is already fixed, so a
      RED cannot reproduce it, and per §11.4.115's own honest boundary a RED
      that does not fail is a finding — which is this entry. Mutation 1 is
      the polarity pair: it restores the historical defect and the guard
      fails on it.
      EVIDENCE. go build ./... exit 0. internal/selection 87 PASS,
      internal/catalogue + internal/serviceconfig ok.
      container/test_helix_model_gate.py 25 tests OK. All re-run on the
      MERGED main (c45b7a1) from a sibling path where go.mod's relative
      replace directives resolve, not only on the pre-merge base.
      TWO ADJACENT FACTS FOR WHOEVER READS THIS NEXT, neither mine to fix.
      (a) `go test ./...` in helix_llm currently refuses with "updates to
      go.mod needed"; localised to a missing require for
      github.com/golang-jwt/jwt/v5 v5.3.1, i.e. the in-flight JWT work, NOT
      this change — confirmed by removing this file and seeing the refusal
      persist. go.mod was restored byte-identical after the diagnosis.
      (b) WHY TWO IMPLEMENTATIONS EXIST AT ALL, since OPEN-2 implies
      duplication: they are not duplicates of the same deployment. The Go
      internal/selection is the in-process decision every *-boot lane makes
      before it starts a container. container/helix_model_gate.py is the
      SHARED VOCABULARY for the three audio services (stt_catalogue_wiring,
      services/tts, services/audio) that run INSIDE those containers, where
      the Go packages are not present — its own docstring records that it
      exists because a previous attempt split those three across separate
      efforts and produced four mutually incompatible schemas. So it is a
      legitimate second implementation for a second deployment context, and
      the right response is a binding test, not a deletion (§11.4.124 would
      require a git-history investigation before removing either, and the
      history says the gate was built on purpose).
    severity: MEDIUM
    status: "closed - the divergence does not reproduce (fixed by 064dd75, 43 min before the finding was written); ranking now bound by TestGoOfferOrderingAgreesWithThePythonGate, proven by 3 paired mutations; ledger corrected"
